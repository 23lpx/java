---
category: 学习任务
phase: 第一轮
day: 10
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第十天学习任务与闭卷题单

> Day 10 按各线真实进度推进：Java、Spring、Web/项目各学习 10 道新题；已完成编号节点收口的 MySQL、Redis 转入综合场景实战；算法线进入堆与第 K 大元素。

导航：[[00-总目录]] · [[00-第一轮学习队列]] · [[00-第九天学习任务与闭卷题单]] · [[00-每日学习与闭卷考核模板]]

## 开始前

- [ ] 复制 [[00-每日学习与闭卷考核模板]]，以当天日期命名。
- [ ] 从 Day 9 记录中为每条学习线选择一个 `不会` 或 `不稳` 断点。
- [ ] 如果某条线连续失败题超过 2 道，本日先修复最靠前的核心断点，再决定是否执行综合场景。
- [ ] Day 9 尚未执行时，先完成 [[00-第九天学习任务与闭卷题单]]。

## Day 9 回流槽

| 学习线 | 回流题号 | Day 9 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 / 实战 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | [[01-Java基础/07-Java8]] 题 91～96；[[08-JVM/01-JVM基础]] 题 472～475 | 10 / 0 | 区分 Stream 中间/终止操作、元素副作用与 Optional 边界，再建立 JVM、JDK/JRE、运行时数据区和线程私有区域的概念图 |
| ② Spring 与数据访问 | [[02-Spring/08-Spring事务]] 题 187～196 | 10 / 0 | 串联持久性、声明式事务、代理边界、默认回滚规则、rollbackFor、异常捕获和自调用失效 |
| ③ MySQL | 回查 [[04-MySQL/03-索引#253. InnoDB 为什么使用 B+Tree？|253]]、[[04-MySQL/03-索引#263. 什么是覆盖索引？|263]]、[[04-MySQL/03-索引#267. 联合索引 `(a,b,c)` 哪些查询可以使用索引？|267]]、[[04-MySQL/03-索引#272. 什么是 EXPLAIN？|272]]、[[04-MySQL/05-锁#301. 乐观锁通常如何实现？|301]]、[[04-MySQL/05-锁#304. 如何降低死锁发生概率？|304]]；完成查询与并发联合演练 | 0 / 1 场景 | 为订单查询提出候选联合索引并用执行计划验证，再设计原子状态更新、一致加锁顺序和有限重试，不凭索引名称直接下结论 |
| ④ Redis | 回查 [[05-Redis/03-Redis缓存#330. 什么是 Cache Aside 模式？|330]]、[[05-Redis/03-Redis缓存#340. 如何降低缓存和数据库不一致的概率？|340]]、[[05-Redis/04-缓存穿透击穿雪崩#347. 缓存空值如何解决缓存穿透？|347]]、[[05-Redis/04-缓存穿透击穿雪崩#353. 如何解决缓存击穿？|353]]、[[05-Redis/04-缓存穿透击穿雪崩#359. TTL 加随机值有什么作用？|359]]、[[05-Redis/05-RedisTemplate与SpringCache#372. 为什么项目中同时使用 RedisTemplate 和 Spring Cache？|372]]；完成缓存故障推演 | 0 / 1 场景 | 从正常 Cache Aside 链路逐步加入无效 Key、热点过期、删除失败和 Redis 不可用，分别给出边界清楚的缓解方案 |
| ⑤ Web、项目与工程 | [[07-项目/03-第三方支付]] 题 471；[[06-Web/04-TCP与网络]] 题 523～531 | 10 / 0 | 用最终一致性收口支付链路，再串联 TCP/UDP、可靠传输、连接建立与关闭、TIME_WAIT、DNS 和 URL 请求链 |
| ⑥ 算法与手写 | 回流 [[12-算法/02-算法基础#648. 堆通常解决什么问题？|题 648]]；数组第 K 大元素 | 0 + 实战 | 维护容量为 k 的最小堆，说明堆顶语义、淘汰规则、重复元素口径以及 `O(n log k)` 的适用边界 |

> MySQL 与 Redis 本日为综合实战，不增加编号题，也不重复计算覆盖量。场景答案必须引用已校准知识节点，并把“执行计划或监控证据”“通用方案”“项目真实实现”分开表述。

### MySQL 线综合场景

给定订单表核心字段：`id`、`user_id`、`status`、`created_at`、`amount`，需要查询某用户某状态下最近的 20 个订单：

```sql
SELECT id, amount
FROM orders
WHERE user_id = ? AND status = ?
ORDER BY created_at DESC
LIMIT 20;
```

必须完成：

1. 提出候选联合索引，并解释等值列、排序列、主键和 `amount` 是否覆盖查询；不能把候选方案说成未经验证的最终索引。
2. 给出 `EXPLAIN` 的检查项；若使用 `EXPLAIN ANALYZE`，说明它会真实执行语句，线上使用必须控制副作用和成本。
3. 为“未支付 → 已支付”写出带旧状态条件的原子 `UPDATE`，并根据受影响行数区分首次成功、重复、记录不存在或非法状态。
4. 说明批量更新多笔订单时如何通过统一主键顺序、短事务和有限重试降低死锁概率。

### Redis 线综合场景

以菜品分类缓存 `dish:category:{id}` 为例，依次推演：正常读缓存、无效分类 ID 高频请求、热点 Key 同时过期、数据库更新成功但删缓存失败、Redis 临时不可用。

必须完成：

1. 画出正常 Cache Aside 读写链，并明确数据库是事实来源。
2. 对无效 Key 说明参数校验、空值缓存或布隆过滤器的选择条件和误判/一致性边界。
3. 对热点过期说明请求合并、互斥重建或逻辑过期的取舍，不能承诺绝对不击穿。
4. 对删除失败说明可靠重试、Outbox/CDC、监控和 TTL 兜底，不能把延迟双删说成强一致。
5. 对 Redis 不可用说明限流、降级、回源保护和故障拓扑；简历未证明的 Sentinel/Cluster 部署只能作为通用方案。

### 算法线编码要求

实现 `int findKthLargest(int[] nums, int k)`。输入数组非 `null`、长度至少为 1，满足 `1 <= k <= nums.length`，元素可以重复；返回按降序排列后的第 k 个元素，重复值分别占据名次。必须使用容量不超过 k 的最小堆，不得修改输入数组。

至少验证：

```text
nums = [3, 2, 1, 5, 6, 4], k = 2           -> 5
nums = [3, 2, 3, 1, 2, 4, 5, 5, 6], k = 4 -> 4
nums = [-1], k = 1                          -> -1
nums = [2, 2, 2], k = 2                     -> 2
nums = [-3, -1, -2], k = 3                  -> -3
```

完成代码后必须口述：为什么堆中始终保留当前最大的 k 个元素、容量超过 k 时为什么淘汰堆顶、遍历结束后堆顶为什么恰好是第 k 大，以及时间复杂度 `O(n log k)`、额外空间 `O(k)`。还要说明 k 接近 n 时排序或快速选择可能更合适。

### 每线完成条件

- [ ] Day 9 回流题已重新闭卷回答，并记录修复后的结果。
- [ ] Java、Spring、Web/项目的新题至少完成一次脱稿回答或职责链输出。
- [ ] MySQL 场景同时包含索引候选、执行计划证据、原子更新和死锁边界。
- [ ] Redis 场景覆盖五种状态，并区分真实项目、通用方案与部署核验项。
- [ ] 支付最终一致性没有被表述为跨支付宝和本地数据库的强一致。
- [ ] 第 K 大代码通过全部用例，并能证明堆顶语义。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-每日学习与闭卷考核模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[08-JVM/01-JVM基础#474. JVM 运行时内存区域有哪些？|题 474：JVM 运行时内存区域有哪些？]]

- 主问题：列出规范中的运行时数据区，并说明线程私有与共享关系。
- 追问 1：HotSpot 元空间、方法区、运行时常量池和直接内存分别是什么关系？
- 追问 2：为什么排查内存问题不能看到 OOM 就只调大 `-Xmx`？

### ② Spring 与数据访问

[[02-Spring/08-Spring事务#192. Spring 默认哪些异常会触发回滚？|题 192：Spring 默认哪些异常会触发回滚？]]

- 主问题：说明传统默认回滚规则及其判断发生的代理边界。
- 追问 1：受检异常、`rollbackFor`、异常被 catch 后正常返回分别如何影响回滚？
- 追问 2：Spring 6.2 的全局回滚配置为什么不能被当成所有项目的默认事实？

### ③ MySQL

[[04-MySQL/03-索引#272. 什么是 EXPLAIN？|题 272：什么是 EXPLAIN？]]

- 主问题：说明 EXPLAIN 能提供哪些执行计划证据，以及传统 EXPLAIN 的估算性质。
- 场景追问：针对白天订单查询，候选索引如何帮助过滤和排序，为什么仍要结合 `type`、`key`、`rows`、`Extra` 与真实数据验证？
- 边界追问：`EXPLAIN ANALYZE` 与传统 EXPLAIN 有什么关键区别，为什么不能随意用于有副作用或线上高成本语句？

### ④ Redis

[[05-Redis/03-Redis缓存#340. 如何降低缓存和数据库不一致的概率？|题 340：如何降低缓存和数据库不一致的概率？]]

- 主问题：从提交后失效、可靠重试/事件、TTL、请求合并与监控说明组合方案。
- 场景追问：数据库已提交但删除失败时，怎样让状态最终收敛？
- 边界追问：为什么 TTL、MQ、分布式锁和延迟双删都不能单独承诺强一致？

### ⑤ Web、项目与工程

[[07-项目/03-第三方支付#471. 支付成功但本地订单状态更新失败怎么办？|题 471：支付成功但本地订单状态更新失败怎么办？]]

- 主问题：说明回滚、不返回成功应答、平台重试、主动查询/对账和幂等状态迁移。
- 追问 1：数据库已提交但成功应答丢失时，重复通知应如何处理？
- 追问 2：项目真实实现到哪里，主动查询、定时对账和异常订单处置为什么只能按生产级增强表述？

### ⑥ 算法与手写

[[12-算法/02-算法基础#648. 堆通常解决什么问题？|题 648：堆通常解决什么问题？]]

- 主问题：说明堆维护最值的能力、典型场景和主要操作复杂度。
- 手写：重新实现今天的第 K 大元素，不能查看白天代码。
- 追问：为什么求第 K 大使用容量为 k 的最小堆？重复值如何计名次？若要返回前 k 大且有序，还需要做什么？

## 评分规则

Java、Spring、Web/项目题每题 10 分：核心结论 2 分、原理链路 2 分、成立条件与边界 2 分、两层追问 2 分、场景或工程影响 2 分。

MySQL、Redis 场景题每题 10 分：核心链路 2 分、证据或监控 2 分、故障边界 2 分、方案取舍 2 分、项目真实性 2 分。

算法题 10 分：函数契约 2 分、堆不变量与淘汰规则 3 分、代码和返回值 2 分、边界用例 1 分、复杂度与替代方案 2 分。

总分 60 分：

- 48～60：整体通过；单题不足 8 分仍记为 `不稳`。
- 36～47：Day 11 保留对应回流槽，并减少失败学习线的新题量或场景复杂度。
- 0～35：暂停薄弱线扩题，优先修复重复失败的知识链。

## 考后动作

- [ ] 对照原笔记纠错，记录首个错误断点。
- [ ] 将六道题分别标记为 `不会` / `不稳` / `通过`。
- [ ] 仅根据闭卷证据更新相关知识笔记的 `status`。
- [ ] MySQL 与 Redis 场景未通过时，下一日继续修复失败链，不用新的场景掩盖问题。
- [ ] 把连续失败题和堆不变量的首个错误加入 Day 11 回流。
- [ ] 原笔记疑点单独记录，核验可靠资料后再创建维护 PR。
