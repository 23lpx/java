---
category: 学习任务
phase: 第一轮
day: 12
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第十二天学习任务与闭卷题单

> Day 12 继续按各线真实进度推进：Java、Spring/MyBatis、工程各学习 10 道新题；MySQL 训练 MVCC 与锁定读，Redis 训练分布式锁故障边界；算法线手写反转链表。

导航：[[00-总目录]] · [[00-第一轮学习队列]] · [[00-第十一天学习任务与闭卷题单]] · [[00-每日学习与闭卷考核模板]]

## 开始前

- [ ] 复制 [[00-每日学习与闭卷考核模板]]，以当天日期命名。
- [ ] 从 Day 11 记录中为每条学习线选择一个 `不会` 或 `不稳` 断点。
- [ ] 如果某条线连续失败题超过 2 道，本日先修复最靠前的核心断点，再决定是否执行综合场景。
- [ ] Day 11 尚未执行时，先完成 [[00-第十一天学习任务与闭卷题单]]。

## Day 11 回流槽

| 学习线 | 回流题号 | Day 11 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 / 实战 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | [[08-JVM/02-GC与类加载]] 题 486～493；[[09-Java并发/01-线程基础]] 题 494～495 | 10 / 0 | 串联 GC 必要性、可达性、引用类型、类加载阶段和委派边界，再区分进程、线程以及平台/虚拟线程的基本位置 |
| ② Spring 与数据访问 | [[03-MyBatis/01-MyBatis]] 题 207～216 | 10 / 0 | 区分参数绑定与文本替换，建立动态 SQL 的 if/where/set/foreach 组合规则，并说明批量插入的语句规模与事务边界 |
| ③ MySQL | 回查 [[04-MySQL/04-事务#281. READ COMMITTED 和 REPEATABLE READ 有什么区别？|281]]、[[04-MySQL/04-事务#283. 什么是 MVCC？|283]]、[[04-MySQL/04-事务#286. 什么是 Read View？|286]]、[[04-MySQL/05-锁#296. 什么是行锁？|296]]、[[04-MySQL/05-锁#299. 什么是悲观锁？|299]]、[[04-MySQL/05-锁#301. 乐观锁通常如何实现？|301]]；完成快照读与锁定读演练 | 0 / 1 场景 | 用两个并发事务解释 RC/RR 的 Read View、普通 SELECT 与 FOR UPDATE、条件更新和实际锁范围，避免把 MVCC 说成无锁 |
| ④ Redis | 回查 [[05-Redis/04-缓存穿透击穿雪崩#350. 什么是缓存击穿？|350]]、[[05-Redis/04-缓存穿透击穿雪崩#353. 如何解决缓存击穿？|353]]、[[05-Redis/07-Redis进阶#587. 什么是 Redis 分布式锁？|587]]、[[05-Redis/07-Redis进阶#588. 为什么 `SETNX` 可以实现简单分布式锁？|588]]、[[05-Redis/07-Redis进阶#589. 简单的 `SETNX` 分布式锁存在哪些问题？|589]]、[[05-Redis/07-Redis进阶#590. Redisson 是什么？|590]]；完成分布式锁故障演练 | 0 / 1 场景 | 从热点重建互斥进入租约、唯一 token、安全释放、超时旧持有者、续租和主从切换，并保留资源侧兜底 |
| ⑤ Web、项目与工程 | [[10-开发基础/01-日志]] 题 542～549；[[10-开发基础/02-Git]] 题 550～551 | 10 / 0 | 建立日志级别、SLF4J/Logback 分层和接口异常排查链，再区分工作区、暂存区与首次 Git 提交流程 |
| ⑥ 算法与手写 | 回流 [[12-算法/02-算法基础#638. 链表常见题型有哪些？|题 638]]；原地反转单链表 | 0 + 实战 | 用 prev、cur、next 维护已反转前缀与未处理后缀，说明保存后继、断链风险、节点身份和 `O(1)` 额外空间 |

> MySQL 与 Redis 本日仍为综合实战，不增加编号题，也不重复计算覆盖量。分布式锁和数据库锁都只能在明确故障模型与资源边界后使用，不能作为所有并发问题的默认答案。

### MySQL 线综合场景

订单初始状态为 `UNPAID`。事务 T1 使用 RR 开始事务并执行普通 `SELECT`；事务 T2 随后把订单更新为 `PAID` 并提交；T1 再次执行普通 `SELECT`，之后又执行 `SELECT ... FOR UPDATE`。

必须完成：

1. 说明 RR 下首次一致性读建立 Read View 后，T1 的后续普通一致性读为何通常仍看到旧版本；若改为 RC，Read View 时机有何变化。
2. 说明 `SELECT ... FOR UPDATE` 是锁定读/当前读，为什么可能看到比既有快照更新的已提交状态，并申请实际扫描范围上的锁。
3. 若业务要竞争 `UNPAID -> PAID`，分别给出悲观锁方案和 `UPDATE ... WHERE status = 'UNPAID'` 条件更新方案，并说明受影响行数为 0 不只代表重复成功。
4. 对范围锁定读说明 record/gap/next-key lock 受隔离级别、索引与查询条件影响；不能用“FOR UPDATE 只锁结果行”概括。
5. 说明事务内不应夹带耗时远程调用；发生死锁时应回滚完整事务、有限重试并保证外部副作用幂等。

### Redis 线综合场景

多个应用实例在热点 Key 失效后同时回源数据库。团队准备使用 Redis 锁，让一个实例重建缓存，其他实例有限等待或降级。

必须完成：

1. 定义锁 Key、随机唯一 token、租约时长、获取失败策略和临界区范围；基础获取必须使用单条 `SET key token NX PX ttl`。
2. 释放时原子校验 token 后删除；说明直接 `DEL` 为什么可能误删后来持有者的锁，以及 Redis 版本对 Lua/`DELEX` 方案的影响。
3. 推演持有者暂停超过 TTL 后恢复：token 校验只能防误删，不能阻止陈旧持有者继续写外部资源。
4. 说明 watchdog/续租可降低正常长任务提前过期概率，但不能消除长时间停顿、网络分区或续租线程故障。
5. 推演主节点在锁写入尚未复制时故障：新主节点可能没有该锁，因此异步主从切换不能保证绝对互斥。
6. 给出资源侧兜底：幂等、版本/状态条件、唯一约束或 fencing token；简历未证明使用 Redisson 或分布式锁时只能作为通用方案。

### 算法线编码要求

节点定义如下，实现 `ListNode reverseList(ListNode head)`。输入链表允许为空，保证无环；需要原地反转并返回新的头节点，复用所有原节点并保持节点值不变，只允许修改 `next` 指针。

```java
class ListNode {
    int val;
    ListNode next;
}
```

至少验证：

```text
head = null          -> null
head = [1]           -> [1]
head = [1, 2]        -> [2, 1]
head = [1, 2, 3, 4]  -> [4, 3, 2, 1]
head = [1, 1, 2]     -> [2, 1, 1]
```

完成代码后必须口述：循环开始时 `prev` 和 `cur` 分别表示什么、为什么修改 `cur.next` 前必须保存 `next`、循环结束后为什么 `prev` 是新头、旧头的 `next` 为什么最终为 `null`，以及时间复杂度 `O(n)`、额外空间 `O(1)`。还要验证反转后没有丢节点、没有新增节点、没有形成环。

### 每线完成条件

- [ ] Day 11 回流题已重新闭卷回答，并记录修复后的结果。
- [ ] Java、MyBatis、日志/Git 的新题至少完成一次脱稿回答或职责链输出。
- [ ] MySQL 场景区分快照读、锁定读、悲观锁、条件更新和锁范围。
- [ ] Redis 场景覆盖获取、释放、超时、续租、故障转移与资源侧兜底。
- [ ] 日志排查保留完整 cause 与关联上下文，且不输出敏感信息。
- [ ] 反转链表代码通过全部用例，并能证明节点可达性和循环不变量。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-每日学习与闭卷考核模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[08-JVM/02-GC与类加载#492. 什么是双亲委派机制？|题 492：什么是双亲委派机制？]]

- 主问题：说明常见 ClassLoader 的已加载检查、父加载器委派和 `findClass` 顺序。
- 追问 1：父加载器为什么不是 Java 继承中的父类，Bootstrap 在 Java API 中如何表示？
- 追问 2：插件、容器、SPI 和模块场景为什么可能采用不同委派策略，双亲委派又为何不保证全 JVM 类名唯一？

### ② Spring 与数据访问

[[03-MyBatis/01-MyBatis#208. 为什么 `#{}` 可以降低 SQL 注入风险？|题 208：为什么 #{} 可以降低 SQL 注入风险？]]

- 主问题：从 JDBC 参数标记、PreparedStatement 和 TypeHandler 说明结构与值的分离。
- 追问 1：参数绑定与手工转义有什么本质区别，为什么不等于数据库一定使用服务端预编译缓存？
- 追问 2：动态表名、列名和 ORDER BY 为什么仍需服务端白名单，不能直接交给 `${}`？

### ③ MySQL

[[04-MySQL/04-事务#281. READ COMMITTED 和 REPEATABLE READ 有什么区别？|题 281：READ COMMITTED 和 REPEATABLE READ 有什么区别？]]

- 主问题：从 Read View 时机和普通一致性读说明 RC 与 RR 的主要区别。
- 场景追问：为什么同一 RR 事务里的普通 SELECT 和后续 FOR UPDATE 可能看到不同版本？
- 边界追问：MVCC 为什么没有消除写锁、锁定读、范围锁和死锁？

### ④ Redis

[[05-Redis/07-Redis进阶#587. 什么是 Redis 分布式锁？|题 587：什么是 Redis 分布式锁？]]

- 主问题：把它定义为带租约的协作式互斥协议，说明 token、TTL 和安全释放。
- 场景追问：持有者暂停超过 TTL 后恢复时会发生什么，fencing token 如何在资源侧拒绝陈旧写？
- 边界追问：为什么主从故障转移、watchdog 或 Redisson 都不能自动提供绝对互斥？

### ⑤ Web、项目与工程

[[10-开发基础/01-日志#549. 如何通过日志排查接口异常？|题 549：如何通过日志排查接口异常？]]

- 主问题：按时间、环境、接口、关联 ID、首次异常、下游与数据库证据还原请求时间线。
- 追问 1：为什么最外层 500 或最后一行 ERROR 往往只是症状，cause 链与首次偏离更重要？
- 追问 2：如何兼顾排障上下文、避免重复 ERROR 和保护 Token、支付报文及个人信息？

### ⑥ 算法与手写

[[12-算法/02-算法基础#638. 链表常见题型有哪些？|题 638：链表常见题型有哪些？]]

- 主问题：说明反转、快慢指针、dummy、合并、判环等常见结构及各自的不变量。
- 手写：重新实现今天的原地反转链表，不能查看白天代码。
- 追问：若忘记先保存 `cur.next` 会发生什么？如何证明反转过程中所有节点始终可由 `prev` 或 `cur` 到达？

## 评分规则

Java、MyBatis、日志/Git题每题 10 分：核心结论 2 分、原理链路 2 分、成立条件与边界 2 分、两层追问 2 分、场景或工程影响 2 分。

MySQL、Redis 场景题每题 10 分：核心链路 2 分、故障时序 2 分、成立条件 2 分、方案取舍 2 分、项目真实性 2 分。

算法题 10 分：函数契约 1 分、循环不变量 3 分、指针更新和返回值 2 分、节点/环验证 2 分、复杂度 2 分。

总分 60 分：

- 48～60：整体通过；单题不足 8 分仍记为 `不稳`。
- 36～47：Day 13 保留对应回流槽，并减少失败学习线的新题量或场景复杂度。
- 0～35：暂停薄弱线扩题，优先修复重复失败的知识链。

## 考后动作

- [ ] 对照原笔记纠错，记录首个错误断点。
- [ ] 将六道题分别标记为 `不会` / `不稳` / `通过`。
- [ ] 仅根据闭卷证据更新相关知识笔记的 `status`。
- [ ] MySQL 与 Redis 场景未通过时，下一日继续修复失败链，不用新的场景掩盖问题。
- [ ] 把连续失败题和链表指针的首个错误加入 Day 13 回流。
- [ ] 原笔记疑点单独记录，核验可靠资料后再创建维护 PR。
