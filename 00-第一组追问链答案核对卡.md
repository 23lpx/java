---
category: 追问训练答案
phase: 第二轮
group: 1
status: 可使用
tags:
  - Java后端
  - 求职
  - 追问链
  - 答案核对
---

# 第一组追问链答案核对卡

> 本卡只在完成 [[00-第一组追问链训练题单]] 的闭卷首答后打开。示例用于核对结论、机制、边界、场景和证据，不是要求逐字背诵的唯一答案。

导航：[[00-总目录]] · [[00-P0母题与追问链地图]] · [[00-第一组追问链训练题单]] · [[00-追问链训练模板]] · [[00-第二轮学习队列]]

## 使用规则

1. 先保留自己的 30 / 60 / 120 秒首答和 L0～L4 现场记录，再看本卡。
2. 只标记第一个让答案失效的错误或缺失；后面碰巧说对不能覆盖前面的断点。
3. 示例答案允许换词和换顺序，但核心结论、成立边界与事实属性不能改变。
4. 项目场景必须区分真实简历主张、待核验实现和通用工程增强；本卡不会替项目补造证据。
5. 核对后把 A/B/C 结果写入 [[00-第二轮学习队列#六线证据队列]]，本卡本身不记录虚构成绩。

## ① Java：J4 线程池过载

知识依据：[[09-Java并发/02-锁与线程池#511. 什么是线程池？|511]]、[[09-Java并发/02-锁与线程池#514. ThreadPoolExecutor 有哪些核心参数？|514]]、[[09-Java并发/02-锁与线程池#517. workQueue 是什么？|517]]、[[09-Java并发/02-锁与线程池#518. 拒绝策略是什么？|518]]、[[09-Java并发/02-锁与线程池#519. 常见拒绝策略有哪些？|519]]、[[09-Java并发/02-锁与线程池#520. Tomcat 为什么使用线程池处理请求？|520]]。

### 30 秒参考回答

线程池把任务提交与线程生命周期分离，用受控的工作线程、队列和拒绝策略管理资源与过载，不只是“复用线程所以更快”。`ThreadPoolExecutor` 接收任务的典型顺序是：运行线程少于 core 时创建核心 worker，否则尝试入队；队列不能接收时再扩到 maximum，仍不能接收或线程池已关闭时执行拒绝策略。参数必须结合任务耗时、到达率、等待预算、内存和拒绝后的业务语义确定。

### 60 秒扩展要点

- 七个核心参数共同决定线程、队列和拒绝行为；核心线程默认按需创建，可显式预启动。
- 有界队列把容量边界显式化；无界队列通常使 maximum 难以生效，只是把拒绝推迟成排队延迟或内存压力。
- `AbortPolicy` 抛异常；`CallerRunsPolicy` 可形成同步反压，但会占用 HTTP 或事件线程，线程池关闭时也不会执行任务。
- 盲目调大 maximum 可能增加上下文切换、下游并发和连接池争用，不能修复慢依赖。
- Tomcat 的执行线程、连接数和接收队列是不同边界；Servlet 异步和虚拟线程也会改变“一请求一平台线程”的简化模型。

### 120 秒场景闭环

假设接口 P99 升高、任务队列持续接近容量上限、拒绝数增加，客户端又自动重试。先关联活跃线程、池大小、队列深度与等待时间、任务耗时、外部依赖耗时、连接池等待、超时和重试率，判断是服务能力不足、任务阻塞还是慢依赖拖住 worker。若依赖变慢，单纯扩线程会把更多并发压向故障点；应先限制超时和重试、做隔离或降级，再依据压测和容量预算调整线程与队列。验证时观察吞吐是否稳定、P95/P99 和排队时间是否下降、拒绝是否被业务正确感知，以及重试流量是否收敛。该场景是通用工程分析，不能在没有监控和压测证据时表述为个人调优成果。

> [!check]- L0～L4 核对
> - L0：说明线程池的资源管理与过载价值，而非只说线程复用。
> - L1：准确说出 `core → queue → max → reject`，并知道运行状态和队列类型会影响流程。
> - L2：能解释无界队列、CallerRuns 和扩大 maximum 的代价。
> - L3：从现象推进到线程池、任务和依赖三类证据，不先拍脑袋改参数。
> - L4：用延迟、队列、拒绝、依赖和重试指标验证，并明确通用场景边界。

## ② Spring：S1 单例 Bean 与请求状态

知识依据：[[02-Spring/01-Spring基础#99. 什么是 IoC？|99]]、[[02-Spring/01-Spring基础#101. 什么是 DI？|101]]、[[02-Spring/01-Spring基础#112. Spring Bean 默认是单例吗？|112]]、[[02-Spring/01-Spring基础#113. 单例 Bean 一定线程安全吗？|113]]、[[02-Spring/01-Spring基础#114. Spring Bean 有哪些常见作用域？|114]]、[[02-Spring/01-Spring基础#115. Spring Bean 生命周期大致是什么？|115]]、[[06-Web/02-ThreadLocal#402. 为什么项目中用 ThreadLocal 保存 userId / empId？|402]]、[[06-Web/02-ThreadLocal#407. 为什么 ThreadLocal 使用完需要 `remove()`？|407]]。

### 30 秒参考回答

IoC 是把对象创建、装配和生命周期控制交给容器，DI 是容器把协作者提供给对象的主要实现方式。Spring 默认 `singleton` 只表示“每个容器、每个 Bean 定义共享一个实例”，不表示 JVM 全局唯一，更不自动保证线程安全。无状态 Service 通常适合单例；请求身份等可变状态不能放进单例字段。

### 60 秒扩展要点

- 必需依赖优先构造器注入；多候选时仍需限定符、主候选或集合等选择规则。
- 单例 Bean 是否安全取决于共享可变字段、依赖契约和并发路径；“没有字段”也不能证明整个依赖链安全。
- prototype 是每次向容器请求时创建，不是每次方法调用自动新建，也不能作为通用并发修复。
- 典型生命周期包括实例化、依赖填充、Aware、初始化前后处理、初始化回调、使用和销毁；后处理器可能创建代理。
- ThreadLocal 只适合同一同步线程中的上下文关联；所有退出路径都要由建立上下文的边界清理，异步线程需显式传播并分别清理。

### 120 秒场景闭环

假设压测出现用户身份串号，先把它当作待诊断现象而不是项目既有事故。检查当前身份是否被写入单例字段，JWT 验证后 ThreadLocal 在哪些路径建立，认证拒绝、业务异常、提前返回和异步调度是否都能清理或重新建立上下文；同时核对日志中的线程、请求标识和用户标识。修复优先让业务 Bean 保持无状态，把领域必需的 userId 显式传入；确需请求上下文时，在最外层用可靠的 `try/finally` 或框架完成回调管理 ThreadLocal。最后用并发交错测试覆盖不同用户、异常和线程复用，验证每个请求只能读取自己的身份。简历可确认 JWT、双拦截器与 ThreadLocal 链路，但没有证据证明真实发生过串号事故。

> [!check]- L0～L4 核对
> - L0：分清 IoC、DI、Bean 与默认 singleton 的含义。
> - L1：解释共享实例为什么不等于线程安全，并能概述作用域和生命周期。
> - L2：说明 singleton 字段、prototype 和 ThreadLocal 三种做法各自的边界。
> - L3：覆盖建立、读取、异常清理、线程复用和异步切换。
> - L4：并发测试必须能制造交错并检查隔离，不能用“一次未复现”证明安全。

## ③ MySQL：M3 MVCC 与并发扣减

知识依据：[[04-MySQL/04-事务#277. MySQL 有哪些事务隔离级别？|277]]、[[04-MySQL/04-事务#281. READ COMMITTED 和 REPEATABLE READ 有什么区别？|281]]、[[04-MySQL/04-事务#283. 什么是 MVCC？|283]]、[[04-MySQL/04-事务#286. 什么是 Read View？|286]]、[[04-MySQL/05-锁#293. 为什么数据库需要锁？|293]]、[[04-MySQL/05-锁#296. 什么是行锁？|296]]、[[04-MySQL/05-锁#300. 什么是乐观锁？|300]]、[[04-MySQL/05-锁#301. 乐观锁通常如何实现？|301]]。

### 30 秒参考回答

MVCC 让 InnoDB 的普通一致性读按 Read View 选择记录版本，从而减少普通读与写之间的锁冲突；它不是锁，也不会把“先查库存、再扣减”两条业务语句自动变成原子操作。并发扣减仍要使用带库存或版本条件的原子 UPDATE，或在同一事务中使用正确索引的锁定读，并依据受影响行数或锁后状态判断结果。

### 60 秒扩展要点

- RC 每次普通一致性读使用新快照；RR 在同一事务中通常复用第一次一致性读建立的快照。
- `SELECT ... FOR UPDATE`、UPDATE 和 DELETE 不是普通旧快照读，需要读取可加锁或修改的较新状态。
- InnoDB 的行级锁跟随索引记录和扫描范围；没有合适索引可能锁住大量记录，但不是自动“升级表锁”。
- 条件更新可写成 `UPDATE ... SET stock = stock - 1 WHERE id = ? AND stock > 0`，影响 1 行才表示抢到库存。
- 版本号方案同样必须在一条 SQL 中比较并递增；影响 0 行还要区分记录不存在、冲突或非法状态，不能无限重试。

### 120 秒场景闭环

两个事务都用普通 SELECT 看到库存 1，并不能保证后续都安全。优先方案是把判断与扣减合成带条件的原子 UPDATE，影响 1 行后再在同一本地事务中写订单，影响 0 行返回库存不足或核对记录状态；这样冲突窗口短且容易根据行数判断。若业务必须在扣减前读取并校验多项状态，可在同一事务中按有索引条件 `SELECT ... FOR UPDATE`，随后更新，但要评估锁持有时间、范围和死锁。版本号适合显式检测冲突，但热点库存下频繁重试成本可能更高。验证要并发提交超过库存数量的请求，确认成功扣减数不超过初始库存、最终库存不为负、订单与扣减一致，并观察锁等待、死锁和有限重试指标。

> [!check]- L0～L4 核对
> - L0：说明 MVCC 解决一致性读问题，不替代写竞争控制。
> - L1：说清 RC/RR 快照时机，以及普通一致性读和锁定读的区别。
> - L2：解释隔离级别、MVCC 或一次 SELECT 为什么不能保证跨语句原子性。
> - L3：能比较条件更新、版本号和悲观锁，而不是只报方案名。
> - L4：验证超卖、负库存、订单一致性、锁等待、死锁与有限幂等重试。

## ④ Redis：R4 持久化与高可用

知识依据：[[05-Redis/06-持久化#374. Redis 为什么需要持久化？|374]]、[[05-Redis/06-持久化#375. RDB 是什么？|375]]、[[05-Redis/06-持久化#376. AOF 是什么？|376]]、[[05-Redis/06-持久化#377. RDB 和 AOF 有什么区别？|377]]、[[05-Redis/06-持久化#380. Redis 重启后如何恢复数据？|380]]、[[05-Redis/07-Redis进阶#583. Redis 主从复制是什么？|583]]、[[05-Redis/07-Redis进阶#585. Redis Sentinel 是什么？|585]]、[[05-Redis/07-Redis进阶#586. Redis Cluster 是什么？|586]]。

### 30 秒参考回答

RDB 和 AOF 主要解决实例重启后的数据恢复：RDB 是时点快照，AOF 记录 base 状态和增量变更；主从复制提供当前数据副本，Sentinel 为非分片主从做监控和故障转移，Cluster 同时提供分片与故障转移。它们解决的问题不同，持久化不等于高可用，副本也不等于隔离误操作的备份。

### 60 秒扩展要点

- RDB 的丢失窗口取决于最近成功快照；BGSAVE 的 fork、写时复制内存和后台 I/O 仍有成本。
- AOF 的 RPO 取决于 `appendfsync`；Redis 7+ 使用 base、增量文件和 manifest，`everysec` 仍有未刷盘窗口。
- 正常启动时若 AOF 开启则按有效 AOF 集合恢复，否则尝试 RDB；文件缺失、权限或损坏可能使恢复失败。
- 复制默认异步，已确认写入仍可能在故障转移时丢失；副本还会同步误删除。
- Sentinel 不分片且存在检测、选举和客户端重连窗口；Cluster 有 16384 个槽、跨槽限制和多数主节点可达等可用条件。

### 120 秒场景闭环

进程崩溃后的恢复主要看可用 RDB/AOF 与刷盘窗口；整机故障还需要位于其他故障域的副本和自动切换；误删除会复制到副本，必须依赖隔离的历史备份或恢复流程；主从网络分区则要考虑异步复制、选主条件和客户端重连。方案不能只检查配置存在，而要先定义 RPO/RTO，再做进程终止、主机不可达、切换和文件恢复演练，记录最后成功快照/AOF 状态、复制延迟、切换耗时、客户端错误窗口和恢复后的关键数据核对。简历只确认 Redis 缓存使用，没有确认主从、Sentinel、Cluster 或真实故障演练，回答必须标为通用工程能力。

> [!check]- L0～L4 核对
> - L0：能把持久化、复制、故障转移、分片和备份分开。
> - L1：解释 RDB/AOF 恢复与刷盘窗口，而不是只背“小快”和“大慢”。
> - L2：主动否定“AOF 零丢失、replica 等于备份、Sentinel 零中断”。
> - L3：针对进程、整机、误删除和分区选择不同机制。
> - L4：用 RPO、RTO、复制延迟、切换和客户端重连演练验证，并守住项目边界。

## ⑤ Web/项目：W4 支付回调幂等

知识依据：[[06-Web/03-HTTP#419. 什么是 HTTP 方法幂等？|419]]、[[06-Web/03-HTTP#420. 哪些 HTTP 方法通常具有幂等性？|420]]、[[07-项目/03-第三方支付#456. 什么是支付宝异步通知？|456]]、[[07-项目/03-第三方支付#458. 支付回调验签通过后还要校验什么？|458]]、[[07-项目/03-第三方支付#464. 什么是支付回调幂等？|464]]、[[07-项目/03-第三方支付#467. 项目中如何保证支付回调幂等？|467]]、[[07-项目/03-第三方支付#468. 为什么支付状态迁移必须是原子的？|468]]、[[07-项目/03-第三方支付#471. 支付成功但本地订单状态更新失败怎么办？|471]]。

### 30 秒参考回答

HTTP 方法幂等比较的是重复相同请求的预期服务器效果；支付业务幂等则要求同一笔通知重复到达时，订单状态和发货、记账、消息等副作用最多发生一次有效变更。支付回调要依次完成验签、应用/商户与订单金额等业务字段校验、合法状态判断和原子状态迁移，不能把“验签通过”或“先查后改”直接等同于严格幂等。

### 60 秒扩展要点

- 支付平台可能因超时、未收到成功应答或网络异常重复通知；前端跳转不能替代服务端异步通知。
- 验签证明签名范围内参数的来源与完整性，仍需核对应用/收款方、商户订单号、金额和交易状态。
- `SELECT 未支付 → 普通 UPDATE` 存在并发竞态；应使用只允许 `UNPAID → PAID` 的条件 UPDATE，并依据受影响行数判断首次处理权。
- 影响 0 行不一定是重复成功，还可能是订单不存在、已取消或状态非法。
- HTTP 的 POST 没有标准幂等保证，但具体支付接口可以用稳定业务键、状态机、唯一约束和幂等记录实现业务幂等。

### 120 秒场景闭环

同一订单收到重复或乱序通知时，先验签并核对业务字段，再在事务中以商户订单号和旧状态做原子条件更新。只有影响 1 行的处理者可以执行同一事务内的本地副作用；影响 0 行则重新确认是已成功处理、订单不存在还是非法状态。若本地更新失败，事务回滚且不能向平台返回成功，让通知保留重试机会；若数据库已经提交但成功应答丢失，后续重试通过幂等检查后直接成功应答。主动交易查询、定时对账、Outbox/消息补偿和生产密钥治理是通用增强。简历只确认支付宝沙箱预下单、RSA 验签、状态校验和重复通知过滤；原子 SQL、主动查询、对账与补偿在获得代码证据前不能说成已经实现。

> [!check]- L0～L4 核对
> - L0：区分 HTTP 语义幂等和支付业务副作用幂等。
> - L1：说明重复来源，以及验签、业务校验、状态机和原子更新各自职责。
> - L2：指出先查后改竞态、影响 0 行分支和返回成功应答的持久化边界。
> - L3：覆盖重复、乱序、本地失败、提交后应答丢失和非法状态。
> - L4：逐项标注真实简历主张、待核验实现和生产级增强。

## ⑥ 算法：A2 前缀和 + HashMap

知识依据：[[12-算法/01-Java算法常用API#629. HashMap 常用方法有哪些？|629]]、[[12-算法/02-算法基础#637. 数组常见题型有哪些？|637]]、[[12-算法/02-算法基础#640. Hash 表常见题型有哪些？|640]]。

### 30 秒参考回答

设当前位置的前缀和为 `prefix`，此前前缀为 `old`，对应子数组和就是 `prefix - old`；所以要找和为 `k` 的子数组，只需统计此前出现过多少个 `prefix - k`。HashMap 保存“前缀和 → 出现次数”，先放入 `0 -> 1`，遍历时先查询再加入当前前缀。负数会破坏普通滑动窗口的单调收缩条件，所以本题使用前缀和。

### 60 秒扩展要点

- `0 -> 1` 代表数组开始前的空前缀，使从下标 0 开始的合法非空子数组能被统计。
- 必须先查询 `prefix - k` 再加入当前 prefix，否则 `k = 0` 时会把当前位置与自身组成的空区间错误计入。
- Map 存出现次数而不是只存是否出现，因为同一前缀值的每次出现都对应不同起点。
- 前缀和与答案使用 long：int 累加可能溢出，合法子数组数量最坏为 `n(n+1)/2`。
- 算法期望时间 `O(n)`，最坏额外空间 `O(n)`；不修改输入数组。

### 120 秒场景闭环

对 `[1,-1,0]`、`k=0`：初始 Map 为 `{0:1}`。读到 1 时 `prefix=1`，此前没有前缀 1，答案仍 0，再加入 `{1:1}`；读到 -1 时 `prefix=0`，此前前缀 0 出现 1 次，答案变 1，再把 0 的次数增为 2；读到 0 时 `prefix=0`，此前前缀 0 已出现 2 次，对应 `[1,-1,0]` 和 `[0]`，答案变 3。每一步只统计“以当前位置结尾”的合法子数组，所有非空子数组都有唯一结束位置，因此不会漏计或重复计数。

### Java 参考实现

```java
import java.util.HashMap;
import java.util.Map;

public class SubarraySumCounter {
    public static long countSubarrays(int[] nums, long k) {
        Map<Long, Long> prefixFrequency = new HashMap<>();
        prefixFrequency.put(0L, 1L);

        long prefix = 0L;
        long answer = 0L;
        for (int num : nums) {
            prefix += num;
            answer += prefixFrequency.getOrDefault(prefix - k, 0L);
            prefixFrequency.merge(prefix, 1L, Long::sum);
        }
        return answer;
    }

    private static void check(long expected, long actual) {
        if (expected != actual) {
            throw new AssertionError("expected=" + expected + ", actual=" + actual);
        }
    }

    public static void main(String[] args) {
        check(0L, countSubarrays(new int[] {}, 0L));
        check(2L, countSubarrays(new int[] {1, 1, 1}, 2L));
        check(3L, countSubarrays(new int[] {1, -1, 0}, 0L));
        check(6L, countSubarrays(new int[] {0, 0, 0}, 0L));
        check(1L, countSubarrays(new int[] {-1, -1, 1}, 0L));
        check(2L, countSubarrays(
                new int[] {Integer.MAX_VALUE, 1, -Integer.MAX_VALUE}, 1L));
    }
}
```

> [!check]- L0～L4 核对
> - L0：由 `prefix - old = k` 推出查找 `prefix - k`，并说明负数下不直接用普通滑窗。
> - L1：Map 保存此前前缀频次，初始化 `0 -> 1`，顺序必须先查后加。
> - L2：说明 long、空数组、零、负数、重复值与输入不变契约。
> - L3：能手推 `[1,-1,0]` 的 Map 和答案变化，并真正运行六组测试。
> - L4：以唯一结束位置证明完备且不重复，给出期望 `O(n)` 时间和 `O(n)` 空间。

## 核对结果回流

| 学习线 | 母题 | 与参考答案的首个差异 | 失败层 | 等级 | 最小修复节点 |
| --- | --- | --- | --- | --- | --- |
| ① Java 基础与运行时 | J4 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ② Spring 与数据访问 | S1 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ③ MySQL | M3 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ④ Redis | R4 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ⑤ Web、项目与工程 | W4 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ⑥ 算法与手写 | A2 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |

完成核对后，把真实结果同步到 [[00-第二轮学习队列#六线证据队列]]。只有在拿到这些结果后，才生成针对断点的换问法复测或第二组题单。
