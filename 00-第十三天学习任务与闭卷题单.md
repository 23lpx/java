---
category: 学习任务
phase: 第一轮
day: 13
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第十三天学习任务与闭卷题单

> Day 13 按各线真实剩余量推进：Java、工程各学习 10 道新题，Spring/MyBatis 学习最后 8 道新题并回查 2 道主干题；MySQL 训练死锁诊断，Redis 训练延迟排障；算法线手写括号匹配。

导航：[[00-总目录]] · [[00-第一轮学习队列]] · [[00-第十二天学习任务与闭卷题单]] · [[00-每日学习与闭卷考核模板]]

## 开始前

- [ ] 复制 [[00-每日学习与闭卷考核模板]]，以当天日期命名。
- [ ] 从 Day 12 记录中为每条学习线选择一个 `不会` 或 `不稳` 断点。
- [ ] 如果某条线连续失败题超过 2 道，本日先修复最靠前的核心断点，再决定是否执行综合场景。
- [ ] Day 12 尚未执行时，先完成 [[00-第十二天学习任务与闭卷题单]]。

## Day 12 回流槽

| 学习线 | 回流题号 | Day 12 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 / 回查或实战 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | [[09-Java并发/01-线程基础]] 题 496～503；[[09-Java并发/02-锁与线程池]] 题 504～505 | 10 / 0 | 串联线程创建与状态、start/run、sleep/wait、上下文切换和线程安全，再建立 synchronized 的对象锁/Class 锁边界 |
| ② Spring 与数据访问 | 新学 [[03-MyBatis/01-MyBatis]] 题 217～224；回查 [[02-Spring/08-Spring事务#190. Spring 声明式事务大致是怎么实现的？|190]]、[[03-MyBatis/01-MyBatis#216. 如何通过 `<foreach>` 实现批量插入？|216]] | 8 / 2 回查 | 串联主键回填、项目子表写入、一二级缓存和 PageHelper，再闭卷复述事务代理—SqlSession—批量写入调用链 |
| ③ MySQL | 回查 [[04-MySQL/03-索引#272. 什么是 EXPLAIN？|272]]、[[04-MySQL/05-锁#296. 什么是行锁？|296]]、[[04-MySQL/05-锁#301. 乐观锁通常如何实现？|301]]、[[04-MySQL/05-锁#302. 什么是数据库死锁？|302]]、[[04-MySQL/05-锁#303. MySQL 为什么会产生死锁？|303]]、[[04-MySQL/05-锁#304. 如何降低死锁发生概率？|304]]；完成死锁诊断演练 | 0 / 1 场景 | 从相反加锁顺序还原等待环，结合索引与扫描范围定位锁集合，并设计短事务、一致顺序、有限重试和幂等边界 |
| ④ Redis | 回查 [[05-Redis/01-Redis基础#307. Redis 为什么快？|307]]、[[05-Redis/01-Redis基础#308. Redis 是完全单线程的吗？|308]]、[[05-Redis/01-Redis基础#309. Redis 单线程为什么还能这么快？|309]]、[[05-Redis/01-Redis基础#310. 什么是 IO 多路复用？|310]]、[[05-Redis/01-Redis基础#318. 为什么缓存通常要设置 TTL？|318]]、[[05-Redis/01-Redis基础#319. Redis 中 Key 应该如何设计？|319]]、[[05-Redis/02-过期与内存管理#325. Redis 内存满了怎么办？|325]]；完成延迟排障演练 | 0 / 1 场景 | 从事件循环、慢命令、大 Key、持久化后台任务、淘汰和下游回源还原 P99 抖动，不用“单线程”解释所有问题 |
| ⑤ Web、项目与工程 | [[10-开发基础/02-Git]] 题 552～560；[[10-开发基础/03-Maven]] 题 561 | 10 / 0 | 串联 commit/push/fetch/pull、分支、合并、冲突与历史修正，再说明 Maven 在构建和依赖管理中的位置 |
| ⑥ 算法与手写 | 回流 [[12-算法/02-算法基础#639. 栈和队列常见题型有哪些？|题 639]]；有效括号判断 | 0 + 实战 | 用栈维护最近未匹配左括号或期望右括号，说明嵌套顺序、提前失败、遍历结束判定和 `O(n)` 空间上界 |

> Spring/MyBatis 本日只有 8 道未覆盖编号题，因此使用 2 道主干题完成调用链回查，不把旧题计为新题。通过本日新题与回查后，该学习线完成第一轮编号节点收口，后续转入场景与错题修复。

### MySQL 线综合场景

事务 T1 依次更新订单 101、102；事务 T2 几乎同时依次更新订单 102、101。两边各完成第一条更新后，第二条更新相互等待。

必须完成：

1. 画出“事务—已持有锁—等待锁”的等待环，区分死锁与能够等持锁事务正常提交后继续的普通锁等待。
2. 说明即使 SQL 看起来只处理业务行，也可能涉及聚簇索引、二级索引、间隙或重复键检查；实际锁集合要结合执行计划和死锁日志确认。
3. 给出诊断证据：最近一次 InnoDB 死锁信息、相关 SQL、事务开始时间、索引和访问顺序；不能只因看到长事务就直接认定死锁根因。
4. 通过批量 ID 排序、一致访问表/记录顺序、合适索引和短事务降低等待环概率，事务中不执行耗时远程调用。
5. 捕获死锁受害者错误后回滚并有限退避重试整个事务；若存在消息、支付等外部副作用，先保证幂等，不能从失败 SQL 后半段继续。
6. 说明死锁只能降低概率；频繁出现时应依据证据重构事务与索引，而不是无限重试。

### Redis 线综合场景

业务监控显示 Redis P99 延迟突然升高，CPU 并未持续打满，同时 `evicted_keys` 和数据库回源量上升。故障窗口内还出现一次 RDB/AOF 后台任务。

必须完成：

1. 先收集时间对齐的证据：命令延迟/慢日志、命令统计、Key 大小与访问分布、内存/RSS/碎片、淘汰与过期速率、持久化状态、网络和下游回源量。
2. 解释 Redis 命令执行主路径串行化的好处与边界；现代 Redis 还有 I/O 线程和后台任务，不能说成“整个 Redis 进程只有一个线程”。
3. 推演大集合全量读取、超大批处理或复杂度较高命令如何长时间占用事件循环；处理前保存证据，随后限制批量、使用渐进式扫描、拆分 Key 或调整数据模型。
4. 推演后台保存/重写带来的 fork、写时复制内存和磁盘竞争；它们不等于命令在子线程执行就对延迟零影响。
5. 解释内存淘汰导致命中率下降和数据库回源放大的反馈环，并结合容量、策略、TTL 和回源保护处理。
6. 说明“增加线程”“关闭持久化”或“直接扩容”都不是无证据结论；修复后要用同一批 P99、慢命令、淘汰和回源指标验证。

### 算法线编码要求

实现 `boolean isValid(String s)`。输入字符串非 `null`，长度可以为 0，只包含 `(`、`)`、`[`、`]`、`{`、`}`；当且仅当所有括号类型匹配且嵌套顺序正确时返回 `true`。

至少验证：

```text
s = ""       -> true
s = "()"     -> true
s = "()[]{}" -> true
s = "(]"     -> false
s = "([)]"   -> false
s = "{[]}"   -> true
s = "("      -> false
```

完成代码后必须口述：栈中保存左括号还是期望的右括号、遇到右括号时哪些情况立即失败、为什么遍历结束还要检查栈为空，以及时间复杂度 `O(n)`、最坏额外空间 `O(n)`。Java 实现优先使用 `ArrayDeque<Character>`，不要把同步的旧 `Stack` 当成唯一选择。

### 每线完成条件

- [ ] Day 12 回流题已重新闭卷回答，并记录修复后的结果。
- [ ] Java 并发、MyBatis、Git/Maven 新题至少完成一次脱稿回答或职责链输出。
- [ ] Spring/MyBatis 的 8 道新题与 2 道回查题均有闭卷结果。
- [ ] MySQL 场景包含等待环、诊断证据、治理手段与有限重试边界。
- [ ] Redis 场景包含同一时间线上的命令、内存、持久化、网络和回源证据。
- [ ] 括号匹配代码通过全部用例，并能解释栈不变量和两类失败条件。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-每日学习与闭卷考核模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[09-Java并发/01-线程基础#500. `sleep()` 和 `wait()` 有什么区别？|题 500：sleep 和 wait 有什么区别？]]

- 主问题：从所属类、调用条件、锁释放、唤醒方式和用途比较 sleep 与 wait。
- 追问 1：`wait()` 为什么必须在持有对应监视器时调用，唤醒后是否立即获得锁？
- 追问 2：sleep 和 wait 被中断时如何表现，为什么两者都不应被当成精确调度保证？

### ② Spring 与数据访问

[[03-MyBatis/01-MyBatis#224. PageHelper 大致如何实现分页？|题 224：PageHelper 大致如何实现分页？]]

- 主问题：说明线程绑定分页参数、MyBatis 拦截器改写 SQL 和结果包装的基本链路。
- 追问 1：为什么分页调用必须紧邻目标查询，异步或线程复用时要关注什么？
- 追问 2：PageHelper 是否固定执行 count 与 LIMIT，方言、复杂 SQL 和深分页有哪些边界？

### ③ MySQL

[[04-MySQL/05-锁#303. MySQL 为什么会产生死锁？|题 303：MySQL 为什么会产生死锁？]]

- 主问题：从不同加锁顺序、索引记录/范围和并发时序解释等待环。
- 场景追问：对白天两个订单事务如何还原持锁与等待关系，统一排序为什么只能降低概率？
- 边界追问：数据库回滚受害事务后，应用为何仍需完整、有限、幂等地重试？

### ④ Redis

[[05-Redis/01-Redis基础#307. Redis 为什么快？|题 307：Redis 为什么快？]]

- 主问题：从内存访问、数据结构、事件驱动和命令执行路径说明主要原因。
- 场景追问：为什么大 Key/慢命令、fork 与磁盘竞争仍可能造成延迟尖峰？
- 边界追问：Redis 并非整个进程完全单线程，这一事实为什么也不意味着可以忽略命令复杂度？

### ⑤ Web、项目与工程

[[10-开发基础/02-Git#560. `git reset` 和 `git revert` 有什么区别？|题 560：git reset 和 git revert 有什么区别？]]

- 主问题：从移动引用/索引/工作区与新增反向提交比较 reset、revert。
- 追问 1：为什么共享分支通常优先 revert，reset 后强推有什么协作风险？
- 追问 2：`--soft`、`--mixed`、`--hard` 分别影响哪些状态，为什么不能凭口诀在脏工作区执行？

### ⑥ 算法与手写

[[12-算法/02-算法基础#639. 栈和队列常见题型有哪些？|题 639：栈和队列常见题型有哪些？]]

- 主问题：说明栈、队列、双端队列和单调结构分别适合维护什么顺序不变量。
- 手写：重新实现今天的有效括号判断，不能查看白天代码。
- 追问：遇到右括号时栈为空与类型不匹配分别说明什么？为什么 `([)]` 不能只按三类括号数量判断？

## 评分规则

Java、Spring/MyBatis、Git/Maven题每题 10 分：核心结论 2 分、原理链路 2 分、成立条件与边界 2 分、两层追问 2 分、场景或工程影响 2 分。

MySQL、Redis 场景题每题 10 分：证据链 2 分、故障时序 2 分、成立条件 2 分、治理闭环 2 分、项目真实性 2 分。

算法题 10 分：函数契约 1 分、栈不变量 3 分、失败与结束条件 2 分、代码和返回值 2 分、复杂度 2 分。

总分 60 分：

- 48～60：整体通过；单题不足 8 分仍记为 `不稳`。
- 36～47：Day 14 保留对应回流槽，并减少失败学习线的新题量或场景复杂度。
- 0～35：暂停薄弱线扩题，优先修复重复失败的知识链。

## 考后动作

- [ ] 对照原笔记纠错，记录首个错误断点。
- [ ] 将六道题分别标记为 `不会` / `不稳` / `通过`。
- [ ] 仅根据闭卷证据更新相关知识笔记的 `status`。
- [ ] Spring/MyBatis 只有在 8 道新题和 2 道回查均通过后，才标记第一轮对应队列项完成。
- [ ] MySQL 与 Redis 场景未通过时，下一日继续修复失败链，不用新的场景掩盖问题。
- [ ] 把连续失败题和括号栈的首个错误加入 Day 14 回流。
- [ ] 原笔记疑点单独记录，核验可靠资料后再创建维护 PR。
