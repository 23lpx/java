---
category: 仓库维护
phase: V1.3
status: 可使用
tags:
  - Java后端
  - Obsidian
  - 版本复核
  - 维护队列
---

# 版本敏感内容复核队列

> 本页管理“下一批应该复核哪些知识笔记”，不判断内容已经过时，也不表示个人不会。实时顺序由只读脚本根据维护元数据和风险信号生成，脚本不会修改 Vault。

导航：[[00-总目录]] · [[00-V1.3维护路线图]] · [[00-维护规范]]

## 判定边界

- `reviewed_at` 只表示最近一次完成准确性复核的日期；日期较早只会提高候选优先级，不会自动把内容判错。
- `version_scope` 记录结论实际覆盖的规范、框架、组件或协议范围；缺失时进入元数据补全队列，不能凭文件主题猜写版本。
- `verified` 只允许 `已校验` 或 `待校验`；设置为 `已校验` 必须有真实审查依据。
- 风险词命中只说明结论更值得优先人工检查。包含“事务”“线程安全”或“默认”等词，不等于答案存在错误。
- 本队列与知识笔记的 `status` 无关，仓库维护不得据此修改 `未学习 / 看过 / 能回答 / 需复习`。

## 队列输入

排序器读取 50 篇知识笔记的以下信息：

1. Frontmatter：`priority`、`verified`、`reviewed_at`、`version_scope`。
2. 正文风险族：版本/API、并发/内存、事务/一致性、实现细节、绝对化措辞。
3. 扫描基准日期：默认使用运行当天，也可通过 `--as-of` 固定日期以复现实验结果。

实时查看前 20 项：

```bash
python3 scripts/report_review_queue.py --check --limit 20
```

只做确定性元数据检查：

```bash
python3 scripts/report_review_queue.py --check --limit 0
```

## 排序规则

| 信号 | 分值 | 含义 |
| --- | ---: | --- |
| P0 / P1 / P2 | +30 / +15 / +5 | 求职优先级只影响维护先后，不代表技术价值 |
| 缺 `verified` | +50 | 尚未记录内容校验状态 |
| `verified: 待校验` | +40 | 已明确需要重新核验 |
| 缺 `reviewed_at` | +50 | 无法确认最近一次准确性审查时间 |
| 缺 `version_scope` | +50 | 无法确认结论适用范围 |
| 超过 90 / 180 / 365 天 | +10 / +25 / +40 | 只提升候选优先级，不直接判定过时 |
| 版本/API、并发/内存、事务/一致性 | 每族 +20 | 版本变化或错误边界的影响较大 |
| 实现细节、绝对化措辞 | 每族 +15 | 容易把特定实现或条件性结论写成永久规则 |

每个风险族对一篇笔记只计一次，避免长文仅因词频较高垄断队列。总分是维护调度分，不是准确率或质量评分。

## 当前基线

以 2026-08-25 为基准：

- 知识笔记：50 篇。
- `verified / reviewed_at / version_scope` 三项均完整：24 篇。
- 至少缺一项维护元数据：26 篇。
- 当前日期距离已有复核日期只有数日，因此尚无笔记因 90 / 180 / 365 天阈值加分。
- 队列前部首先用于补齐缺失证据，而不是重新改写已经校准的全部正文。

该基线是一次可复现快照；实时数量以脚本输出为准。

## 执行流程

1. 每批从实时队列头部选择一个单模块或约 30～60 道题，先确认缺失的是元数据、来源还是正文结论。
2. 对版本和默认行为优先查规范、官方文档、对应版本源码或协议；第三方文章只作为线索。
3. 只有实际复核完成后才写 `reviewed_at`，并按证据填写 `version_scope` 和 `verified`。
4. 若结论有误，修正文同时保留常见面试版本与新版本差异；若结论仍成立，只补证据和范围，不为产生 diff 改写措辞。
5. 每批运行 Vault 完整性检查和版本队列元数据检查，并在 Pull Request 中记录资料依据和剩余风险。

## 验收与回流

- 非法 `verified`、非法日期和晚于扫描日期的 `reviewed_at` 会导致 CI 失败。
- 缺失维护元数据只进入队列，不阻止合并；否则无法渐进修复当前 26 篇存量笔记。
- 某篇完成复核后，应从“缺字段”候选转为按日期和风险保持性抽查，不从仓库中永久移除。
- 新增知识笔记必须立即参与扫描；排序器不维护手写文件白名单。
- 批次 4 的来源追溯工作直接消费本队列结果，但仍按单模块拆分 PR。
