---
category: 学习任务
phase: 第一轮
day: 14
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第十四天学习任务与闭卷题单

> Day 14 按各线真实剩余量推进：Java 并发、Maven 各学习 10 道新题；已完成编号节点收口的 Spring/MyBatis、MySQL、Redis 转入事务失效、重复下单和热点 Key 击穿场景；算法线手写两数之和。

导航：[[00-总目录]] · [[00-第一轮学习队列]] · [[00-第十三天学习任务与闭卷题单]] · [[00-每日学习与闭卷考核模板]]

## 开始前

- [ ] 复制 [[00-每日学习与闭卷考核模板]]，以当天日期命名。
- [ ] 从 Day 13 记录中为每条学习线选择一个 `不会` 或 `不稳` 断点。
- [ ] 如果某条线连续失败题超过 2 道，本日先修复最靠前的核心断点，再决定是否执行综合场景。
- [ ] Day 13 尚未执行时，先完成 [[00-第十三天学习任务与闭卷题单]]。

## Day 13 回流槽

| 学习线 | 回流题号 | Day 13 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 / 回查或实战 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | [[09-Java并发/02-锁与线程池]] 题 506～515 | 10 / 0 | 区分 `volatile` 的可见性、有序性和非原子复合操作，再从死锁四条件进入线程池用途、复用收益与核心参数 |
| ② Spring 与数据访问 | 回查 [[02-Spring/08-Spring事务#188. `@Transactional` 有什么作用？|188]]、[[02-Spring/08-Spring事务#190. Spring 声明式事务大致是怎么实现的？|190]]、[[02-Spring/08-Spring事务#192. Spring 默认哪些异常会触发回滚？|192]]、[[02-Spring/08-Spring事务#194. `rollbackFor` 有什么作用？|194]]、[[02-Spring/08-Spring事务#195. 把异常 catch 住为什么可能导致事务不回滚？|195]]、[[02-Spring/08-Spring事务#196. 同一个类内部调用另一个事务方法为什么可能失效？|196]]、[[02-Spring/08-Spring事务#200. 项目中为什么主表和子表写入需要放在同一个事务中？|200]]、[[03-MyBatis/01-MyBatis#216. 如何通过 `<foreach>` 实现批量插入？|216]]；完成事务未回滚排障 | 0 / 1 场景 | 从代理入口、异常传播和实际事务边界解释“主表成功、明细失败”，区分真实项目链路与假设故障 |
| ③ MySQL | 回查 [[04-MySQL/02-表设计#240. 什么是唯一索引？|240]]、[[04-MySQL/02-表设计#241. 主键和唯一索引有什么区别？|241]]、[[04-MySQL/04-事务#277. MySQL 有哪些事务隔离级别？|277]]、[[04-MySQL/05-锁#293. 为什么数据库需要锁？|293]]、[[04-MySQL/05-锁#300. 什么是乐观锁？|300]]、[[04-MySQL/05-锁#301. 乐观锁通常如何实现？|301]]；完成重复下单幂等演练 | 0 / 1 场景 | 识别“先查后插”的竞态，用业务唯一约束兜底首次创建，并说明更新类竞争才适合版本号或条件更新 |
| ④ Redis | 回查 [[05-Redis/01-Redis基础#317. 什么是 TTL？|317]]、[[05-Redis/01-Redis基础#318. 为什么缓存通常要设置 TTL？|318]]、[[05-Redis/04-缓存穿透击穿雪崩#350. 什么是缓存击穿？|350]]、[[05-Redis/04-缓存穿透击穿雪崩#351. 缓存击穿通常发生在什么场景？|351]]、[[05-Redis/04-缓存穿透击穿雪崩#352. 什么是热点 Key？|352]]、[[05-Redis/04-缓存穿透击穿雪崩#353. 如何解决缓存击穿？|353]]、[[05-Redis/04-缓存穿透击穿雪崩#354. 互斥锁如何解决缓存击穿？|354]]、[[05-Redis/04-缓存穿透击穿雪崩#355. 什么是逻辑过期？|355]]；完成热点 Key 重建演练 | 0 / 1 场景 | 比较请求合并、互斥重建与逻辑过期的正确性、可用性和陈旧数据代价，不把 TTL 抖动当成单热点万能方案 |
| ⑤ Web、项目与工程 | [[10-开发基础/03-Maven]] 题 562～571 | 10 / 0 | 串联 POM、依赖下载、本地仓库、生命周期和 clean/package/install，再用依赖树定位冲突与调解结果 |
| ⑥ 算法与手写 | 回流 [[12-算法/02-算法基础#640. Hash 表常见题型有哪些？|题 640]]；两数之和 | 0 + 实战 | 用一趟扫描维护“已见值 → 最早下标”，解释查找先于写入、重复值、无解返回和整数溢出边界 |

> Spring/MyBatis、MySQL、Redis 已完成第一轮编号节点覆盖，本日只进行回查和综合场景，不把旧题计为新题。Day 14 的新题量为 20，道数下降不代表学习强度下降，场景线按证据链和故障闭环单独验收。

### Spring/MyBatis 线综合场景

通用订单创建流程先插入订单主表并获得主键，再通过 MyBatis 批量插入订单明细。一次请求中明细写入抛出异常，但数据库留下了主表记录。有人发现外层 Service 捕获了异常并返回“创建失败”；另一个可疑点是同类方法通过 `this` 调用了带 `@Transactional` 的创建方法。

必须完成：

1. 先用事务日志、异常堆栈、数据源/连接和调用入口确认实际事务边界，不能看到 `@Transactional` 就断言事务一定已生效。
2. 说明声明式事务通常依靠代理拦截外部调用；同类 `this` 调用可能绕过代理，但是否失效仍需结合代理模式、调用路径和具体配置确认。
3. 说明异常被捕获且未继续抛出时，事务拦截器可能看到“正常返回”；可选择让异常传播、在明确边界转换后重新抛出，或显式标记回滚，不能只打印日志后继续提交。
4. 区分默认回滚的 `RuntimeException` / `Error` 与 checked exception；若业务要求后者回滚，应显式配置 `rollbackFor`，不能把所有异常一概而论。
5. 主表、主键回填和明细批量写入应位于同一数据库事务；批量 SQL 的规模、错误定位和数据库约束仍需单独验证。
6. 把“声明式事务、主表/子表、批量写入”表述为简历已有链路；本次异常和 `this` 调用是排障假设，不能包装为实际发生过的线上事故。

### MySQL 线综合场景

客户端超时后使用同一 `request_id` 重试创建订单，网关又把第一次请求转发成功。两个事务几乎同时执行“按 `user_id + request_id` 查询不存在”，随后都准备插入订单。

必须完成：

1. 解释“先查后插”为什么存在检查与写入之间的竞态；提高隔离级别或在应用层加一次判断都不能自动等价于业务幂等。
2. 将 `(user_id, request_id)` 设计为非空业务唯一键，并说明主键负责行身份、业务唯一索引负责拒绝重复语义；候选索引仍要结合字段长度、写入成本和查询模式评估。
3. 两个事务并发插入时，只允许一个首次创建成功；另一个捕获明确的唯一约束冲突后，读取既有订单并核对幂等键对应的业务参数，不能把所有数据库异常都当成重复请求。
4. 若同一个 `request_id` 携带不同金额或商品，应返回冲突而不是静默复用旧结果；幂等响应需要定义可重放的状态与结果。
5. 说明版本号/条件更新适合“已有行”的状态竞争，不能单独解决尚不存在记录的并发首次插入；创建防重仍需要唯一性栅栏或等价原子机制。
6. 数据库事务不能自动撤销已经发生的第三方支付、消息或远程调用；Outbox、消息去重等属于通用生产增强，简历未证明时不得表述为个人实现。

### Redis 线综合场景

午间流量高峰，菜品分类热点 Key `dish:category:10` 到期。约两百个请求同时未命中缓存并回源数据库，数据库负载快速上升；其他分类 Key 仍正常命中。

必须完成：

1. 根据“单个热点 Key 失效并被高并发访问”识别缓存击穿；若是大量 Key 同时失效或 Redis 整体不可用，应转而分析雪崩，不能只看数据库变慢就下结论。
2. 互斥重建时只允许一个执行者查询数据库并回填，其他请求应有限等待、快速失败或降级；获得重建资格后再次检查缓存，避免前一个执行者已完成回填却重复查库。
3. 若使用 Redis 锁，沿用唯一 token、租约和原子释放边界；锁只能控制重建并发，数据库与资源侧仍需限流、超时和降级保护。
4. 逻辑过期允许先返回旧值并异步触发单个重建者，换取可用性但接受短暂陈旧；逻辑时间、重建失败重试和长期无人刷新都需要治理。
5. TTL 随机抖动主要降低大量 Key 集中过期的概率，不能消除某个热点 Key 到期时的击穿；热点预热也必须有容量和失效策略。
6. 修复前后对比命中率、数据库 QPS/P99、重建次数、等待/降级量和 Redis 错误；该场景是通用演练，不扩写为简历项目真实故障。

### 算法线编码要求

实现 `int[] twoSum(int[] nums, int target)`。`nums` 非 `null`，长度可以为 0；返回任意一对满足 `i < j` 且按数学整数相加有 `nums[i] + nums[j] == target` 的下标。若有多解，返回从左到右扫描时最先发现的 `j` 对应结果，并保留该值最早出现的 `i`；无解返回空数组。

至少验证：

```text
nums = [], target = 0                              -> []
nums = [2, 7, 11, 15], target = 9                 -> [0, 1]
nums = [3, 2, 4], target = 6                      -> [1, 2]
nums = [3, 3], target = 6                         -> [0, 1]
nums = [-3, 4, 3, 90], target = 0                 -> [0, 2]
nums = [1, 2, 3], target = 7                      -> []
nums = [2147483647, 2147483647], target = -2      -> []
```

完成代码后必须口述：扫描到 `j` 前 HashMap 保存什么、为什么必须先查补数再写当前值、`putIfAbsent` 如何保留最早下标、重复数字为何能使用不同位置，以及时间复杂度期望 `O(n)`、最坏额外空间 `O(n)`。补数使用 `long` 计算；超出 `int` 范围时直接视为不存在，避免 Java `int` 溢出制造假解。

### 每线完成条件

- [ ] Day 13 回流题已重新闭卷回答，并记录修复后的结果。
- [ ] Java 并发与 Maven 新题至少完成一次脱稿回答、参数图或命令职责链。
- [ ] Spring/MyBatis 场景能够从代理入口、异常传播和数据库事务证据定位失败边界。
- [ ] MySQL 场景能够区分先查后插、唯一性栅栏、更新型乐观锁和外部副作用。
- [ ] Redis 场景包含击穿识别、单重建者、失败策略、逻辑过期代价和指标验证。
- [ ] 两数之和代码通过全部用例，并能解释 HashMap 不变量、确定性返回和溢出边界。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-每日学习与闭卷考核模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[09-Java并发/02-锁与线程池#507. `volatile` 能保证原子性吗？|题 507：volatile 能保证原子性吗？]]

- 主问题：说明 `volatile` 能保证什么，为什么 `count++` 仍不是原子操作。
- 追问 1：读—改—写在哪个并发窗口丢失更新，`volatile` 的可见性为什么补不上这个窗口？
- 追问 2：什么时候选择 `AtomicInteger`、锁或线程封闭，单次读写原子是否等于复合业务原子？

### ② Spring 与数据访问

[[02-Spring/08-Spring事务#196. 同一个类内部调用另一个事务方法为什么可能失效？|题 196：同类内部调用事务方法为什么可能失效？]]

- 主问题：从代理对象与目标对象的调用路径解释自调用边界。
- 场景追问：订单创建方法通过 `this` 调用时，如何用调用入口、事务日志和数据结果验证事务是否真正开启？
- 边界追问：把方法改成 public 为什么仍不必然解决问题，还有哪些重构或显式事务方案？

### ③ MySQL

[[04-MySQL/02-表设计#240. 什么是唯一索引？|题 240：什么是唯一索引？]]

- 主问题：说明唯一索引解决的约束与查询问题，以及它与主键的区别。
- 场景追问：为什么 `(user_id, request_id)` 唯一约束能兜底两个并发“先查后插”？
- 边界追问：字段允许 `NULL`、幂等键复用但参数不同、捕获到其他约束异常时分别有什么风险？

### ④ Redis

[[05-Redis/04-缓存穿透击穿雪崩#353. 如何解决缓存击穿？|题 353：如何解决缓存击穿？]]

- 主问题：比较请求合并/互斥重建、逻辑过期和热点预热的核心取舍。
- 场景追问：为什么获取重建资格后还应再次检查缓存，其他请求为什么不能无限等待？
- 边界追问：TTL 加随机值主要缓解什么，为什么不能单独解决一个热点 Key 的击穿？

### ⑤ Web、项目与工程

[[10-开发基础/03-Maven#571. 什么是 Maven 依赖冲突？|题 571：什么是 Maven 依赖冲突？]]

- 主问题：说明同一构件出现多个版本时，依赖调解与运行时不兼容风险。
- 追问 1：如何通过 `dependency:tree` 找到引入路径，`exclusions` 与 `dependencyManagement` 分别解决什么？
- 追问 2：构建成功为什么不代表运行一定正确，测试范围和插件/父 POM 版本还有哪些边界？

### ⑥ 算法与手写

[[12-算法/02-算法基础#640. Hash 表常见题型有哪些？|题 640：Hash 表常见题型有哪些？]]

- 主问题：说明 Hash 表适合频次统计、存在性判断、值到下标映射和分组的原因与代价。
- 手写：重新实现今天的两数之和，不能查看白天代码。
- 追问：为什么先写当前元素可能把同一下标使用两次？补数计算为什么要处理 `int` 溢出？

## 评分规则

Java、Maven 题每题 10 分：核心结论 2 分、原理链路 2 分、成立条件与边界 2 分、两层追问 2 分、场景或工程影响 2 分。

Spring/MyBatis、MySQL、Redis 场景题每题 10 分：证据链 2 分、故障时序 2 分、成立条件 2 分、治理闭环 2 分、项目真实性 2 分。

算法题 10 分：函数契约 1 分、HashMap 不变量 3 分、重复值与溢出边界 2 分、代码和返回值 2 分、复杂度 2 分。

总分 60 分：

- 48～60：整体通过；单题不足 8 分仍记为 `不稳`。
- 36～47：Day 15 保留对应回流槽，并减少失败学习线的新题量或场景复杂度。
- 0～35：暂停薄弱线扩题，优先修复重复失败的知识链。

## 考后动作

- [ ] 对照原笔记纠错，记录首个错误断点。
- [ ] 将六道题分别标记为 `不会` / `不稳` / `通过`。
- [ ] 仅根据闭卷证据更新相关知识笔记的 `status`。
- [ ] Java 并发和 Maven 只有在本日 20 道新题通过后，才推进到各自下一段。
- [ ] 三个综合场景未通过时，下一日继续修复失败链，不用新的场景掩盖问题。
- [ ] 把连续失败题和 HashMap 的首个错误加入 Day 15 回流。
- [ ] 原笔记疑点单独记录，核验可靠资料后再创建维护 PR。
