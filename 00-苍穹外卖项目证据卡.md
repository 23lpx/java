---
category: 项目证据卡
phase: 第二轮
status: 待补证据
tags:
  - Java后端
  - 求职
  - 项目证据
  - 苍穹外卖
---

# 苍穹外卖项目证据卡

> 本页以 2026-08-20 版简历为唯一项目事实来源。七条亮点是简历明确主张，但当前知识库没有项目源码，因此统一标记为 `V0`；回答骨架用于组织真实内容，不是新增项目经历。

导航：[[00-总目录]] · [[00-简历证据与追问地图]] · [[00-项目证据卡模板]] · [[00-模拟面试与反馈入口]]

## 项目总述

### 简历已确认

- 项目：苍穹外卖。
- 技术栈列出 Spring Boot、Spring MVC、MyBatis、MySQL、Redis、JWT、AOP、支付宝沙箱和阿里云 OSS。
- 具体职责覆盖认证上下文、公共字段填充、业务缓存、关联数据事务、地址快照、沙箱支付和统一异常处理。

### 60 秒开场骨架

1. 先说明项目解决的业务问题，不罗列所有技术名。
2. 说明自己围绕认证、数据一致性和公共能力完成了哪些模块。
3. 主动选择证据最完整的一条链路，例如主子表事务或 JWT 上下文，展开关键机制。
4. 结果只使用简历已有的定性描述；没有测量时不添加 QPS、响应时间或百分比。
5. 收口时说明这是学习/项目实现环境，沙箱支付与生产支付能力分开。

### 待补证据

- 项目代码仓库或本地路径。
- 启动方式、依赖版本、数据库结构和最小演示数据。
- 七条亮点分别对应的类、方法、配置、SQL 和接口用例。
- 阿里云 OSS 的真实使用场景；如果无法确认，不把技术栈词扩写成职责。

## 认证与上下文隔离

### 简历主张

使用 JWT、管理端/用户端双拦截器和 ThreadLocal，分离认证链路，解析 Token 并传递用户上下文。主张强度 `AR`，当前证据 `V0`。

### 知识节点

[[06-Web/01-CookieSessionJWT#385. JWT 是什么？|JWT]] · [[06-Web/01-CookieSessionJWT#389. Signature 有什么作用？|Signature]] · [[06-Web/01-CookieSessionJWT#394. JWT 如何判断是否过期？|过期校验]] · [[06-Web/01-CookieSessionJWT#399. 项目中用户登录后的 JWT 完整流程是什么？|项目流程]] · [[02-Spring/05-Filter与Interceptor#154. 为什么 JWT 登录校验适合放在拦截器？|拦截器认证]] · [[02-Spring/05-Filter与Interceptor#157. 为什么管理端和用户端可以使用两个拦截器？|双拦截器]] · [[06-Web/02-ThreadLocal#402. 为什么项目中用 ThreadLocal 保存 userId / empId？|上下文]] · [[06-Web/02-ThreadLocal#407. 为什么 ThreadLocal 使用完需要 `remove()`？|清理]]。

### 回答骨架

- 背景：管理端与用户端接口、身份类型或 Token 规则需要隔离。
- 动作：分别配置拦截器，在请求进入业务层前获取并验证 Token，把已确认身份写入当前请求线程上下文。
- 机制：Controller/Service 读取上下文，避免层层传递；请求完成后由明确所有者清理。
- 结果：解决认证和同步请求上下文传递问题，不把 ThreadLocal 说成授权或加密机制。
- 边界：异步线程、主动撤销、刷新 Token、具体 Claims 和清理位置都必须以代码为证。

### 连续追问

1. JWT 的 Header、Payload、Signature 各自作用是什么，Payload 是否加密？
2. 为什么两个拦截器优于在 Controller 重复校验，路径重叠怎样避免？
3. ThreadLocal 为什么在线程池中必须清理，异常和提前返回路径如何覆盖？
4. 多实例部署时 JWT 校验和 ThreadLocal 各自解决什么，不解决什么？

### 待补实现证据

- 两个拦截器、路径注册和 Token 工具的真实位置。
- Claims、签名算法、有效期和失败响应。
- ThreadLocal 建立/读取/清理的所有路径及异常用例。

### 不能越界

不声称 Token 内容已加密、注销黑名单/刷新 Token 已实现、ThreadLocal 自动传播异步任务，或项目从未出现上下文残留。

## 公共字段自动填充

### 简历主张

通过自定义 `@AutoFill`、AOP 和反射拦截新增/修改操作，注入创建人、创建时间等公共字段，减少重复手工填充。主张强度 `AR`，当前证据 `V0`。

### 知识节点

[[02-Spring/06-AOP#160. 哪些场景适合使用 AOP？|AOP 场景]] · [[02-Spring/06-AOP#163. 什么是 Pointcut？|切点]] · [[02-Spring/06-AOP#164. 什么是 Advice？|通知]] · [[02-Spring/06-AOP#167. Spring AOP 底层大致如何实现？|代理]] · [[02-Spring/07-反射#173. 反射可以在运行时做什么？|反射能力]] · [[02-Spring/07-反射#177. `Method.invoke()` 有什么作用？|Method.invoke]] · [[02-Spring/07-反射#180. 项目的 AutoFill 为什么需要反射？|AutoFill]]。

### 回答骨架

- 背景：多个新增/修改方法重复设置审计字段，容易遗漏且维护成本高。
- 动作：用注解表达操作类型，切面统一拦截符合约定的方法，并通过反射调用实体字段对应写入方法。
- 机制：从当前上下文和当前时间生成公共值，新增与修改只填充各自需要的字段。
- 结果：消除重复手工填充；不声称数据库约束和业务校验因此可以删除。
- 边界：切点范围、参数位置、方法命名、反射异常和代理调用都需要代码核验。

### 连续追问

1. 为什么这是横切关注点，和工具类手工调用相比有什么取舍？
2. 自定义注解放在哪里，切点如何避免误拦截？
3. 反射为什么能复用不同实体，setter 不存在或参数类型不匹配怎么办？
4. Spring AOP 代理边界和同类自调用会不会影响该切面？

### 待补实现证据

- 注解定义、操作类型、切点表达式和通知方法。
- 被拦截方法参数约定、实际字段与 setter 列表。
- 新增/修改、空用户上下文和反射失败测试。

### 不能越界

不声称支持任意实体、编译期类型安全、完全没有反射开销，或自动填充能替代数据库默认值和审计约束。

## 热点数据缓存

### 简历主张

使用 RedisTemplate 与 Spring Cache 缓存菜品/套餐查询结果，数据变更时清理缓存，降低数据库重复查询压力。主张强度 `AR`，当前证据 `V0`。

### 知识节点

[[05-Redis/03-Redis缓存#330. 什么是 Cache Aside 模式？|Cache Aside]] · [[05-Redis/03-Redis缓存#335. 数据库数据发生修改后缓存怎么办？|写后处理]] · [[05-Redis/03-Redis缓存#337. 为什么 Redis 和 MySQL 可能出现数据不一致？|不一致]] · [[05-Redis/03-Redis缓存#340. 如何降低缓存和数据库不一致的概率？|一致性治理]] · [[05-Redis/05-RedisTemplate与SpringCache#367. `@Cacheable` 有什么作用？|Cacheable]] · [[05-Redis/05-RedisTemplate与SpringCache#368. `@CacheEvict` 有什么作用？|CacheEvict]] · [[05-Redis/05-RedisTemplate与SpringCache#372. 为什么项目中同时使用 RedisTemplate 和 Spring Cache？|双工具]] · [[05-Redis/05-RedisTemplate与SpringCache#373. 项目中数据修改后如何清理对应缓存？|项目清理]]。

### 回答骨架

- 背景：菜品/套餐读取频繁、修改相对少，重复查库没有必要。
- 动作：按实际访问粒度缓存查询结果，未命中回源数据库；变更后失效相关缓存。
- 机制：数据库作为事实源，RedisTemplate 与 Spring Cache 分别用于适合的访问方式。
- 结果：简历只允许说降低重复查询压力，没有可验证数据前不说提升百分比。
- 边界：Key、TTL、序列化、关联缓存范围、事务提交与失效顺序都需代码核验。

### 连续追问

1. 为什么使用 RedisTemplate 和 Spring Cache 两种方式，各自缓存什么？
2. 修改菜品时为什么删除而不是直接更新，套餐聚合缓存怎样识别依赖？
3. 数据库提交、`@CacheEvict` 和并发旧值回填可能怎样产生不一致？
4. 穿透、击穿、雪崩的治理是否在项目真实实现，还是通用扩展？

### 待补实现证据

- 缓存命名、Key、Value 类型、TTL、序列化配置和读写入口。
- 菜品/套餐修改操作对应的清理范围。
- 命中、未命中、更新、删除失败与并发回填用例。

### 不能越界

不声称缓存与数据库强一致、项目实现了全部三类异常治理、部署了 Redis 高可用，或取得未测量的性能数字。

## 关联数据一致性

### 简历主张

结合 Spring 声明式事务、主键回填和批量插入，实现主表/子表关联数据原子写入，异常时整体回滚。主张强度 `AR`，当前证据 `V0`。

### 知识节点

[[02-Spring/08-Spring事务#188. `@Transactional` 有什么作用？|Transactional]] · [[02-Spring/08-Spring事务#190. Spring 声明式事务大致是怎么实现的？|事务代理]] · [[02-Spring/08-Spring事务#195. 把异常 catch 住为什么可能导致事务不回滚？|异常边界]] · [[02-Spring/08-Spring事务#196. 同一个类内部调用另一个事务方法为什么可能失效？|自调用]] · [[02-Spring/08-Spring事务#200. 项目中为什么主表和子表写入需要放在同一个事务中？|项目事务]] · [[03-MyBatis/01-MyBatis#216. 如何通过 `<foreach>` 实现批量插入？|批量插入]] · [[03-MyBatis/01-MyBatis#217. 什么是主键回填？|主键回填]] · [[07-项目/02-订单与关联数据#446. 为什么主表和子表必须放在同一事务中？|关联一致性]]。

### 回答骨架

- 背景：一笔业务需要同时写主记录和多条明细，任一部分失败都不能留下半成品。
- 动作：Service 外部入口建立声明式事务，先插入主表并取得主键，再组装明细批量写入。
- 机制：主键回填提供关联键；批量 SQL 减少往返；异常越过事务代理边界时整体回滚。
- 结果：保证本地数据库事务范围内的同成同败。
- 边界：异常类型、catch、自调用、新线程、跨服务和非事务资源必须单独分析。

### 连续追问

1. 主键回填为什么不等于事务已经提交？
2. foreach 批量插入与循环单条执行的收益和 SQL 大小边界是什么？
3. 异常被 catch 后正常返回、同类自调用或异步执行为什么可能破坏预期？
4. 如果写入跨数据库或还要发消息，本地事务还够不够？

### 待补实现证据

- 事务入口、传播/回滚配置和 Mapper 调用链。
- 自增主键回填配置、批量 SQL 与表外键/唯一约束。
- 明细中途失败后主表和已写明细均回滚的测试。

### 不能越界

不声称本地 `@Transactional` 可以覆盖异步线程、远程调用、消息系统或多数据库，也不编造线上回滚事故。

## 历史订单地址快照

### 简历主张

下单时固化收货人信息，使历史订单与可变地址簿解耦，避免后续地址变化影响历史记录。主张强度 `AR`，当前证据 `V0`。

### 知识节点

[[07-项目/02-订单与关联数据#447. 为什么订单不能只保存 addressBookId？|不能只存 ID]] · [[07-项目/02-订单与关联数据#448. 什么是订单地址快照？|地址快照]] · [[07-项目/02-订单与关联数据#449. 为什么地址快照属于合理的数据冗余？|合理冗余]] · [[07-项目/02-订单与关联数据#450. 数据冗余一定是不好的设计吗？|冗余边界]] · [[07-项目/02-订单与关联数据#451. 为什么历史订单不能跟随地址簿变化？|历史语义]]。

### 回答骨架

- 背景：地址簿代表当前可编辑资料，订单代表下单时已经发生的业务事实。
- 动作：下单时把配送所需收货信息复制到订单侧，而不是查询时始终关联当前地址簿。
- 机制：后续地址修改或删除不会改变历史订单展示和履约依据。
- 结果：实现历史订单与可变主数据解耦。
- 边界：具体复制字段、脱敏、地址变更流程和数据保留规则都需表结构核验。

### 连续追问

1. 为什么只保存 addressBookId 会破坏历史订单语义？
2. 快照需要复制哪些字段，如何避免复制无关或敏感信息？
3. 数据冗余会增加哪些存储、更新和合规成本？
4. 用户删除地址或修改手机号后，历史订单应怎样展示？

### 待补实现证据

- 地址簿表、订单表的真实字段和复制代码位置。
- 下单后修改/删除地址仍保持历史订单不变的接口用例。
- 敏感字段展示和数据保留规则。

### 不能越界

不声称项目实现事件溯源、不可篡改审计、复杂隐私合规体系或地址版本表，除非代码和需求确实存在。

## 支付宝沙箱支付闭环

### 简历主张

对接支付宝沙箱预下单，处理异步回调 RSA 验签，并通过状态校验与重复通知过滤实现幂等，更新支付状态形成扫码支付闭环。主张强度 `AR`，当前证据 `V0`。

### 知识节点

[[07-项目/03-第三方支付#452. 支付宝 Precreate 是什么？|Precreate]] · [[07-项目/03-第三方支付#455. 用户扫码支付后的完整流程是什么？|完整流程]] · [[07-项目/03-第三方支付#456. 什么是支付宝异步通知？|异步通知]] · [[07-项目/03-第三方支付#458. 支付回调验签通过后还要校验什么？|业务校验]] · [[07-项目/03-第三方支付#459. 什么是 RSA 验签？|RSA 验签]] · [[07-项目/03-第三方支付#464. 什么是支付回调幂等？|幂等]] · [[07-项目/03-第三方支付#467. 项目中如何保证支付回调幂等？|项目幂等]] · [[07-项目/03-第三方支付#468. 为什么支付状态迁移必须是原子的？|状态迁移]] · [[07-项目/03-第三方支付#471. 支付成功但本地订单状态更新失败怎么办？|最终一致性]]。

### 回答骨架

- 背景：扫码支付不能只相信前端跳转，需要以平台服务端通知和本地订单状态为依据。
- 动作：创建沙箱预下单获得二维码，接收异步通知后完成 RSA 验签和业务字段校验，再按本地合法状态处理支付结果。
- 机制：状态校验与重复通知过滤防止同一回调重复产生业务副作用。
- 结果：在沙箱环境完成扫码支付闭环并可靠更新支付状态。
- 边界：具体 RSA 算法参数、原子更新 SQL、平台响应、重试与对账实现必须以代码和平台配置核验。

### 连续追问

1. Precreate 返回什么，二维码内容怎样交给前端？
2. 验签证明什么，为什么还要校验商户、订单号、金额和支付状态？
3. 重复或乱序通知怎样保证幂等，状态校验是否是数据库原子条件更新？
4. 平台已支付但本地更新失败怎么办，项目是否真实实现主动查询或定时对账？

### 待补实现证据

- 沙箱配置、预下单调用、二维码返回和回调入口。
- 验签参数、业务字段校验、订单状态更新和重复通知处理代码。
- 合法回调、伪造回调、重复通知、金额不符和本地更新失败用例。

### 不能越界

不把沙箱说成生产支付，不声称主动查询、定时对账、消息补偿、退款或生产密钥治理已实现；不在仓库保存任何密钥。

## 统一响应与异常处理

### 简历主张

使用 `@RestControllerAdvice` 统一捕获业务异常和数据库约束异常，封装统一响应模型并规范前后端格式。主张强度 `AR`，当前证据 `V0`。

### 知识节点

[[02-Spring/03-SpringMVC#127. 一个 HTTP 请求进入 Spring MVC 后经历哪些流程？|MVC 请求链]] · [[02-Spring/04-参数校验#145. 参数校验失败后如何统一处理？|校验失败]] · [[07-项目/01-统一响应与异常处理#429. 为什么后端需要统一响应结构？|统一响应]] · [[07-项目/01-统一响应与异常处理#431. 什么是全局异常处理？|全局异常]] · [[07-项目/01-统一响应与异常处理#432. `@RestControllerAdvice` 有什么作用？|Advice]] · [[07-项目/01-统一响应与异常处理#433. `@ExceptionHandler` 有什么作用？|ExceptionHandler]] · [[07-项目/01-统一响应与异常处理#437. 数据库异常为什么不应该直接返回前端？|数据库异常]] · [[07-项目/01-统一响应与异常处理#438. 唯一约束异常如何转换成友好提示？|友好提示]]。

### 回答骨架

- 背景：Controller 重复 try-catch 会让响应格式和错误信息不一致，还可能泄露内部细节。
- 动作：在全局 Advice 中按异常类型映射稳定业务码和用户可理解信息，成功与失败统一使用响应模型。
- 机制：业务异常表达预期失败，数据库约束异常作为数据层兜底，内部日志与外部响应分离。
- 结果：规范前后端交互格式并减少重复处理代码。
- 边界：异常匹配顺序、作用域、HTTP 状态码、日志关联和未知异常策略需代码核验。

### 连续追问

1. `@ControllerAdvice`、`@RestControllerAdvice` 和 `@ExceptionHandler` 分别做什么？
2. 参数校验、业务异常、唯一约束和系统异常怎样分类？
3. 为什么不能把 SQL 信息、堆栈和内部类名直接返回前端？
4. 统一返回 HTTP 200 是否合理，业务码与 HTTP 状态如何分工？

### 待补实现证据

- 统一响应类、全局 Advice、异常类型和映射表。
- 参数错误、业务错误、重复数据和未知异常接口用例。
- 外部响应与内部日志的脱敏和关联方式。

### 不能越界

不声称全局捕获等于系统不会失败，不把所有数据库异常都映射成“数据重复”，也不把内部错误细节返回客户端。

## 阿里云 OSS 核验风险

### 当前事实

阿里云 OSS 只出现在简历技术栈，没有对应的职责、用途、实现动作或结果描述。主张强度 `S`，当前证据 `V0`。

### 必须先回答

1. 项目是否真实调用 OSS，上传的对象是什么？
2. 上传发生在服务端还是客户端，接口输入输出是什么？
3. 使用 SDK、签名直传还是其他方式，凭证怎样获取和保护？
4. 数据库保存对象 Key 还是完整 URL，删除/替换怎样处理？
5. 是否有大小、类型、权限、过期访问或异常重试限制？

### 待补实现证据

- 真实依赖、配置项名称、上传入口和对象路径策略。
- 一次可复现上传/读取用例和失败处理。
- 凭证不进入仓库的配置证明。

### 不能越界

在证据补齐前，不把常见 OSS 上传教程当成个人实现，不主动声称分片上传、CDN、私有桶签名 URL、断点续传或生产权限治理。

## 总体验收

- [ ] 七条项目亮点都能在 60 秒内说清背景、动作、机制、结果和边界。
- [ ] 每条至少定位一个 V1 实现材料，并完成一个 V2 验证用例。
- [ ] 所有性能、规模和故障数字都有真实环境、基线与复测方式；没有数据就删除数字。
- [ ] 阿里云 OSS 已补真实证据，或面试表达明确限制在实际使用范围。
- [ ] 项目回答中的未知细节能坦诚标记，不用通用方案补写个人经历。
- [ ] 模拟/真实面试断点已回流 [[00-第二轮学习队列#六线证据队列]]。
