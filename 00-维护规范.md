---
category: 仓库维护
tags:
  - Java后端
  - Obsidian
  - 维护规范
---

# Java 后端实习面试知识库维护规范

## 一、最高目标

本知识库是个人 Java 后端求职知识地图，不是追求大而全的技术百科。

维护优先级固定为：

1. 准确性高于完备性。
2. 求职价值高于题目数量。
3. 理解和表达高于机械背诵。
4. 真实项目场景高于虚构包装。
5. Obsidian 数据完整性高于目录美观。

### 学习优先级语义

- `P0`：Java 后端高频基础、简历技能或真实项目强相关。目标是 30～90 秒脱稿回答，并能承接至少两层追问。
- `P1`：常见扩展、原理题、笔试与工程基础。目标是说清核心原理、适用边界和典型场景。
- `P2`：当前简历未直接使用或实习面试中相对低频。目标是知道用途、关键限制和知识位置。

`priority` 按整篇知识笔记标记，表示当前求职阶段的学习顺序和掌握深度，不表示技术本身的重要程度。简历相关性与知识频率是两个维度：不能因为某个通用方案常见，就把它包装成个人项目经历；也不能仅凭一次面试追问就批量调整全库优先级。

## 二、核心题目结构

全部编号题必须按以下顺序各保留一次，并保证每段正文非空：

```markdown
## 编号. 问题

**面试回答**

**原理与理解**

**成立条件与边界**

**实际场景**

**常见追问**

**易错点**
```

“实际场景”允许使用中文括号标注场景类型，例如 `实际场景（真实项目）` 或 `实际场景（通用工程）`，但不能省略场景段。不是每个扩展知识点都必须独立成题，可以通过“常见追问”形成问题链，避免把一个概念过度拆分成多个重复问题。

完整性检查会验证六段的唯一性、顺序和非空内容。自动检查只证明结构完整，不能替代技术准确性审查。

## 三、场景使用规则

“实际场景”必须属于以下类型之一：

- **真实项目场景**：确实在个人项目中实现或使用过，可以使用“项目中”表述。
- **通用工程场景**：业界常见做法，只能使用“实际开发中”“常见系统中”等表述。
- **反例或边界场景**：说明什么时候不适合使用，或错误使用会造成什么问题。

禁止把通用案例改写成个人项目经历。无法确认是否真实实现时，默认按通用工程场景描述。

## 四、准确性与资料来源

资料优先级：

1. Java 语言规范、JDK API、JEP 和对应版本源码。
2. Spring、MyBatis、MySQL、Redis 等项目的官方文档和源码。
3. HTTP RFC、支付平台官方文档等协议或产品一手资料。
4. 高质量技术书籍。
5. 第三方文章只作为线索，不能单独支撑关键结论。

需要特别审查以下措辞：

- 一定、完全、永远、绝不会；
- 默认、通常、可能、建议；
- 线程安全、原子性、一致性、幂等；
- JDK、Spring、MySQL、Redis 等版本相关结论。

无法确认的内容标记为“待校验”，不使用肯定语气包装。

## 五、版本边界

版本敏感的笔记应逐步补充以下 Frontmatter 属性：

```yaml
verified: 待校验
reviewed_at:
version_scope:
  - JDK 8
```

- `status` 只表示个人学习状态，内容维护不得擅自修改。
- `verified` 表示内容准确性状态。
- `reviewed_at` 表示最近一次准确性审查日期。
- `version_scope` 表示答案适用的技术版本。

旧版本知识和新版本差异应并列说明，不能直接用新版本结论覆盖常见面试语境。

## 六、Obsidian 兼容规则

1. 保留 YAML Frontmatter，禁止格式化工具擅自重排或删除属性。
2. 保留 `\[\[文件\]\]`、`\[\[文件#标题\]\]`、`\[\[文件|别名\]\]` 等 Wikilink。
3. 不批量把 Wikilink 转换成普通 Markdown 链接。
4. 不随意移动或重命名文件；确需操作时必须同步更新全部引用。
5. 不随意修改被引用的标题；确需修改时必须同步更新标题锚点。
6. 题号作为稳定 ID。删除或合并题目后允许留出缺号，不全库重排。
7. 保持 UTF-8 编码、中文文件名和现有目录结构。
8. 不删除整个 `.obsidian` 目录；配置清理由仓库所有者单独决定。
9. 自动化只能检查文件，不得自动格式化或重写 Vault。

## 七、学习状态保护

允许的学习状态：

- 未学习
- 看过
- 能回答
- 需复习

知识内容修改不能顺带改变 `status`。如果题目被移动或合并，应尽量保留原有学习状态，并在 Pull Request 中说明映射关系。

## 八、分支与 Pull Request

- 每次只维护一个模块或一个明确目标。
- 分支建议使用 `maintenance/v版本-主题`。
- 不直接向 `master` 写入批量内容修改。
- 每个 Pull Request 必须说明修改范围、准确性依据和 Obsidian 兼容检查结果。
- 知识内容优先按 30～60 道题或单个模块拆分审查。

合并前必须运行：

```bash
python scripts/check_vault.py
```

## 九、合并与删除规则

题目满足以下条件时可以考虑合并：

- 问题实质相同，仅换了表达方式；
- 答案大段重复，没有形成新的追问层级；
- 拆分后反而破坏完整回答链。

合并时应：

1. 保留更适合作为稳定入口的题号。
2. 更新全部标题链接。
3. 在 Pull Request 中记录被合并题号。
4. 不为填补缺号重新编号后续题目。

## 十、验收清单

- [ ] 技术结论可以由可靠资料验证。
- [ ] 版本、前提和例外已经说明。
- [ ] 面试回答可以在 30～90 秒内复述。
- [ ] 场景类型真实且表述准确。
- [ ] 常见追问能形成知识链。
- [ ] 没有擅自修改学习状态。
- [ ] Frontmatter 和 Wikilink 保持有效。
- [ ] 完整性检查脚本通过。
