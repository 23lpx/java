---
category: 学习任务
phase: 第一轮
day: 17
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第十七天学习任务与闭卷题单

> Day 17 按真实进度推进：工程线学习中间件/分布式与部署基础 10 道新题；已完成编号节点收口的 Java、Spring/MyBatis、MySQL、Redis 转入线程池饱和、异步事务、长事务和缓存穿透场景；算法线手写多源 BFS。

导航：[[00-总目录]] · [[00-第一轮学习队列]] · [[00-第十六天学习任务与闭卷题单]] · [[00-每日学习与闭卷考核模板]]

## 开始前

- [ ] 复制 [[00-每日学习与闭卷考核模板]]，以当天日期命名。
- [ ] 从 Day 16 记录中为每条学习线选择一个 `不会` 或 `不稳` 断点。
- [ ] 如果某条线连续失败题超过 2 道，本日先修复最靠前的核心断点，再决定是否执行综合场景。
- [ ] Day 16 尚未执行时，先完成 [[00-第十六天学习任务与闭卷题单]]。

## Day 16 回流槽

| 学习线 | 回流题号 | Day 16 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 / 回查或实战 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | 回查 [[09-Java并发/02-锁与线程池#511. 什么是线程池？|511]]、[[09-Java并发/02-锁与线程池#512. 为什么要使用线程池？|512]]、[[09-Java并发/02-锁与线程池#513. 线程池相比频繁创建线程有什么优势？|513]]、[[09-Java并发/02-锁与线程池#514. ThreadPoolExecutor 有哪些核心参数？|514]]、[[09-Java并发/02-锁与线程池#515. corePoolSize 是什么？|515]]、[[09-Java并发/02-锁与线程池#516. maximumPoolSize 是什么？|516]]、[[09-Java并发/02-锁与线程池#517. workQueue 是什么？|517]]、[[09-Java并发/02-锁与线程池#518. 拒绝策略是什么？|518]]、[[09-Java并发/02-锁与线程池#519. 常见拒绝策略有哪些？|519]]；完成线程池饱和排障 | 0 / 1 场景 | 从任务到达、核心线程、队列、最大线程和拒绝还原饱和路径，用指标区分线程池容量、任务耗时与下游故障 |
| ② Spring 与数据访问 | 回查 [[02-Spring/08-Spring事务#188. `@Transactional` 有什么作用？|188]]、[[02-Spring/08-Spring事务#190. Spring 声明式事务大致是怎么实现的？|190]]、[[02-Spring/08-Spring事务#192. Spring 默认哪些异常会触发回滚？|192]]、[[02-Spring/08-Spring事务#195. 把异常 catch 住为什么可能导致事务不回滚？|195]]、[[02-Spring/08-Spring事务#198. 什么是事务传播行为？|198]]、[[02-Spring/08-Spring事务#199. `Propagation.REQUIRED` 是什么意思？|199]]、[[02-Spring/08-Spring事务#200. 项目中为什么主表和子表写入需要放在同一个事务中？|200]]、[[03-MyBatis/01-MyBatis#216. 如何通过 `<foreach>` 实现批量插入？|216]]；完成异步事务边界演练 | 0 / 1 场景 | 解释线程绑定事务为何不会自动跨到 CompletableFuture，区分同步原子写、独立事务和最终一致性设计 |
| ③ MySQL | 回查 [[04-MySQL/04-事务#283. 什么是 MVCC？|283]]、[[04-MySQL/04-事务#286. 什么是 Read View？|286]]、[[04-MySQL/04-事务#287. 什么是 undo log？|287]]、[[04-MySQL/04-事务#288. undo log 有什么作用？|288]]、[[04-MySQL/04-事务#289. 什么是 redo log？|289]]、[[04-MySQL/04-事务#290. redo log 有什么作用？|290]]、[[04-MySQL/05-锁#296. 什么是行锁？|296]]、[[04-MySQL/05-锁#304. 如何降低死锁发生概率？|304]]；完成长事务排障 | 0 / 1 场景 | 区分长只读快照的版本保留与长写事务的锁持有，结合事务年龄、锁等待、undo/purge 和回滚成本治理 |
| ④ Redis | 回查 [[05-Redis/04-缓存穿透击穿雪崩#343. 什么是缓存穿透？|343]]、[[05-Redis/04-缓存穿透击穿雪崩#344. 缓存穿透是如何产生的？|344]]、[[05-Redis/04-缓存穿透击穿雪崩#345. 缓存穿透为什么会给数据库造成压力？|345]]、[[05-Redis/04-缓存穿透击穿雪崩#346. 如何解决缓存穿透？|346]]、[[05-Redis/04-缓存穿透击穿雪崩#347. 缓存空值如何解决缓存穿透？|347]]、[[05-Redis/04-缓存穿透击穿雪崩#348. 什么是布隆过滤器？|348]]、[[05-Redis/04-缓存穿透击穿雪崩#349. 布隆过滤器为什么存在误判？|349]]、[[05-Redis/04-缓存穿透击穿雪崩#360. 缓存穿透、击穿、雪崩有什么区别？|360]]；完成随机无效 ID 攻击演练 | 0 / 1 场景 | 从输入、缓存 miss、数据库空结果和高基数 Key 还原穿透链路，组合校验、限流、空值与布隆过滤器生命周期 |
| ⑤ Web、项目与工程 | [[11-中间件与分布式/01-中间件与分布式基础]] 题 615～620；[[10-开发基础/05-部署基础]] 题 621～624 | 10 / 0 | 串联注册发现、限流、熔断、分布式事务、CAP、Docker 和 Nginx，区分基础设施职责、故障窗口与业务保证 |
| ⑥ 算法与手写 | 回流 [[12-算法/02-算法基础#644. BFS 的基本写法是什么？|题 644]]；腐烂橘子 | 0 + 实战 | 把全部腐烂橘子作为第 0 层同时入队，维护新鲜数量和分钟层数，说明入队即标记、不可达判定与原地修改契约 |

> Java、Spring/MyBatis、MySQL、Redis 已完成第一轮编号节点覆盖，本日只进行回查和综合场景，不把旧题计为新题。Day 17 的新题量为 10，全部来自工程线；四个场景按证据链和故障闭环单独验收。

### Java 线综合场景

订单导出服务使用 `ThreadPoolExecutor(core=16, maximum=64, queueCapacity=10000, AbortPolicy)`。流量高峰时队列迅速接近上限，接口 P99 持续上升，随后出现拒绝；团队准备把队列改成无界并把最大线程数调到 500。

必须完成：

1. 收集到达率、完成率、活动/池线程数、队列长度与等待时间、任务耗时分布、拒绝数、CPU/内存/GC 和下游依赖延迟，先确认瓶颈时间线。
2. 说明任务通常先使用核心线程，再按队列和最大线程规则接收；具体扩容路径受队列类型与配置影响，不能只背“核心—队列—最大”而忽略实现条件。
3. 大队列可暂时少拒绝，却会把过载转化为长等待、占用内存和超时后无效工作；无界队列还可能让 `maximumPoolSize` 基本失去扩容作用。
4. 区分 CPU 密集、阻塞 I/O 和下游限速任务；线程数必须用机器资源、阻塞比、服务时间和压测校准，不能套固定倍数公式。
5. 明确业务拒绝语义：`AbortPolicy` 让调用方感知失败；`CallerRunsPolicy` 在未 shutdown 时可形成同步反压，但会占用请求线程；关键任务不能静默丢弃。
6. 治理应包含有界容量、入口限流/降级、任务超时与取消、下游保护、幂等重试和监控；本场景为通用演练，不表述成简历真实线上事故。

### Spring/MyBatis 线综合场景

订单 Service 的 `createOrder()` 标记了 `@Transactional`：主线程先插入订单主表，再通过 `CompletableFuture.runAsync` 调用 Mapper 批量插入明细，然后方法立即返回。异步明细写入失败时，主表仍然提交。

必须完成：

1. 画出主线程事务、异步工作线程、各自连接/SqlSession 和异常传播路径；Spring 命令式事务通常绑定当前线程，不会因 lambda 由同一方法创建就自动跨线程传播。
2. `Propagation.REQUIRED` 只在当前调用线程已有事务时加入；异步线程若通过代理进入事务方法，通常开启自己的独立事务，不能加入已提交或仍在另一线程中的主事务。
3. `future.join()` 可以让主线程观察完成或异常，但不会把两个线程的数据库操作合成一个事务；若异步事务已经提交，外层回滚也不能撤销它。
4. 主表、主键回填和明细批量写入若要求同库原子性，应在同一同步事务中完成；事务内不夹带耗时远程调用。
5. 若业务必须异步，重新定义为最终一致性流程：可靠地产生任务、消费者幂等、状态机、失败重试/补偿和可观测性，不能用 `@Transactional` 注解掩盖跨线程边界。
6. 简历确认的是声明式事务、主表/子表和批量写入；未证明使用 CompletableFuture、消息事务或补偿机制，面试中必须明确这是通用反例。

### MySQL 线综合场景

批处理任务开启事务后扫描并更新一万条订单，中途逐条调用远程服务，事务持续三十分钟。期间线上写请求出现锁等待，监控还显示旧版本清理压力上升。

必须完成：

1. 对齐事务开始时间、当前 SQL、已修改行、锁等待关系、undo/历史版本保留、redo 写入和远程调用耗时；具体诊断视图与字段按 MySQL 版本核验。
2. 长写事务通常让已获得的记录锁持续到提交/回滚，并产生用于回滚/MVCC 的 undo；长一致性读即使不持有这些写锁，也可能因旧 Read View 延迟相关历史版本清理。
3. 区分 undo 的回滚/版本链职责与 redo 的崩溃恢复职责；不能把“undo 很大”说成 redo 一定很大，也不能把 purge 延迟等同于死锁。
4. 把远程调用移出持锁事务，按稳定主键顺序、受控批次和短事务处理；分批提交会改变全批原子性，必须配合幂等、进度检查点和失败恢复设计。
5. 发现异常长事务时先确认业务身份、外部副作用和预计回滚成本，不能直接杀会话；回滚本身可能长时间占用资源，需要监控和变更流程。
6. 用事务时长、锁等待、历史版本/undo 压力、批次吞吐和失败恢复演练验证改造，而不是只看一次任务是否最终跑完。

### Redis 线综合场景

接口允许按菜品 ID 查询详情。攻击流量持续请求随机、几乎不存在的 ID；Redis 每次 miss 后都查询 MySQL，数据库 QPS 快速上涨，而缓存命中率接近零。

必须完成：

1. 根据“高频请求不存在数据、缓存和数据库都无法命中”识别缓存穿透，区别单热点过期的击穿和大量 Key 同时失效的雪崩。
2. 在入口做格式/范围校验、鉴权和限流，减少明显无效与恶意请求；缓存策略不能替代访问控制和容量保护。
3. 对数据库确认不存在的结果缓存短 TTL 空值，并区分“空值占位”与正常业务空对象；随机高基数攻击下要限制 Key 数量和 TTL，避免用空值制造新的内存压力。
4. 布隆过滤器可快速判定“一定不在集合”的请求并允许“可能存在”继续查库；假阳性会带来少量回源，位数组大小和哈希数量影响误判率与内存。
5. 标准布隆过滤器理论上的“无假阴性”依赖元素确实被正确加入且生命周期一致；新增数据、重建切换、删除和同步失败都要设计，否则系统可能错误拒绝真实 ID。
6. 监控无效 ID 比例、空值 Key 数、布隆拦截/放行、数据库空结果 QPS 和误拒绝反馈；项目是否使用空值或布隆过滤器属于核验项，不得虚构。

### 算法线编码要求

实现 `int orangesRotting(int[][] grid)`。`grid` 非 `null`、为矩形，只包含 0（空）、1（新鲜）和 2（腐烂）；上下左右相邻的新鲜橘子每分钟腐烂。返回所有新鲜橘子腐烂所需的最少分钟数，不可能时返回 -1；空网格或初始没有新鲜橘子时返回 0。允许原地把新鲜格修改为腐烂状态。

至少验证：

```text
grid = []                                  -> 0
grid = [[2,1,1],[1,1,0],[0,1,1]]           -> 4
grid = [[2,1,1],[0,1,1],[1,0,1]]           -> -1
grid = [[0,2]]                              -> 0
grid = [[1]]                                -> -1
grid = [[2,2],[2,2]]                        -> 0
```

完成代码后必须口述：为什么全部初始腐烂格要同时作为第 0 层入队、为什么新鲜格在入队时就要改为腐烂并递减计数、每一层与一分钟如何对应，以及时间复杂度 `O(mn)`、最坏队列空间 `O(mn)`。只有四方向等权传播才能直接使用这套多源 BFS 最短时间证明。

### 每线完成条件

- [ ] Day 16 回流题已重新闭卷回答，并记录修复后的结果。
- [ ] Java 场景包含饱和路径、指标证据、队列/线程取舍、拒绝语义和下游保护。
- [ ] Spring/MyBatis 场景能解释线程事务边界，并区分同步原子写与异步最终一致性。
- [ ] MySQL 场景能区分锁持有、Read View、undo/purge、redo 和回滚成本。
- [ ] Redis 场景包含穿透识别、入口保护、空值容量、布隆生命周期和指标验证。
- [ ] 工程线 10 道新题形成职责地图；腐烂橘子通过全部用例并能解释多源 BFS 层次不变量。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-每日学习与闭卷考核模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[09-Java并发/02-锁与线程池#514. ThreadPoolExecutor 有哪些核心参数？|题 514：ThreadPoolExecutor 有哪些核心参数？]]

- 主问题：说明核心/最大线程数、存活时间、工作队列、线程工厂和拒绝处理器如何共同决定接收路径。
- 场景追问：为什么容量一万的队列可能让接口先长时间排队再拒绝，无界队列又怎样影响最大线程数？
- 边界追问：CPU 密集和阻塞 I/O 任务如何通过测量定容量，为什么不存在通用最优线程数？

### ② Spring 与数据访问

[[02-Spring/08-Spring事务#198. 什么是事务传播行为？|题 198：什么是事务传播行为？]]

- 主问题：说明传播行为处理的是事务方法相互调用时如何加入、创建、挂起或不使用事务。
- 场景追问：异步线程中的 `REQUIRED` 为什么不能加入主线程事务，`future.join()` 又为什么不能合并两个事务？
- 边界追问：自调用绕过代理、跨线程和跨数据库分别属于什么边界，为什么不能只改一个 propagation 值？

### ③ MySQL

[[04-MySQL/04-事务#288. undo log 有什么作用？|题 288：undo log 有什么作用？]]

- 主问题：说明 undo 对事务回滚和 MVCC 历史版本的职责。
- 场景追问：三十分钟写事务与长只读快照分别怎样影响锁和旧版本清理？
- 边界追问：undo、redo、binlog 为什么不能用“一个撤销、两个重做”草率概括，回滚长事务有什么运维代价？

### ④ Redis

[[05-Redis/04-缓存穿透击穿雪崩#348. 什么是布隆过滤器？|题 348：什么是布隆过滤器？]]

- 主问题：说明位数组、多哈希、“一定不存在/可能存在”和假阳性。
- 场景追问：随机无效 ID 攻击中，布隆过滤器、空值缓存、校验和限流分别处于哪一层？
- 边界追问：为什么“标准结构无假阴性”仍不能推出业务系统绝不误拒绝新增数据？

### ⑤ Web、项目与工程

[[11-中间件与分布式/01-中间件与分布式基础#620. CAP 理论是什么？|题 620：CAP 理论是什么？]]

- 主问题：说明网络分区发生时，一致性与可用性之间的取舍，而不是背“平时三选二”。
- 追问 1：CAP 中的一致性通常指什么，可用性又为什么不是“服务进程还活着”？
- 追问 2：没有分区时系统是否仍必须固定放弃 C 或 A，业务如何按操作选择降级策略？

### ⑥ 算法与手写

[[12-算法/02-算法基础#644. BFS 的基本写法是什么？|题 644：BFS 的基本写法是什么？]]

- 主问题：说明队列、入队标记、按层遍历和无权/等权最短性的关系。
- 手写：重新实现今天的腐烂橘子，不能查看白天代码。
- 追问：多源初始化为什么等价于增加一个超级源点？若传播时间不同，为什么不能直接按普通 BFS 层数计算？

## 评分规则

Java、工程题每题 10 分：核心结论 2 分、原理链路 2 分、成立条件与边界 2 分、两层追问 2 分、场景或工程影响 2 分。

Spring/MyBatis、MySQL、Redis 场景题每题 10 分：证据链 2 分、故障时序 2 分、成立条件 2 分、治理闭环 2 分、项目真实性 2 分。

算法题 10 分：函数契约 1 分、多源 BFS 不变量 3 分、层数与不可达边界 2 分、代码和返回值 2 分、复杂度 2 分。

总分 60 分：

- 48～60：整体通过；单题不足 8 分仍记为 `不稳`。
- 36～47：Day 18 保留对应回流槽，并减少失败学习线的新题量或场景复杂度。
- 0～35：暂停薄弱线扩题，优先修复重复失败的知识链。

## 考后动作

- [ ] 对照原笔记纠错，记录首个错误断点。
- [ ] 将六道题分别标记为 `不会` / `不稳` / `通过`。
- [ ] 仅根据闭卷证据更新相关知识笔记的 `status`。
- [ ] 工程线只有在 615～624 全部通过后，才推进到最后的部署节点。
- [ ] 四个综合场景未通过时，下一日继续修复失败链，不用新的场景掩盖问题。
- [ ] 把连续失败题和多源 BFS 的首个错误加入 Day 18 回流。
- [ ] 原笔记疑点单独记录，核验可靠资料后再创建维护 PR。
