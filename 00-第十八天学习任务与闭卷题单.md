---
category: 学习任务
phase: 第一轮
day: 18
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第十八天学习任务与闭卷题单

> Day 18 完成第一轮编号覆盖收口：工程线学习最后 3 道新题；Java、Spring/MyBatis、MySQL、Redis 分别训练 JVM 锁死、请求身份串号、一对多联表分页和 Redis 大 Value 延迟；算法线手写链表判环。完成本日只代表 649 道题都进入过学习流程，不代表已经全部掌握。

导航：[[00-总目录]] · [[00-第一轮学习队列]] · [[00-第十七天学习任务与闭卷题单]] · [[00-每日学习与闭卷考核模板]]

## 开始前

- [ ] 复制 [[00-每日学习与闭卷考核模板]]，以当天日期命名。
- [ ] 从 Day 17 记录中为每条学习线选择一个 `不会` 或 `不稳` 断点。
- [ ] 如果某条线连续失败题超过 2 道，本日先修复最靠前的核心断点，再执行对应场景。
- [ ] Day 17 尚未执行时，先完成 [[00-第十七天学习任务与闭卷题单]]。

## Day 17 回流槽

| 学习线 | 回流题号 | Day 17 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 / 回查或实战 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | 回查 [[09-Java并发/01-线程基础#502. 什么叫线程安全？|502]]、[[09-Java并发/01-线程基础#503. 为什么会出现线程安全问题？|503]]、[[09-Java并发/02-锁与线程池#504. `synchronized` 是什么？|504]]、[[09-Java并发/02-锁与线程池#505. `synchronized` 可以锁什么？|505]]、[[09-Java并发/02-锁与线程池#509. 什么是死锁？|509]]、[[09-Java并发/02-锁与线程池#510. 死锁产生的四个必要条件是什么？|510]]、[[09-Java并发/03-并发进阶#594. ReentrantLock 是什么？|594]]、[[09-Java并发/03-并发进阶#595. synchronized 和 ReentrantLock 有什么区别？|595]]；完成 JVM 锁死排障 | 0 / 1 场景 | 根据线程转储还原“持有什么、等待什么”的环，比较统一加锁顺序与可中断/超时获取的适用边界 |
| ② Spring 与数据访问 | 回查 [[02-Spring/05-Filter与Interceptor#148. Interceptor 是什么？|148]]、[[02-Spring/05-Filter与Interceptor#150. `HandlerInterceptor` 有哪些主要方法？|150]]、[[02-Spring/05-Filter与Interceptor#151. `preHandle()` 在什么时候执行？|151]]、[[02-Spring/05-Filter与Interceptor#153. `afterCompletion()` 在什么时候执行？|153]]、[[02-Spring/05-Filter与Interceptor#154. 为什么 JWT 登录校验适合放在拦截器？|154]]、[[06-Web/02-ThreadLocal#400. ThreadLocal 是什么？|400]]、[[06-Web/02-ThreadLocal#407. 为什么 ThreadLocal 使用完需要 `remove()`？|407]]、[[06-Web/02-ThreadLocal#408. Tomcat 线程池为什么会产生 ThreadLocal 数据残留风险？|408]]；完成请求身份串号演练 | 0 / 1 场景 | 画出 JWT 校验、ThreadLocal 写入、Controller 读取和请求结束清理链路，覆盖异常、放行路径与异步线程边界 |
| ③ MySQL | 回查 [[04-MySQL/01-SQL基础#228. `ORDER BY` 有什么作用？|228]]、[[04-MySQL/01-SQL基础#229. `LIMIT` 如何实现分页？|229]]、[[04-MySQL/01-SQL基础#231. LEFT JOIN 是什么？|231]]、[[04-MySQL/01-SQL基础#232. INNER JOIN 和 LEFT JOIN 有什么区别？|232]]、[[04-MySQL/01-SQL基础#234. `COUNT(*)` 和 `COUNT(column)` 有什么区别？|234]]、[[04-MySQL/01-SQL基础#238. 深分页为什么可能比较慢？|238]]、[[07-项目/02-订单与关联数据#439. 为什么订单表和订单明细表要拆开？|439]]、[[07-项目/02-订单与关联数据#440. 什么是一对多关系？|440]]；完成一对多联表分页演练 | 0 / 1 场景 | 解释 LIMIT 作用于连接结果行而非父实体，设计稳定排序、父表分页与明细批量回查，并校准 count 口径 |
| ④ Redis | 回查 [[05-Redis/01-Redis基础#307. Redis 为什么快？|307]]、[[05-Redis/01-Redis基础#308. Redis 是完全单线程的吗？|308]]、[[05-Redis/01-Redis基础#309. Redis 单线程为什么还能这么快？|309]]、[[05-Redis/01-Redis基础#310. 什么是 IO 多路复用？|310]]、[[05-Redis/01-Redis基础#319. Redis 中 Key 应该如何设计？|319]]、[[05-Redis/02-过期与内存管理#325. Redis 内存满了怎么办？|325]]、[[05-Redis/02-过期与内存管理#326. 什么是内存淘汰策略？|326]]、[[05-Redis/05-RedisTemplate与SpringCache#363. RedisTemplate 中的序列化是什么？|363]]；完成大 Value 延迟排障 | 0 / 1 场景 | 把 Redis 命令执行、网络传输、客户端反序列化和 JVM GC 分段测量，按访问粒度治理大对象而不是只看命中率 |
| ⑤ Web、项目与工程 | 回查 [[10-开发基础/05-部署基础#624. Nginx 是什么？|624]]；新学 [[10-开发基础/05-部署基础#625. Nginx 常见用途有哪些？|625]]、[[10-开发基础/05-部署基础#626. 什么是反向代理？|626]]、[[10-开发基础/05-部署基础#627. 什么是负载均衡？|627]]；完成上游实例故障演练 | 3 / 1 场景 | 串联客户端、Nginx 与多个应用实例，区分反向代理、负载分配、被动失败检测、超时和业务幂等 |
| ⑥ 算法与手写 | 回流 [[12-算法/02-算法基础#638. 链表常见题型有哪些？|题 638]]；环形链表判定 | 0 + 实战 | 用快慢指针判断节点引用是否成环，说明相对速度、空指针守卫、不修改链表和 `O(1)` 额外空间 |

> Day 18 的新题量为 3，全部来自工程线 625～627；四条技术线与工程场景共回查/场景化使用 33 道既有主干题。完成 625～627 后，649 道编号题实现第一轮“进入过学习流程”的覆盖收口；掌握程度仍以闭卷结果和笔记 `status` 为准。

### Java 线综合场景

转账服务在 JVM 内为两个账户对象分别加锁。请求 A→B 先锁 A 再等 B，请求 B→A 先锁 B 再等 A；接口持续不返回，但进程仍存活且 CPU 不高。

必须完成：

1. 获取故障时线程转储，定位 `BLOCKED`/等待锁的线程、持有锁、目标锁和调用栈；低 CPU 与进程存活不能排除死锁。
2. 画出线程—锁等待图，说明互斥、持有并等待、不可剥夺、循环等待如何同时成立；不要只看到两个 `synchronized` 就直接判定死锁。
3. 使用所有调用方一致的稳定顺序获取账户锁，并处理排序键相同的兜底；只在一个方法里交换顺序不能消除其他入口形成的环。
4. 若使用 `ReentrantLock.tryLock(timeout, unit)`，只释放自己成功获得的锁，并在 `finally` 中释放；超时只让请求退出等待，不会自动恢复已发生的业务副作用。
5. 有限重试要配合退避、幂等和失败上限，否则可能从死锁变成活锁或放大流量；公平锁也不等于线程调度绝对公平，且可能降低吞吐。
6. JVM 对象锁只协调单进程线程；多实例部署下还要依靠数据库条件更新、事务或其他跨实例一致性方案。本场景是通用演练，不表述成简历真实事故。

### Spring/MyBatis 线综合场景

登录拦截器验签后把 userId 写入 ThreadLocal，但没有在请求结束时清理。压力测试中，同一个 Tomcat 工作线程随后处理另一个请求，业务日志偶发出现前一个用户的 userId。

必须完成：

1. 按请求线程画出 Token 获取与校验、`set`、Controller/Service `get`、响应或异常、`remove` 的完整时序，并用线程名和请求 ID 关联证据。
2. ThreadLocal 隔离的是不同线程的副本，不是不同请求；容器线程池会复用线程，旧值未移除时后续请求可能读取同一线程残留。
3. 在建立上下文前先明确认证失败和放行路径，只有验证成功的身份才能写入；ThreadLocal 是已验证上下文的传递工具，不能替代验签和授权。
4. `afterCompletion` 适合清理已成功通过 `preHandle` 的拦截器上下文，但它只有在该拦截器的 `preHandle` 成功完成并返回 `true` 后才会调用；所有写入路径都必须有对应清理保证。
5. 对同步请求可在拦截器清理；若希望覆盖更外层链路，可在 Filter 的 `try/finally` 中承担建立/清理所有权。异常、提前返回和多拦截器顺序都要测试。
6. 普通 ThreadLocal 不会把值自动传播到异步线程；若显式复制上下文，任务完成后同样必须清理。简历确认了 JWT、双拦截器与 ThreadLocal 链路，未确认发生过身份串号事故。

### MySQL 线综合场景

订单列表为了同时返回明细，直接执行 `orders LEFT JOIN order_detail ... ORDER BY orders.create_time DESC LIMIT 20`。一个订单有多条明细后，页面只剩少量不同订单，翻页还出现重复或遗漏，count 与列表也不一致。

必须完成：

1. 用最小样本展示一条父记录匹配多条子记录后会生成多行连接结果；`LIMIT 20` 限制的是这些结果行，不是 20 个不同订单。
2. 先在订单表按筛选条件和稳定全序分页，例如 `ORDER BY create_time DESC, id DESC`，再按本页订单 ID 批量查询明细并在应用层组装，避免对每个订单发一次查询。
3. count 必须和“订单数”业务口径一致；可从父表直接计数，只有在必须连接过滤时才评估 `COUNT(DISTINCT orders.id)`，不能把 `COUNT(detail.id)` 当订单总数。
4. 若需要保留没有明细的订单，子表过滤条件放在 `ON` 还是 `WHERE` 会改变语义；先写清需求，再用空子表样本验证 LEFT JOIN 结果。
5. 排序列存在相同值时只按时间排序不是确定顺序，翻页可能受执行计划和并发写入影响；追加唯一 ID 作为 tie-breaker，深页再评估基于游标的翻页。
6. 用结果集基数、唯一订单数、count SQL、执行计划和并发插入测试验证修复。订单主表/明细表是一对多属于真实项目结构，本次错页故障是假设场景。

### Redis 线综合场景

菜品详情缓存命中率很高，但接口 P99 仍然升高。某些 Key 的 JSON Value 达到数 MB；Redis 服务端 CPU 看起来正常，应用端却出现网络读取、反序列化和 GC 峰值。

必须完成：

1. 把端到端耗时拆成连接池等待、网络往返与响应字节、Redis 命令执行、客户端反序列化和应用处理，不能用“命中”直接推出“快”。
2. 检查 value 字节大小、数据类型、访问频率、TTL/淘汰、客户端线程和 JVM 分配率；Slow Log 只能提供服务端命令执行证据，不能代替网络和客户端测量。
3. 使用基于 SCAN 的 `redis-cli --bigkeys`/`--memkeys` 或受控抽样定位大 Key，并通过间隔参数节流；生产排查不使用 `KEYS *` 全量阻塞扫描。
4. 依据实际读取粒度只缓存必要字段，或把独立更新、独立读取的部分拆分；拆分会增加 Key、命令和一致性成本，Hash 也不是所有 JSON 的固定替代品。
5. 序列化格式、类型信息、压缩和版本兼容都要压测；压缩可能降低网络字节，却增加 CPU，不能在没有数据时直接宣称收益。
6. 设置单值大小与响应字节监控，验证 P50/P99、带宽、Redis 延迟、反序列化时间和 GC。项目确认使用 RedisTemplate/Spring Cache，未证明存在数 MB 大 Key。

### 工程线综合场景

Nginx 将请求转发到两个 Spring Boot 实例。实例 B 仍能建立连接，但数据库连接池耗尽，请求长时间无响应；线上间歇出现 502/504，团队认为“已经做了负载均衡，所以服务一定高可用”。

必须完成：

1. 说明反向代理隐藏并代表上游接收客户端请求，负载均衡决定把请求分配给哪个实例；二者都不自动保证应用、数据库和业务正确性。
2. 关联客户端状态码、Nginx access/error log、上游地址与状态、请求总时长和上游连接/首字节/响应时间，再对齐实例日志、线程池与数据库连接池。
3. 区分连接失败、上游提前断开、响应超时与应用返回错误；502/504 是定位入口，不足以单独证明具体根因。
4. Nginx 开源 HTTP upstream 支持基于真实请求结果的被动失败检测；“进程端口可连但业务依赖不可用”未必能被简单连接检查及时识别，健康判定要覆盖关键依赖并避免过重。
5. 超时是端到端预算的一部分，盲目调大只会延长占用；向其他上游重试写请求可能重复副作用，必须结合请求是否已发送、方法语义和业务幂等配置。
6. 验证实例摘流/恢复、优雅发布、容量余量、代理自身冗余和监控告警。Nginx/多实例部署是通用能力，本知识库没有证据证明简历项目采用该架构。

### 算法线编码要求

实现 `boolean hasCycle(ListNode head)`。`ListNode` 通过 `next` 指向下一个节点；当沿 `next` 能再次到达同一个节点对象时视为有环。允许 `head == null`，不得修改节点值或 `next`，只需判断是否有环，不要求返回环入口。

至少验证：

```text
head = null                              -> false
1 -> null                                -> false
1 -> 1（节点自环）                       -> true
1 -> 2 -> null                           -> false
1 -> 2 -> 3 -> 2（尾部指回节点 2）       -> true
1 -> 2 -> 3 -> 4 -> 1（尾部指回头节点）  -> true
```

完成代码后必须口述：为什么循环条件需要同时检查 `fast != null` 与 `fast.next != null`、为什么比较的是节点引用而不是节点值、无环时快指针为何必然先到 `null`，以及有环时快慢指针进入环后相对距离为什么会在有限步内变为 0。时间复杂度 `O(n)`、额外空间 `O(1)`；若要返回环入口，还需要在相遇后增加第二阶段，不能把本题结论直接当入口算法。

### 每线完成条件

- [ ] Day 17 回流题已重新闭卷回答，并记录修复后的结果。
- [ ] Java 场景能从线程转储还原等待环，并说明统一顺序、超时锁与多实例边界。
- [ ] Spring/MyBatis 场景覆盖 ThreadLocal 建立、所有退出路径清理、线程复用与异步边界。
- [ ] MySQL 场景能解释连接行与父实体分页差异，并给出稳定排序、count 和明细批量查询方案。
- [ ] Redis 场景完成服务端、网络、反序列化和 JVM 分段测量，并给出可验证的大 Value 治理方案。
- [ ] 工程线 625～627 通过闭卷，Nginx 场景包含失败证据、被动检测、超时/重试与幂等边界；链表判环通过全部用例。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-每日学习与闭卷考核模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[09-Java并发/02-锁与线程池#510. 死锁产生的四个必要条件是什么？|题 510：死锁产生的四个必要条件是什么？]]

- 主问题：说明互斥、持有并等待、不可剥夺和循环等待分别是什么，为什么四者是必要条件而不是故障证据清单。
- 场景追问：两个转账线程卡住时，如何从线程转储证明等待环并定位持有锁与目标锁？
- 边界追问：统一加锁顺序、`tryLock` 超时和公平锁分别解决什么，不解决什么？

### ② Spring 与数据访问

[[06-Web/02-ThreadLocal#407. 为什么 ThreadLocal 使用完需要 `remove()`？|题 407：为什么 ThreadLocal 使用完需要 remove？]]

- 主问题：说明线程池复用、线程局部值生命周期与数据残留/对象滞留风险。
- 场景追问：JWT 拦截器在哪一步写入、在哪些退出路径清理，`afterCompletion` 的调用前提是什么？
- 边界追问：为什么 ThreadLocal 既不是认证机制，也不会自动把 userId 传播到异步线程？

### ③ MySQL

[[04-MySQL/01-SQL基础#231. LEFT JOIN 是什么？|题 231：LEFT JOIN 是什么？]]

- 主问题：说明左表保留、右表匹配和无匹配时 NULL 扩展的结果语义。
- 场景追问：订单一对多连接后直接 LIMIT 20，为什么不能保证返回 20 个订单？
- 边界追问：子表条件放在 ON 与 WHERE 为什么可能改变是否保留无明细订单，count 应按什么口径写？

### ④ Redis

[[05-Redis/05-RedisTemplate与SpringCache#363. RedisTemplate 中的序列化是什么？|题 363：RedisTemplate 中的序列化是什么？]]

- 主问题：说明 Java 对象、Redis 字节表示、Key/Value serializer 与读写兼容的关系。
- 场景追问：缓存命中但读取数 MB JSON 仍慢时，怎样拆分服务端、网络、反序列化和 GC 证据？
- 边界追问：拆 Key、改 Hash 或压缩各自可能引入什么成本，为什么不能只看 Redis CPU？

### ⑤ Web、项目与工程

[[10-开发基础/05-部署基础#627. 什么是负载均衡？|题 627：什么是负载均衡？]]

- 主问题：说明把请求分配到多个服务实例的目的、常见选择维度和它与反向代理的关系。
- 场景追问：一个实例端口可连但数据库连接池耗尽时，Nginx 为什么仍可能把请求交给它？
- 边界追问：被动失败检测、超时、重试和幂等如何配合，为什么“两个实例 + Nginx”不等于完整高可用？

### ⑥ 算法与手写

[[12-算法/02-算法基础#638. 链表常见题型有哪些？|题 638：链表常见题型有哪些？]]

- 主问题：说明链表题为什么重点考节点引用、指针变化顺序和空指针边界。
- 手写：重新实现今天的 `hasCycle`，不能查看白天代码。
- 追问：快慢指针相遇的正确性依据是什么，判断有环与定位环入口为什么是两个不同任务？

## 评分规则

Java、Spring/MyBatis、MySQL、Redis、工程场景每题 10 分：核心结论 2 分、证据链 2 分、成立条件与边界 2 分、治理闭环 2 分、项目真实性 2 分。

算法题 10 分：函数契约 1 分、快慢指针不变量 3 分、空指针与节点身份边界 2 分、代码和返回值 2 分、复杂度 2 分。

总分 60 分：

- 48～60：Day 18 整体通过；单题不足 8 分仍记为 `不稳`，进入第二轮队列。
- 36～47：第一轮编号覆盖可以收口，但第二轮先集中修复本日和累计失败链。
- 0～35：不急于扩大场景范围，先按六条线各自的首个断点回补主干。

## 考后动作

- [ ] 对照原笔记纠错，记录六条线各自的首个错误断点。
- [ ] 将六道题分别标记为 `不会` / `不稳` / `通过`。
- [ ] 仅根据闭卷证据更新相关知识笔记的 `status`，不得因为“已覆盖 649 题”批量标记为 `能回答`。
- [ ] 工程线 625～627 通过后，记录第一轮 649 道编号题覆盖收口；若未通过，仍保留到第二轮首批修复。
- [ ] 汇总 Day 1～Day 18 的 `不会`、`不稳`、连续失败题与场景断点，按 P0、简历相关性和失败频次建立第二轮队列。
- [ ] 第二轮只做薄弱链、混合追问、真实项目表达和限时手写，不机械重刷全部题号。
- [ ] 把链表判环的首个错误和五个综合场景的未完成证据加入下一日回流。
- [ ] 原笔记疑点单独记录，核验可靠资料后再创建维护 PR。
