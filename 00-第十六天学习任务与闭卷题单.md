---
category: 学习任务
phase: 第一轮
day: 16
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第十六天学习任务与闭卷题单

> Day 16 按各线真实剩余量推进：Java 学习最后 6 道新题并回查 4 道并发主干题，工程线学习 10 道新题；Spring/MyBatis、MySQL、Redis 分别训练 PageHelper 错页、索引访问路径和事务后缓存失效；算法线手写最短子数组。

导航：[[00-总目录]] · [[00-第一轮学习队列]] · [[00-第十五天学习任务与闭卷题单]] · [[00-每日学习与闭卷考核模板]]

## 开始前

- [ ] 复制 [[00-每日学习与闭卷考核模板]]，以当天日期命名。
- [ ] 从 Day 15 记录中为每条学习线选择一个 `不会` 或 `不稳` 断点。
- [ ] 如果某条线连续失败题超过 2 道，本日先修复最靠前的核心断点，再决定是否执行综合场景。
- [ ] Day 15 尚未执行时，先完成 [[00-第十五天学习任务与闭卷题单]]。

## Day 15 回流槽

| 学习线 | 回流题号 | Day 15 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 / 回查或实战 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | 新学 [[09-Java并发/03-并发进阶]] 题 594～599；回查 [[09-Java并发/02-锁与线程池#506. `volatile` 是什么？|506]]、[[09-Java并发/02-锁与线程池#514. ThreadPoolExecutor 有哪些核心参数？|514]]、[[09-Java并发/02-锁与线程池#518. 拒绝策略是什么？|518]]、[[09-Java并发/02-锁与线程池#521. ConcurrentHashMap 和 HashMap 有什么区别？|521]] | 6 / 4 回查 | 串联 ReentrantLock、JMM 的可见性/原子性/有序性，再用四道主干题回查变量共享、线程池饱和和并发容器边界 |
| ② Spring 与数据访问 | 回查 [[03-MyBatis/01-MyBatis#203. Mapper 接口有什么作用？|203]]、[[03-MyBatis/01-MyBatis#204. Mapper 接口没有实现类为什么可以执行？|204]]、[[03-MyBatis/01-MyBatis#205. MyBatis Mapper 动态代理是什么？|205]]、[[03-MyBatis/01-MyBatis#220. 什么是 MyBatis 一级缓存？|220]]、[[03-MyBatis/01-MyBatis#221. 什么是 MyBatis 二级缓存？|221]]、[[03-MyBatis/01-MyBatis#222. 一级缓存和二级缓存有什么区别？|222]]、[[03-MyBatis/01-MyBatis#223. PageHelper 是什么？|223]]、[[03-MyBatis/01-MyBatis#224. PageHelper 大致如何实现分页？|224]]；完成错误分页目标排障 | 0 / 1 场景 | 从 Mapper 代理、Executor、插件链和线程分页上下文定位“辅助查询被分页、目标查询未分页”，补齐清理、排序和深分页边界 |
| ③ MySQL | 回查 [[04-MySQL/03-索引#265. 什么是联合索引？|265]]、[[04-MySQL/03-索引#266. 什么是最左匹配原则？|266]]、[[04-MySQL/03-索引#267. 联合索引 `(a,b,c)` 哪些查询可以使用索引？|267]]、[[04-MySQL/03-索引#268. 常见的索引失效场景有哪些？|268]]、[[04-MySQL/03-索引#269. `like '%xxx'` 为什么可能导致索引失效？|269]]、[[04-MySQL/03-索引#270. 对索引列使用函数为什么可能导致索引失效？|270]]、[[04-MySQL/03-索引#271. 隐式类型转换为什么可能导致索引失效？|271]]、[[04-MySQL/03-索引#272. 什么是 EXPLAIN？|272]]、[[04-MySQL/03-索引#273. EXPLAIN 中 `type` 表示什么？|273]]、[[04-MySQL/03-索引#274. EXPLAIN 中 `key` 表示什么？|274]]、[[04-MySQL/03-索引#275. EXPLAIN 中 `rows` 表示什么？|275]]、[[04-MySQL/03-索引#276. EXPLAIN 中 `Extra` 有哪些常见信息？|276]]；完成索引访问路径诊断 | 0 / 1 场景 | 把“索引失效”拆成不能构造范围、只利用部分前缀和成本选择不用三类证据，再用执行计划验证改写 |
| ④ Redis | 回查 [[05-Redis/03-Redis缓存#335. 数据库数据发生修改后缓存怎么办？|335]]、[[05-Redis/03-Redis缓存#336. 为什么通常删除缓存而不是更新缓存？|336]]、[[05-Redis/03-Redis缓存#337. 为什么 Redis 和 MySQL 可能出现数据不一致？|337]]、[[05-Redis/03-Redis缓存#338. 先更新数据库再删除缓存有什么问题？|338]]、[[05-Redis/03-Redis缓存#339. 先删除缓存再更新数据库有什么问题？|339]]、[[05-Redis/03-Redis缓存#340. 如何降低缓存和数据库不一致的概率？|340]]、[[05-Redis/03-Redis缓存#341. 什么是延迟双删？|341]]、[[05-Redis/05-RedisTemplate与SpringCache#368. `@CacheEvict` 有什么作用？|368]]、[[05-Redis/05-RedisTemplate与SpringCache#373. 项目中数据修改后如何清理对应缓存？|373]]；完成事务后失效演练 | 0 / 1 场景 | 画出数据库提交、缓存删除、并发旧读回填和删除失败的时间线，区分默认注解时机、可靠补偿和强一致边界 |
| ⑤ Web、项目与工程 | [[10-开发基础/04-Linux]] 题 582；[[11-中间件与分布式/01-中间件与分布式基础]] 题 606～614 | 10 / 0 | 收口 Linux 文件权限，再建立 MQ、Kafka/RabbitMQ、RPC/Dubbo、微服务与 Spring Cloud 的职责地图，不把技术名词等同于可靠性保证 |
| ⑥ 算法与手写 | 回流 [[12-算法/02-算法基础#643. 滑动窗口常见场景有哪些？|题 643]]；长度最小子数组 | 0 + 实战 | 对正整数数组维护和至少为 target 的最短窗口，说明何时更新答案、为何能持续收缩、正数前提和 `long` 累加边界 |

> Java 本日只有 6 道未覆盖编号题，因此加入 4 道并发主干回查，不把旧题计为新题。通过本日新题和回查后，Java 基础与运行时线完成第一轮编号节点收口。Day 16 的新题总量为 16。

### Spring/MyBatis 线综合场景

订单列表方法先调用 `PageHelper.startPage(pageNum, pageSize)`，随后新增了一次 MyBatis 权限辅助查询，最后才调用订单 Mapper。上线后辅助查询结果被截断，订单列表却没有按预期分页。

必须完成：

1. 根据 SQL 日志、Mapper 调用顺序和插件配置确认分页上下文被哪一次 MyBatis `select` 消费，不能只从 Controller 参数判断分页目标。
2. 说明 Mapper 接口由代理转换为 Executor 查询，PageInterceptor 在插件链读取当前线程分页上下文；`startPage` 通常影响紧随其后的符合条件的查询。
3. 把权限查询移到 `startPage` 之前，并让分页入口紧邻目标订单查询；也可使用把分页作用域和查询回调绑定的安全 API，具体以 PageHelper 版本为准。
4. 若条件分支、异常或提前返回导致分页后没有执行目标查询，应在明确作用域中保证清理，避免线程池复用时遗留上下文；不要假定跨线程异步查询会继承当前分页状态。
5. count 是否执行、复杂 SQL 如何改写、方言和插件顺序都需集成测试；分页结果必须有稳定 `ORDER BY`，大 offset 仍需评估 Seek/游标分页。
6. 项目是否真实使用 PageHelper、权限 Mapper 和具体分页 API 必须从源码核验；本场景不得包装成个人实际线上事故。

### MySQL 线综合场景

订单表存在联合索引 `(user_id, created_at)`，列表查询仍然较慢：

```sql
SELECT id, status, created_at
FROM orders
WHERE user_id = ?
  AND DATE(created_at) = ?
  AND remark LIKE '%urgent%'
ORDER BY created_at DESC
LIMIT 20;
```

必须完成：

1. 先保存实际 SQL、JDBC 参数类型、DDL、索引、统计信息和代表性数据量，再查看 `EXPLAIN`；不能凭“有索引但慢”直接宣布优化器错误。
2. `user_id = ?` 仍可能利用联合索引左前缀，因此整个索引不必然完全“失效”；`DATE(created_at)` 通常妨碍用原始时间列构造高效范围。
3. 把日期条件改写为原列上的左闭右开时间区间；若业务必须按同一表达式查询，再评估匹配的生成列或函数索引及写入成本。
4. 普通 B-tree 通常无法用前导 `%` 为任意位置包含搜索构造前缀范围；根据语言、分词和一致性要求评估全文索引或搜索系统，不能只继续叠加普通索引。
5. 保证 DTO、Mapper 参数与列类型一致，避免隐式转换改变语义或访问路径；用 `type`、`key`、`rows`、`Extra` 和受控环境中的 `EXPLAIN ANALYZE` 验证改写。
6. `key` 非空不等于高效，`Using filesort` 也不等于必然落盘；最终以实际扫描量、回表、排序和端到端耗时判断。

### Redis 线综合场景

菜品更新方法在数据库事务中修改数据，并通过 `@CacheEvict` 清理分类缓存。团队发现两个风险：缓存删除后数据库提交可能失败；数据库已经提交时 Redis 删除又可能因网络错误失败。

必须完成：

1. 画出方法调用、事务提交、缓存切面和 Redis 删除的真实顺序；默认“方法正常返回后淘汰”不必然等于“数据库事务已经提交后淘汰”，切面顺序和配置必须核验。
2. 若提交前删除，数据库最终回滚会造成无意义 miss；并发读还可能在提交前读取旧库值并在删除后回填旧缓存。
3. 若数据库已提交但删除失败，旧缓存会持续到下一次成功失效或 TTL 到期；删除操作应幂等、有限重试并有失败告警。
4. 可在事务提交后注册失效动作，或使用事务感知配置；高要求场景再评估 Outbox/CDC 等可靠事件。任何方案都不能把 MySQL 与 Redis 变成免费的原子事务。
5. 列出详情、列表、分页和聚合缓存的真实依赖，只失效受影响条目；`allEntries = true` 虽简单，却可能造成命中率骤降和集中回源。
6. 展示型菜品信息可按业务接受短暂陈旧，支付、库存与权限判断不能只依赖可能过期的缓存；项目采用注解还是手动删除仍以源码为准。

### 算法线编码要求

实现 `int minSubArrayLen(int target, int[] nums)`。`target > 0`，`nums` 非 `null` 且所有元素均为正整数；返回和大于等于 `target` 的最短连续子数组长度，不存在时返回 0，不修改输入数组。

至少验证：

```text
target = 7,  nums = [2, 3, 1, 2, 4, 3]          -> 2
target = 4,  nums = [1, 4, 4]                   -> 1
target = 11, nums = [1, 1, 1, 1, 1, 1, 1, 1]   -> 0
target = 15, nums = [1, 2, 3, 4, 5]             -> 5
target = 1,  nums = []                          -> 0
target = 2147483647, nums = [2147483647]        -> 1
```

完成代码后必须口述：窗口 `[left, right]` 和累加和分别表示什么、为何 `sum >= target` 时要先更新最短长度再反复收缩、正整数为什么保证右移增和与左移减和，以及时间复杂度 `O(n)`、额外空间 `O(1)`。累加和使用 `long`，避免多个大整数相加溢出。

### 每线完成条件

- [ ] Day 15 回流题已重新闭卷回答，并记录修复后的结果。
- [ ] Java 的 6 道新题和 4 道回查均有闭卷结果，工程线 10 道新题能形成职责地图。
- [ ] Spring/MyBatis 场景能从调用顺序、Mapper 代理、插件上下文和清理边界定位错页。
- [ ] MySQL 场景区分部分使用索引、不能构造范围和成本选择，并用计划与实测验证。
- [ ] Redis 场景包含事务/切面时序、旧值回填、删除失败、依赖范围和补偿边界。
- [ ] 最短子数组代码通过全部用例，并能解释滑窗单调性、更新时机和正数前提。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-每日学习与闭卷考核模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[09-Java并发/03-并发进阶#596. 什么是 Java 内存模型 JMM？|题 596：什么是 Java 内存模型 JMM？]]

- 主问题：说明 JMM 规范的对象、共享变量读写、可见性和有序性，而不是把它等同于 JVM 内存区域图。
- 追问 1：`volatile`、锁和线程启动/终止规则如何建立 happens-before，happens-before 是否等于物理执行顺序？
- 追问 2：原子性、可见性、有序性为什么要分别分析，单线程正确为何不能推出并发正确？

### ② Spring 与数据访问

[[03-MyBatis/01-MyBatis#223. PageHelper 是什么？|题 223：PageHelper 是什么？]]

- 主问题：说明分页入口、线程上下文、PageInterceptor、方言和结果包装的基本职责。
- 场景追问：`startPage` 后先执行权限 Mapper，为什么可能分页错目标，如何重排调用并验证？
- 边界追问：条件分支没有执行查询、异步切线程、复杂 count 和深分页分别有什么风险？

### ③ MySQL

[[04-MySQL/03-索引#268. 常见的索引失效场景有哪些？|题 268：常见的索引失效场景有哪些？]]

- 主问题：把口语“失效”拆成不能构造搜索范围、只用部分索引和成本选择不用。
- 场景追问：联合索引 `(user_id, created_at)` 遇到 `user_id = ? AND DATE(created_at) = ?` 时，哪一部分仍可能使用，日期条件如何改写？
- 边界追问：为什么 `OR`、`IS NULL`、`!=` 和低基数列都不能脱离执行计划直接判定不走索引？

### ④ Redis

[[05-Redis/05-RedisTemplate与SpringCache#373. 项目中数据修改后如何清理对应缓存？|题 373：数据修改后如何清理对应缓存？]]

- 主问题：说明事实来源、依赖 Key、提交后失效、TTL 和失败补偿的完整链路。
- 场景追问：为什么默认 `@CacheEvict` 的方法后置时机不必然等于数据库事务已提交，怎样核验或调整？
- 边界追问：删除成功后仍可能被并发慢读回填旧值，为什么延迟双删也只能概率性缓解？

### ⑤ Web、项目与工程

[[11-中间件与分布式/01-中间件与分布式基础#607. 为什么需要消息队列？|题 607：为什么需要消息队列？]]

- 主问题：从异步、削峰和解耦说明 MQ 的收益，并指出可靠投递、顺序、重复和积压成本。
- 追问 1：MQ 为什么不能自动保证不丢不重，生产者、Broker 和消费者各要处理什么？
- 追问 2：什么场景不该为了“解耦”强行引入 MQ，同步结果和强一致要求如何影响选择？

### ⑥ 算法与手写

[[12-算法/02-算法基础#643. 滑动窗口常见场景有哪些？|题 643：滑动窗口常见场景有哪些？]]

- 主问题：区分固定窗口与可变窗口，以及最长合法窗口和最短满足窗口的更新时机。
- 手写：重新实现今天的长度最小子数组，不能查看白天代码。
- 追问：为什么允许负数后这套双指针滑窗可能失效，应该考虑哪类替代思路？

## 评分规则

Java、工程题每题 10 分：核心结论 2 分、原理链路 2 分、成立条件与边界 2 分、两层追问 2 分、场景或工程影响 2 分。

Spring/MyBatis、MySQL、Redis 场景题每题 10 分：证据链 2 分、故障时序 2 分、成立条件 2 分、治理闭环 2 分、项目真实性 2 分。

算法题 10 分：函数契约 1 分、窗口不变量 3 分、正数与溢出边界 2 分、代码和返回值 2 分、复杂度 2 分。

总分 60 分：

- 48～60：整体通过；单题不足 8 分仍记为 `不稳`。
- 36～47：Day 17 保留对应回流槽，并减少失败学习线的新题量或场景复杂度。
- 0～35：暂停薄弱线扩题，优先修复重复失败的知识链。

## 考后动作

- [ ] 对照原笔记纠错，记录首个错误断点。
- [ ] 将六道题分别标记为 `不会` / `不稳` / `通过`。
- [ ] 仅根据闭卷证据更新相关知识笔记的 `status`。
- [ ] Java 只有在 6 道新题和 4 道回查均通过后，才标记第一轮编号节点收口。
- [ ] 三个综合场景未通过时，下一日继续修复失败链，不用新的场景掩盖问题。
- [ ] 把连续失败题和滑动窗口的首个错误加入 Day 17 回流。
- [ ] 原笔记疑点单独记录，核验可靠资料后再创建维护 PR。
