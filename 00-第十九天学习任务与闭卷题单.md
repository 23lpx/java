---
category: 学习任务
phase: 第二轮
day: 19
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第十九天学习任务与闭卷题单

> Day 19 是第二轮入口诊断，不预设你的薄弱项，也不新增编号题。前五条线混合回查 48 个高频主干节点并完成综合场景，算法线回查自定义排序并手写区间合并；真实结果决定之后的第二轮队列。

导航：[[00-总目录]] · [[00-第二轮学习队列]] · [[00-第十八天学习任务与闭卷题单]] · [[00-第二轮诊断与复习模板]]

## 开始前

- [ ] 复制 [[00-第二轮诊断与复习模板]]，以当天日期命名。
- [ ] 从 Day 18 已填写记录中为每条学习线选择一个 `不会`、`不稳` 或“通过但慢”断点；没有记录或没有断点时使用本页默认诊断题。
- [ ] 先闭卷回答，再打开知识节点校准；不要先重读 48 道题后宣称通过。
- [ ] Day 18 尚未执行时，可以先完成 [[00-第十八天学习任务与闭卷题单]]，也可以直接执行本页建立第二轮基线，但必须如实记录证据来源。

## Day 18 回流槽

| 学习线 | 回流题号 | Day 18 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过但慢 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过但慢 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过但慢 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过但慢 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过但慢 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过但慢 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 / 诊断或实战 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | 回查 [[01-Java基础/05-HashMap#61. HashMap 的底层数据结构是什么？|61]]、[[01-Java基础/05-HashMap#63. HashMap 的 `put()` 流程是什么？|63]]、[[01-Java基础/05-HashMap#65. 什么是 Hash 冲突？|65]]、[[01-Java基础/05-HashMap#67. HashMap 为什么同时需要 `hashCode()` 和 `equals()`？|67]]、[[01-Java基础/05-HashMap#71. HashMap 什么时候扩容？|71]]、[[01-Java基础/05-HashMap#75. HashMap 为什么线程不安全？|75]]、[[01-Java基础/05-HashMap#78. HashMap 和 ConcurrentHashMap 有什么区别？|78]]、[[09-Java并发/02-锁与线程池#522. Java 8 ConcurrentHashMap 大致如何保证线程安全？|522]]；完成单例计数丢失诊断 | 0 / 1 场景 | 区分容器线程安全与复合业务操作原子性，给出单 JVM 单 Key 原子更新方案及跨实例边界 |
| ② Spring 与数据访问 | 回查 [[02-Spring/05-Filter与Interceptor#154. 为什么 JWT 登录校验适合放在拦截器？|154]]、[[02-Spring/05-Filter与Interceptor#157. 为什么管理端和用户端可以使用两个拦截器？|157]]、[[02-Spring/06-AOP#160. 哪些场景适合使用 AOP？|160]]、[[02-Spring/06-AOP#167. Spring AOP 底层大致如何实现？|167]]、[[02-Spring/06-AOP#171. 为什么 Spring 事务可以通过 AOP 实现？|171]]、[[02-Spring/08-Spring事务#190. Spring 声明式事务大致是怎么实现的？|190]]、[[02-Spring/08-Spring事务#195. 把异常 catch 住为什么可能导致事务不回滚？|195]]、[[02-Spring/08-Spring事务#196. 同一个类内部调用另一个事务方法为什么可能失效？|196]]、[[02-Spring/08-Spring事务#200. 项目中为什么主表和子表写入需要放在同一个事务中？|200]]、[[03-MyBatis/01-MyBatis#216. 如何通过 `<foreach>` 实现批量插入？|216]]；完成请求到持久化链路复述 | 0 / 1 场景 | 从 JWT 拦截器串到 ThreadLocal、事务代理、自动填充、主键回填和明细批量插入，并标出代理与异常边界 |
| ③ MySQL | 回查 [[04-MySQL/03-索引#249. 什么是数据库索引？|249]]、[[04-MySQL/03-索引#251. 索引有什么缺点？|251]]、[[04-MySQL/03-索引#258. 什么是聚簇索引？|258]]、[[04-MySQL/03-索引#260. 什么是二级索引？|260]]、[[04-MySQL/03-索引#262. 什么是回表？|262]]、[[04-MySQL/03-索引#263. 什么是覆盖索引？|263]]、[[04-MySQL/03-索引#265. 什么是联合索引？|265]]、[[04-MySQL/03-索引#267. 联合索引 `(a,b,c)` 哪些查询可以使用索引？|267]]、[[04-MySQL/03-索引#272. 什么是 EXPLAIN？|272]]、[[04-MySQL/03-索引#276. EXPLAIN 中 `Extra` 有哪些常见信息？|276]]；完成订单列表索引诊断 | 0 / 1 场景 | 从查询契约提出候选联合索引，用数据分布和 EXPLAIN 验证过滤、排序、回表与深分页代价 |
| ④ Redis | 回查 [[05-Redis/03-Redis缓存#330. 什么是 Cache Aside 模式？|330]]、[[05-Redis/03-Redis缓存#331. 查询 Redis + MySQL 的基本流程是什么？|331]]、[[05-Redis/03-Redis缓存#335. 数据库数据发生修改后缓存怎么办？|335]]、[[05-Redis/03-Redis缓存#336. 为什么通常删除缓存而不是更新缓存？|336]]、[[05-Redis/03-Redis缓存#337. 为什么 Redis 和 MySQL 可能出现数据不一致？|337]]、[[05-Redis/03-Redis缓存#338. 先更新数据库再删除缓存有什么问题？|338]]、[[05-Redis/03-Redis缓存#339. 先删除缓存再更新数据库有什么问题？|339]]、[[05-Redis/03-Redis缓存#340. 如何降低缓存和数据库不一致的概率？|340]]、[[05-Redis/05-RedisTemplate与SpringCache#368. `@CacheEvict` 有什么作用？|368]]、[[05-Redis/05-RedisTemplate与SpringCache#373. 项目中数据修改后如何清理对应缓存？|373]]；完成菜品缓存失效链路诊断 | 0 / 1 场景 | 以数据库为事实源，覆盖事务时序、并发旧值回填、删除失败重试、TTL 和关联缓存范围 |
| ⑤ Web、项目与工程 | 回查 [[07-项目/03-第三方支付#455. 用户扫码支付后的完整流程是什么？|455]]、[[07-项目/03-第三方支付#456. 什么是支付宝异步通知？|456]]、[[07-项目/03-第三方支付#457. 为什么不能只相信前端返回的支付成功？|457]]、[[07-项目/03-第三方支付#458. 支付回调验签通过后还要校验什么？|458]]、[[07-项目/03-第三方支付#460. 为什么支付回调必须验签？|460]]、[[07-项目/03-第三方支付#462. `out_trade_no` 是什么？|462]]、[[07-项目/03-第三方支付#464. 什么是支付回调幂等？|464]]、[[07-项目/03-第三方支付#466. 支付宝为什么可能重复发送回调？|466]]、[[07-项目/03-第三方支付#468. 为什么支付状态迁移必须是原子的？|468]]、[[07-项目/03-第三方支付#471. 支付成功但本地订单状态更新失败怎么办？|471]]；完成重复与乱序回调诊断 | 0 / 1 场景 | 从验签、业务字段校验、条件状态迁移推进到事务、重复通知与对账，并区分真实沙箱链路和生产增强 |
| ⑥ 算法与手写 | 回查 [[12-算法/01-Java算法常用API#636. Java 如何自定义排序？|题 636]]；区间合并 | 0 + 实战 | 不修改输入，排序副本后维护已处理前缀不变量，覆盖闭区间端点、比较器溢出和 `O(n log n)` 复杂度 |

> Day 19 新增编号题为 0。前五条线诊断 48 个既有主干节点，算法线回查题 636 并完成一道实际编码；“回查过”不等于“通过”，只按闭卷和代码结果建立第二轮证据。

### Java 线综合场景

Spring 单例 `StatsService` 使用共享 `HashMap<Long, Integer>` 记录用户请求次数。多个请求线程执行 `containsKey → get → put` 后，统计值偶发变小；有人建议只把类型替换成 `ConcurrentHashMap`。

必须完成：

1. 说明普通 HashMap 的结构和修改都不提供并发安全保证，先停止用共享 HashMap 承担无同步写入。
2. 即使改成 ConcurrentHashMap，分开的 `get` 与 `put` 仍是读—改—写复合操作，多个单方法各自线程安全不等于整个业务步骤原子。
3. 单 JVM、单 Key 计数可使用 `merge(key, 1, Integer::sum)` 或短小的 `compute` 原子完成；不要在重映射函数中执行远程调用、阻塞 I/O 或修改其他映射。
4. 若计数器对象本身可变，还要说明对象内部并发契约；把并发容器的 Value 换成普通可变对象不会自动让对象字段线程安全。
5. 通过固定线程数、启动屏障、已知总增量和多轮压力测试验证丢失更新；一次结果正确不能证明线程安全。
6. ConcurrentHashMap 只协调当前 JVM 内的映射。多实例共享统计、持久计数或精确结算必须使用具备相应原子语义的外部存储或协调方案。本场景是通用演练，不表述为简历事故。

### Spring/MyBatis 线综合场景

用户请求通过 JWT 拦截器后进入 Controller。Service 的外部代理方法带 `@Transactional`，内部使用 AOP 自动填充公共字段，先插入订单主表并回填主键，再通过 MyBatis `<foreach>` 批量插入明细。运行时异常发生在明细批量写入阶段。

必须完成：

1. 画出双拦截器匹配、JWT 校验、ThreadLocal 写入、Controller、Service 代理、事务拦截器、Mapper 和请求结束清理的调用链；不同端拦截器的路径和令牌规则不要混用。
2. 说明声明式事务依靠 AOP 代理在方法调用周围开启、提交或回滚事务；只有调用经过代理且数据库操作实际参与同一事务，注解才形成预期边界。
3. 主表插入成功并回填主键后再组装明细；主键回填只是后续关联所需数据，不表示事务已经提交。
4. 若明细批量写入抛出的运行时异常越过事务代理边界，默认规则下事务可以回滚；若代码捕获后正常返回、错误地在同类自调用事务方法或跨到新线程，必须重新分析事务是否生效。
5. AOP 自动填充适合横切关注点，但要说明切点、参数约定和反射失败边界；它不替代数据库默认值和业务校验。
6. 无论正常还是异常路径，ThreadLocal 都要由明确所有者清理。JWT 双拦截器、ThreadLocal、自动填充、主子表事务和批量插入属于简历已确认链路；本次“明细运行时失败”是诊断假设。

### MySQL 线综合场景

订单列表查询为：

```sql
SELECT id, user_id, status, order_time, amount
FROM orders
WHERE user_id = ? AND status = ?
ORDER BY order_time DESC, id DESC
LIMIT 20;
```

数据量增长后出现排序和翻页延迟，团队准备直接创建一个“覆盖所有返回列”的宽索引。

必须完成：

1. 先写查询契约：`user_id`、`status` 是等值过滤，`order_time DESC, id DESC` 提供稳定顺序；候选索引可以从 `(user_id, status, order_time, id)` 开始评估，而不是只背最左前缀口诀。
2. 两个排序列方向一致时，MySQL 可能通过索引顺序或反向扫描满足排序；最终是否采用索引仍受数据分布、选择性、返回行数和成本估算影响。
3. 二级联合索引叶子包含主键定位信息，但查询其他非索引列仍可能回表。为覆盖少量高频查询扩展索引要权衡写放大、存储、缓存占用和维护成本。
4. 用生产代表性数据执行 EXPLAIN，并结合实际执行指标观察访问类型、预估/实际行数、key、key_len、Extra、排序和回表；不能因为显示某个 key 就断言方案最优。
5. 深分页不能仅靠索引解决；使用 `(order_time, id)` 作为游标时要保持与排序方向一致，并处理过滤条件变化与并发新增的语义。
6. 若 `status` 变为可选条件或查询经常跨状态，索引顺序和候选方案需要重新评估。索引必须服务真实查询集合，不能为单条 SQL 无限堆列。本场景是通用订单查询演练。

### Redis 线综合场景

项目修改菜品后写入数据库，并清理菜品相关缓存。并发请求可能在更新窗口读到旧值并重新回填；缓存删除也可能失败，团队希望把“一次删除缓存”直接描述为强一致。

必须完成：

1. 以数据库为事实源，画出缓存命中、未命中查库与回填，以及写请求更新数据库和失效缓存的 Cache Aside 基本链路。
2. 说明“更新数据库后删除缓存”仍可能遇到删除失败、事务提交时序和并发旧读回填；任何时序口号都不能自动给出强一致。
3. 写路径要明确事务成功与缓存失效的关系。`@CacheEvict` 默认在方法成功完成后触发，但方法返回与数据库事务提交的实际先后还受代理顺序和事务配置影响，必须用项目配置或测试确认。
4. 对删除失败建立可观测重试或可靠补偿，并保留合理 TTL 作为兜底；重试要幂等且有上限，TTL 只限制旧值存活时间，不消除不一致窗口。
5. 明确缓存依赖范围：修改菜品可能影响按 ID 详情、按分类列表或套餐聚合，但实际 Key、注解和清理范围必须以代码为证，不能从简历推断具体命名。
6. 用并发时序测试覆盖旧读、事务回滚、删除失败和回填竞争，并观察数据库值、缓存值和恢复时间。项目确认使用 RedisTemplate/Spring Cache 与菜品/套餐缓存；具体事务感知配置仍是核验项。

### 工程线综合场景

支付宝沙箱支付链路收到同一 `out_trade_no` 的重复异步通知，通知到达顺序可能与页面跳转不同；某次处理还在本地订单状态更新前后出现异常。

必须完成：

1. 服务器端回调先按支付平台协议验签，再核对商户身份、订单号、金额、币种或状态等业务字段；验签通过只证明消息来源和完整性，不代表业务一定可接受。
2. 以本地订单和合法前置状态为依据执行条件状态迁移，例如仅允许 `未支付 → 已支付`；同一通知重放时不重复产生库存、积分或消息副作用。
3. 订单状态和必须同时落库的本地副作用放在明确事务中；只有处理结果已经可靠落地后才返回成功语义，异常路径按平台协议允许再次通知。
4. 回调与前端同步跳转职责不同，不能用前端“支付成功”直接改本地订单，也不能假设两者到达顺序固定。
5. 对“平台已支付但本地仍未支付”保留重复通知、主动查询、定时对账和人工补偿等最终一致性手段；这些属于生产增强，未确认已在简历项目中全部实现。
6. 支付宝沙箱支付闭环属于简历真实链路；具体签名算法、字段名、通知重试参数和对账任务实现仍以项目代码与平台文档为准，不在闭卷时编造。

### 算法线编码要求

实现 `int[][] merge(int[][] intervals)`。

函数契约：

- `intervals` 非 `null`；每个元素长度为 2，且 `start <= end`。
- 区间为闭区间，端点相接视为重叠，例如 `[1,4]` 与 `[4,5]` 合并为 `[1,5]`。
- 不得修改调用方传入的二维数组或其中的行；返回新分配、按起点升序且互不重叠的区间。
- 空数组返回空数组；输入不满足契约时本题不要求兜底修复，但回答中要明确此前置条件。

至少验证：

```text
[]                                      -> []
[[1,3]]                                 -> [[1,3]]
[[1,3],[2,6],[8,10],[15,18]]            -> [[1,6],[8,10],[15,18]]
[[1,4],[4,5]]                            -> [[1,5]]
[[5,7],[1,2],[2,4]]                      -> [[1,4],[5,7]]
[[-3,-1],[-2,0]]                         -> [[-3,0]]
```

完成代码后必须口述：先复制输入再按起点排序；比较器使用 `Integer.compare`，避免用减法造成整数溢出；循环开始时，结果已按起点排序、互不重叠并覆盖已处理前缀。若当前起点 `<=` 最后区间终点，则把终点扩展为两者最大值，否则追加新拷贝。排序后扫描得到时间复杂度 `O(n log n)`；计入输入副本和输出，额外空间 `O(n)`。若契约改为开区间，端点相接的判断会变化。

### 每线完成条件

- [ ] Day 18 有真实断点时已先回流；没有记录时已明确使用默认诊断，不伪造历史结果。
- [ ] Java 场景能区分普通 HashMap、ConcurrentHashMap 单方法保证、复合操作与跨实例边界。
- [ ] Spring/MyBatis 能完整复述请求—代理—事务—批量持久化—清理链路，并标出异常、自调用和线程边界。
- [ ] MySQL 能提出候选索引并用数据与 EXPLAIN 验证，说明排序、覆盖、回表和深分页权衡。
- [ ] Redis 能解释事务与失效时序、并发旧值回填、重试/TTL 兜底和关联缓存核验项。
- [ ] 工程线能守住支付回调验签、业务校验、原子状态迁移和项目真实性边界；区间合并通过全部用例并完成正确性说明。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-第二轮诊断与复习模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[01-Java基础/05-HashMap#63. HashMap 的 `put()` 流程是什么？|题 63：HashMap 的 put 流程是什么？]]

- 主问题：从计算桶位置、处理空桶或冲突、更新旧值到插入后扩容，复述 JDK 8 HashMap 的主流程。
- 场景追问：为什么把共享 HashMap 换成 ConcurrentHashMap 仍不能保证 `get → put` 计数原子？
- 边界追问：`compute` / `merge` 适合解决哪个范围的问题，为什么重映射函数要短小，跨实例怎么办？

### ② Spring 与数据访问

[[02-Spring/08-Spring事务#190. Spring 声明式事务大致是怎么实现的？|题 190：Spring 声明式事务大致是怎么实现的？]]

- 主问题：说明事务元数据、AOP 代理、事务拦截器和事务管理器如何围绕一次方法调用工作。
- 场景追问：主表插入、主键回填和明细批量写入在哪个边界内才能一起回滚？
- 边界追问：异常被 catch、同类自调用和新线程为什么会改变事务判断？

### ③ MySQL

[[04-MySQL/03-索引#267. 联合索引 `(a,b,c)` 哪些查询可以使用索引？|题 267：联合索引可以怎样使用？]]

- 主问题：解释联合索引的有序前缀，区分“能利用部分索引”“能用于过滤/排序”和“完全不能读索引”。
- 场景追问：订单查询为什么可以从 `(user_id, status, order_time, id)` 开始评估，如何验证排序是否被索引满足？
- 边界追问：status 可选、选择性变化、覆盖索引变宽和深分页分别会带来什么重新评估？

### ④ Redis

[[05-Redis/03-Redis缓存#340. 如何降低缓存和数据库不一致的概率？|题 340：如何降低缓存和数据库不一致的概率？]]

- 主问题：从事实源、写后失效、TTL、失败重试和并发回填说明一致性治理，不使用“绝对一致”表述。
- 场景追问：菜品更新成功但缓存删除失败，怎样恢复并验证旧值存活时间？
- 边界追问：`@CacheEvict` 与事务提交的顺序为什么要看代理和配置，关联缓存范围如何确认？

### ⑤ Web、项目与工程

[[07-项目/03-第三方支付#468. 为什么支付状态迁移必须是原子的？|题 468：为什么支付状态迁移必须是原子的？]]

- 主问题：说明合法前置状态、条件更新、并发回调和副作用幂等的关系。
- 场景追问：重复或乱序通知到达时，如何避免二次处理并正确返回平台响应？
- 边界追问：哪些是简历确认的沙箱链路，哪些是主动查询、对账等生产增强？

### ⑥ 算法与手写

[[12-算法/01-Java算法常用API#636. Java 如何自定义排序？|题 636：Java 如何自定义排序？]]

- 主问题：说明 Comparator 的比较契约，以及为什么整数比较优先使用 `Integer.compare` 而不是相减。
- 手写：重新实现今天的 `merge`，不能查看白天代码，并运行六组测试。
- 追问：扫描不变量如何证明结果覆盖已处理前缀，闭区间端点相接为什么要合并，不修改输入如何保证？

## 评分与证据分级

Java、Spring/MyBatis、MySQL、Redis、工程场景每题 10 分：核心结论 2 分、原理链路 2 分、成立条件与边界 2 分、场景闭环 2 分、限时与真实性 2 分。

算法题 10 分：函数契约 1 分、排序与扫描不变量 3 分、输入不可变与边界测试 2 分、代码正确性 2 分、复杂度 2 分。

总分 60 分；总分只反映本次诊断概况，第二轮队列按每一线的首个断点建立：

- 单题 0～5：A（不会），下一次优先修复核心结论或代码正确性。
- 单题 6～7：B（不稳），补齐原理、边界、场景或追问后换问法复测。
- 单题 8～10 但超时或依赖提示：C（通过但慢），进入间隔抽查。
- 单题 8～10 且限时独立完成：本次通过；保留后续随机复测，不等同于整篇笔记自动 `能回答`。

## 考后动作

- [ ] 将六条线的题号、首个断点、A/B/C 等级和证据日期写入 [[00-第二轮学习队列#六线证据队列]]。
- [ ] 每条失败链只确定下一个最小修复动作与复测日期，不创建脱离证据的固定长日历。
- [ ] 仅根据闭卷覆盖范围更新相关知识笔记的 `status`；单题通过不批量修改整篇状态。
- [ ] 把真实项目、待核验细节和通用工程建议分别记录，面试表达不越过证据边界。
- [ ] 区间合并记录首个失败用例、修复和复杂度；通过后仍安排一次不看旧代码的间隔手写。
- [ ] 原笔记出现疑点时单独记录，核对官方资料后再创建维护 PR，不在学习记录中直接改写知识结论。
