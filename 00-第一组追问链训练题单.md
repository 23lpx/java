---
category: 追问训练题单
phase: 第二轮
group: 1
status: 待开始
tags:
  - Java后端
  - 求职
  - 追问链
---

# 第一组追问链训练题单

> 本组不新增编号题，也不预设薄弱点。六条线各选一条母题，要求先给 30 秒结论，再承接到 L4 证据与取舍；建议分两次完成，总计 90～120 分钟。

导航：[[00-总目录]] · [[00-P0母题与追问链地图]] · [[00-追问链训练模板]] · [[00-第一组追问链答案核对卡]] · [[00-第二轮学习队列]]

## 开始前

- [ ] 复制 [[00-追问链训练模板]]；自我训练时打开录音和计时。
- [ ] 关闭知识笔记，先完成 30 秒首答；答完 L0～L4 后再展开检查点。
- [ ] 六条线均保留首答原貌后，才打开 [[00-第一组追问链答案核对卡]]；答案卡不是背诵稿。
- [ ] 每一层只记录首个错误或停顿，不用后续补答覆盖现场证据。
- [ ] 真实项目问题对照 [[00-简历证据与追问地图]]，V0 细节明确说待核验。
- [ ] 某条线当天精力不足时可以拆到下一次，但不填写未执行成绩。

## ① Java：J4 线程池过载

关联节点：[[09-Java并发/02-锁与线程池#511. 什么是线程池？|511]]、[[09-Java并发/02-锁与线程池#514. ThreadPoolExecutor 有哪些核心参数？|514]]、[[09-Java并发/02-锁与线程池#517. workQueue 是什么？|517]]、[[09-Java并发/02-锁与线程池#518. 拒绝策略是什么？|518]]、[[09-Java并发/02-锁与线程池#519. 常见拒绝策略有哪些？|519]]、[[09-Java并发/02-锁与线程池#520. Tomcat 为什么使用线程池处理请求？|520]]。

- L0：线程池解决什么问题，为什么不能只说“复用线程所以更快”？
- L1：`corePoolSize=8`、`maximumPoolSize=32`、有界队列 100、AbortPolicy 时，一个新任务按什么顺序被接收、入队、扩容或拒绝？
- L2：为什么参数没有通用最优值？无界队列、CallerRunsPolicy 和盲目调大 maximumPoolSize 各有什么边界？
- L3：接口 P99 升高、队列持续满、拒绝增加，客户端又自动重试，怎样判断是慢依赖、容量不足还是任务本身阻塞？
- L4：你会采集哪些指标验证调整有效，如何避免重试和降级再次放大流量？

> [!check]- 核对点（答题前不要展开）
> - 典型 ThreadPoolExecutor 流程是核心线程接收、队列、非核心扩容、拒绝；还要说明核心线程是否预启动等配置边界。
> - 线程池限制资源并平滑突发，但队列只是延迟压力，不能消除慢依赖或无限流量。
> - 验证至少关联活跃线程、池大小、队列深度/等待时间、拒绝、任务耗时、依赖耗时、超时和重试率。

## ② Spring：S1 单例 Bean 与请求状态

关联节点：[[02-Spring/01-Spring基础#99. 什么是 IoC？|99]]、[[02-Spring/01-Spring基础#101. 什么是 DI？|101]]、[[02-Spring/01-Spring基础#112. Spring Bean 默认是单例吗？|112]]、[[02-Spring/01-Spring基础#113. 单例 Bean 一定线程安全吗？|113]]、[[02-Spring/01-Spring基础#114. Spring Bean 有哪些常见作用域？|114]]、[[02-Spring/01-Spring基础#115. Spring Bean 生命周期大致是什么？|115]]、[[06-Web/02-ThreadLocal#402. 为什么项目中用 ThreadLocal 保存 userId / empId？|402]]、[[06-Web/02-ThreadLocal#407. 为什么 ThreadLocal 使用完需要 `remove()`？|407]]。

- L0：IoC、DI 和 Bean 是什么关系，默认单例表示什么？
- L1：为什么单例 Bean 本身不等于线程安全？无状态 Service 与保存可变字段的 Service 有什么区别？
- L2：把请求 userId 存到单例字段、prototype Bean 或 ThreadLocal，三种做法分别有什么问题和适用边界？
- L3：压力测试出现用户身份串号，如何从请求线程、Bean 字段、ThreadLocal 建立/清理和异步任务定位？
- L4：怎样用并发测试证明修复有效，为什么“跑一次没复现”不是线程安全证据？

> [!check]- 核对点（答题前不要展开）
> - Spring 默认 singleton 是容器中共享同一 Bean 实例，不提供对实例可变字段的自动同步。
> - 优先保持业务 Bean 无状态；ThreadLocal 只适合同步线程上下文并需要所有退出路径清理，prototype 也不是通用并发修复。
> - 项目确认 JWT、双拦截器和 ThreadLocal 链路，但没有证据证明真实发生过串号事故。

## ③ MySQL：M3 MVCC 与并发扣减

关联节点：[[04-MySQL/04-事务#277. MySQL 有哪些事务隔离级别？|277]]、[[04-MySQL/04-事务#281. READ COMMITTED 和 REPEATABLE READ 有什么区别？|281]]、[[04-MySQL/04-事务#283. 什么是 MVCC？|283]]、[[04-MySQL/04-事务#286. 什么是 Read View？|286]]、[[04-MySQL/05-锁#293. 为什么数据库需要锁？|293]]、[[04-MySQL/05-锁#296. 什么是行锁？|296]]、[[04-MySQL/05-锁#300. 什么是乐观锁？|300]]、[[04-MySQL/05-锁#301. 乐观锁通常如何实现？|301]]。

- L0：MVCC 主要解决什么问题，它和锁是什么关系？
- L1：RC 与 RR 的 Read View 时机有什么差别，普通 SELECT 与锁定读看到的版本为什么可能不同？
- L2：为什么两个事务都先查到库存 1 时，MVCC 不会自动让“先查再减”整体原子？
- L3：比较条件 UPDATE、版本号和 `SELECT ... FOR UPDATE` 三种扣减方案，怎样判断成功并保持订单写入一致？
- L4：怎样验证没有超卖、没有长时间锁等待，并处理死锁或有限重试的幂等？

> [!check]- 核对点（答题前不要展开）
> - MVCC 提供一致性读并减少读写冲突，不把跨语句业务步骤自动组合为原子操作。
> - 条件更新依靠受影响行数判定；锁定读需要同一事务、正确索引和完整的失败/回滚处理。
> - 提高隔离级别不能替代业务条件、事务边界、锁范围分析和重试幂等。

## ④ Redis：R4 持久化与高可用

关联节点：[[05-Redis/06-持久化#374. Redis 为什么需要持久化？|374]]、[[05-Redis/06-持久化#375. RDB 是什么？|375]]、[[05-Redis/06-持久化#376. AOF 是什么？|376]]、[[05-Redis/06-持久化#377. RDB 和 AOF 有什么区别？|377]]、[[05-Redis/06-持久化#380. Redis 重启后如何恢复数据？|380]]、[[05-Redis/07-Redis进阶#583. Redis 主从复制是什么？|583]]、[[05-Redis/07-Redis进阶#585. Redis Sentinel 是什么？|585]]、[[05-Redis/07-Redis进阶#586. Redis Cluster 是什么？|586]]。

- L0：RDB、AOF、主从、Sentinel 和 Cluster 分别解决什么问题？
- L1：Redis 重启恢复依赖什么，AOF 刷盘策略和 RDB 快照窗口怎样影响数据丢失上限？
- L2：为什么“开启 AOF”不等于零丢失，“有副本”不等于备份，“有 Sentinel”不等于服务绝不中断？
- L3：单节点进程崩溃、整机故障、误删除 Key 和主从网络分区分别需要哪类机制？
- L4：如何用故障演练验证 RPO、RTO、切换和客户端重连，而不是只检查配置存在？

> [!check]- 核对点（答题前不要展开）
> - 持久化面向进程重启后的数据恢复；复制/故障转移面向可用性；备份还要隔离误操作和共同故障域。
> - 异步复制与不同刷盘策略都可能存在数据窗口，实际上限依赖配置、负载和故障时机。
> - 本简历只确认业务缓存使用，没有确认 Redis 高可用架构；回答应标为通用工程能力。

## ⑤ Web/项目：W4 支付回调幂等

关联节点：[[06-Web/03-HTTP#419. 什么是 HTTP 方法幂等？|419]]、[[06-Web/03-HTTP#420. 哪些 HTTP 方法通常具有幂等性？|420]]、[[07-项目/03-第三方支付#456. 什么是支付宝异步通知？|456]]、[[07-项目/03-第三方支付#458. 支付回调验签通过后还要校验什么？|458]]、[[07-项目/03-第三方支付#464. 什么是支付回调幂等？|464]]、[[07-项目/03-第三方支付#467. 项目中如何保证支付回调幂等？|467]]、[[07-项目/03-第三方支付#468. 为什么支付状态迁移必须是原子的？|468]]、[[07-项目/03-第三方支付#471. 支付成功但本地订单状态更新失败怎么办？|471]]。

- L0：HTTP 方法幂等与支付业务幂等有什么区别？
- L1：异步回调为什么可能重复，验签、业务字段校验、状态校验和原子状态迁移分别承担什么职责？
- L2：为什么“先查是否已支付，再普通 UPDATE”仍可能并发重复处理？返回平台成功的时机有什么边界？
- L3：同一订单收到重复、乱序通知，且一次本地更新失败，怎样组织事务、副作用和恢复链路？
- L4：简历真实实现到哪一步，主动查询、定时对账、消息补偿和生产密钥治理哪些仍是通用增强？

> [!check]- 核对点（答题前不要展开）
> - HTTP 语义不自动保证业务副作用只发生一次；业务幂等需要稳定业务键、合法状态和原子处理。
> - 验签只证明来源与完整性，仍需核对商户、订单、金额、状态等业务字段。
> - 简历确认沙箱预下单、RSA 验签、状态校验和重复通知过滤；原子 SQL、对账与补偿细节仍需代码证据。

## ⑥ 算法：A2 前缀和 + HashMap

关联节点：[[12-算法/01-Java算法常用API#629. HashMap 常用方法有哪些？|629]]、[[12-算法/02-算法基础#637. 数组常见题型有哪些？|637]]、[[12-算法/02-算法基础#640. Hash 表常见题型有哪些？|640]]。

实现 `long countSubarrays(int[] nums, long k)`：返回和恰好为 `k` 的连续非空子数组数量。

函数契约：

- `nums` 非 `null`，允许空数组、负数、零和重复值。
- 不修改输入数组；前缀和与返回计数都使用 `long`。
- 空数组返回 0；输入不满足非 null 前置条件时本题不要求兜底。

至少验证：

```text
[], k = 0                                      -> 0
[1,1,1], k = 2                                 -> 2
[1,-1,0], k = 0                                -> 3
[0,0,0], k = 0                                 -> 6
[-1,-1,1], k = 0                               -> 1
[2147483647,1,-2147483647], k = 1              -> 2
```

- L0：为什么滑动窗口不能直接处理本题的一般负数输入，前缀和怎样转化条件？
- L1：遍历到当前位置时 HashMap 保存什么，为什么必须先查询 `prefix - k` 再加入当前前缀？
- L2：为什么要初始化 `0 -> 1`，为什么前缀和和答案都使用 long？
- L3：请用 `[1,-1,0], k=0` 逐步展示 Map 与答案变化，并运行全部测试。
- L4：用“当前前缀结束的合法子数组数”证明算法正确，说明 `O(n)` 时间和 `O(n)` 空间边界。

> [!check]- 核对点（答题前不要展开）
> - 若当前前缀为 `P[i]`，每个此前出现的 `P[j] = P[i] - k` 对应一个以当前位置结尾的合法子数组。
> - `0 -> 1` 表示数组开始前的空前缀，使从下标 0 开始的合法子数组能被计数；先查后加避免把当前空区间计入。
> - 返回数量最坏为 `n(n+1)/2`，int 可能溢出；前缀求和也可能超过 int。

## 结果与回流

| 学习线 | 母题 | 最早失败层 | 最高提示 | 等级 | 最小修复动作 |
| --- | --- | --- | --- | --- | --- |
| ① Java 基础与运行时 | J4 |  | H0 / H1 / H2 / H3 / H4 | A / B / C / 通过 |  |
| ② Spring 与数据访问 | S1 |  | H0 / H1 / H2 / H3 / H4 | A / B / C / 通过 |  |
| ③ MySQL | M3 |  | H0 / H1 / H2 / H3 / H4 | A / B / C / 通过 |  |
| ④ Redis | R4 |  | H0 / H1 / H2 / H3 / H4 | A / B / C / 通过 |  |
| ⑤ Web、项目与工程 | W4 |  | H0 / H1 / H2 / H3 / H4 | A / B / C / 通过 |  |
| ⑥ 算法与手写 | A2 |  | H0 / H1 / H2 / H3 / H4 | A / B / C / 通过 |  |

- [ ] 六条线的首个失败层已同步到 [[00-第二轮学习队列#六线证据队列]]。
- [ ] 项目表述已对照 [[00-简历证据与追问地图]]，没有把 V0 细节补写成真实实现。
- [ ] 算法题已保存代码、测试、正确性和复杂度说明，不只记录 AC。
- [ ] 下一组只根据本次 A/B/C 结果选母题；没有结果时不预设个人薄弱点。
