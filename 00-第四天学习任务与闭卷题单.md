---
category: 学习任务
phase: 第一轮
day: 4
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第四天学习任务与闭卷题单

> Day 4 前五条线各推进 10 道新题；算法线固定为“旧题回流 + 一道实际编码题 + 正确性说明”，不再为了保持题量重复计算已经覆盖的算法八股题。

导航：[[00-总目录]] · [[00-第一轮学习队列]] · [[00-第三天学习任务与闭卷题单]] · [[00-每日学习与闭卷考核模板]]

## 开始前

- [ ] 复制 [[00-每日学习与闭卷考核模板]]，以当天日期命名。
- [ ] 从 Day 3 记录中为每条学习线选择一个 `不会` 或 `不稳` 断点。
- [ ] 同一题连续失败时先写出“我具体卡在哪里”，再决定重读、画图还是手写，避免只重复阅读。
- [ ] Day 3 尚未执行时，先完成 [[00-第三天学习任务与闭卷题单]]。

## Day 3 回流槽

| 学习线 | 回流题号 | Day 3 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | [[01-Java基础/02-Object与String]] 题 31～34；[[01-Java基础/03-泛型]] 题 35～40 | 10 | 串联字符串池、String 比较与拼接，再用“读取/写入能力”解释泛型通配符和 PECS 边界 |
| ② Spring 与数据访问 | [[02-Spring/03-SpringMVC]] 题 127～136 | 10 | 画出 HTTP 请求进入 Spring MVC 后的主链路，并说明路由匹配、Controller 注解和参数绑定分工 |
| ③ MySQL | [[04-MySQL/03-索引]] 题 255～264 | 10 | 串联 B+Tree、聚簇/二级索引、回表与覆盖索引，能根据查询字段判断是否可能覆盖 |
| ④ Redis | [[05-Redis/03-Redis缓存]] 题 335～342；[[05-Redis/04-缓存穿透击穿雪崩]] 题 343～344 | 10 | 画出写后失效和并发不一致窗口，区分概率降低与强一致，再识别缓存穿透的输入链路 |
| ⑤ Web、项目与工程 | [[06-Web/03-HTTP]] 题 411～420 | 10 | 从报文结构进入方法语义，重点区分 GET/POST/PUT/PATCH 与协议幂等语义 |
| ⑥ 算法与手写 | 回流 [[12-算法/02-算法基础#641. 二分查找的基本写法是什么？|题 641]]；手写 `lowerBound` | 0 + 实战 | 实现“返回第一个大于等于 target 的位置；不存在时返回数组长度”，说明循环不变量、终止性和边界测试 |

### 算法线编码要求

函数契约：输入升序 `int[] nums` 和 `target`，返回第一个满足 `nums[i] >= target` 的下标；若不存在则返回 `nums.length`。

至少验证：

- 空数组。
- 单元素数组，目标分别小于、等于和大于该元素。
- `[1, 2, 2, 4]` 中目标为 `0`、`2`、`3`、`5`。
- 存在重复值时返回第一个合法位置，而不是任意命中位置。

完成代码后必须口述：搜索区间含义、循环不变量、每次移动为何不丢解、循环为什么终止、时间和空间复杂度。

### 每线完成条件

- [ ] 回流题已重新闭卷回答，并记录修复后的结果。
- [ ] 新学内容至少完成一次脱稿回答、链路图或手写。
- [ ] 已说出一个常见结论的成立条件，而不是只背结论。
- [ ] `lowerBound` 已通过规定边界用例，并能解释不变量。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-每日学习与闭卷考核模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[01-Java基础/03-泛型#40. `<? extends T>` 和 `<? super T>` 有什么区别？|题 40：extends 和 super 通配符有什么区别？]]

- 主问题：从可安全读取和可安全写入的类型解释两者差异。
- 追问 1：为什么 `List<? extends Number>` 通常不能安全加入一个 `Integer`？
- 追问 2：PECS 是什么经验规则，它不能替代哪些具体类型分析？

### ② Spring 与数据访问

[[02-Spring/03-SpringMVC#127. 一个 HTTP 请求进入 Spring MVC 后经历哪些流程？|题 127：一个 HTTP 请求进入 Spring MVC 后经历哪些流程？]]

- 主问题：从 DispatcherServlet 开始串联映射、适配、Controller、返回值处理和响应。
- 追问 1：HandlerMapping 与 HandlerAdapter 的职责为什么不同？
- 追问 2：参数解析、消息转换和异常处理分别可能在哪些环节介入？

### ③ MySQL

[[04-MySQL/03-索引#263. 什么是覆盖索引？|题 263：什么是覆盖索引？]]

- 主问题：说明“查询所需列可直接由索引提供”的含义。
- 追问 1：覆盖索引为什么可能减少回表，但不能保证查询一定更快？
- 追问 2：`SELECT *` 为什么通常不利于形成覆盖索引？

### ④ Redis

[[05-Redis/03-Redis缓存#340. 如何降低缓存和数据库不一致的概率？|题 340：如何降低缓存和数据库不一致的概率？]]

- 主问题：说明常见写路径、失败重试和失效策略。
- 追问 1：为什么“先更新数据库再删除缓存”仍存在并发和删除失败窗口？
- 追问 2：延迟双删、消息重试或订阅变更日志分别能改善什么，为什么都不能被描述为自动强一致？

### ⑤ Web、项目与工程

[[06-Web/03-HTTP#419. 什么是 HTTP 方法幂等？|题 419：什么是 HTTP 方法幂等？]]

- 主问题：从协议语义说明重复执行一次和多次的预期效果。
- 追问 1：方法语义幂等是否等于具体业务实现天然不会产生重复副作用？
- 追问 2：POST 是否绝对不能实现业务幂等？PUT 是否在任何实现中都必然安全重试？

### ⑥ 算法与手写

[[12-算法/02-算法基础#641. 二分查找的基本写法是什么？|题 641：二分查找的基本写法是什么？]]

- 主问题：先说明普通二分的前提、区间定义和循环收缩规则。
- 手写：重新实现今天的 `lowerBound`，不能查看白天代码。
- 追问：闭区间与左闭右开区间写法的终止条件为什么不同？如何用循环不变量证明返回位置正确？

## 评分规则

前五题每题 10 分：核心结论 2 分、原理链路 2 分、成立条件与边界 2 分、两层追问 2 分、场景或工程影响 2 分。

算法题 10 分：函数契约 2 分、区间与不变量 3 分、代码和返回值 3 分、边界测试与复杂度 2 分。

总分 60 分：

- 48～60：整体通过；单题不足 8 分仍记为 `不稳`。
- 36～47：Day 5 保留对应回流槽，并降低失败学习线的新题量。
- 0～35：停止薄弱线扩题，优先修复重复失败的知识链。

## 考后动作

- [ ] 对照原笔记纠错，并记录问题属于概念、边界、场景还是表达断点。
- [ ] 将六道题分别标记为 `不会` / `不稳` / `通过`。
- [ ] 仅根据闭卷证据更新相关知识笔记的 `status`。
- [ ] 把连续失败题和 `lowerBound` 的首个错误用例加入 Day 5 回流。
- [ ] 原笔记疑点单独记录，核验可靠资料后再创建维护 PR。
