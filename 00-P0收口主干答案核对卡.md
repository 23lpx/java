---
category: 追问训练答案
phase: 第二轮
group: 收口主干
status: 可使用
tags:
  - Java后端
  - 求职
  - P0
  - 答案核对
---

# P0 收口主干答案核对卡

> 本卡覆盖 J3 / S4 / M4 / R3 / W3 / A4 六条收口主干，使 24 条 P0 母题都具备答案参考。覆盖完整不等于闭卷掌握，仍须先答后核对。

导航：[[00-总目录]] · [[00-P0母题与追问链地图]] · [[00-P0母题答案核对入口]] · [[00-追问链训练模板]] · [[00-第二轮学习队列]]

## 使用规则

1. 先独立完成 30 / 60 / 120 秒首答、L0～L4 追问和算法运行，再打开对应部分。
2. 参考答案只校准结论、机制、边界、场景和证据；不得把“读过答案”记成“已经掌握”。
3. 每条只记录首个失效断点，并回流最小知识节点；没有真实记录时不预填等级。
4. 简历已确认内容可以作为项目事实，未核验的配置、代码和性能数据只能标为待核验或通用工程方案。
5. 24/24 表示答案参考层完成，不再用“补答案数量”替代闭卷、连续追问和项目证据训练。

## ① Java：J3 JVM 内存、对象创建与回收

知识依据：[[08-JVM/01-JVM基础#474. JVM 运行时内存区域有哪些？|474]]、[[08-JVM/01-JVM基础#475. 哪些区域是线程私有的？|475]]、[[08-JVM/01-JVM基础#476. 哪些区域是线程共享的？|476]]、[[08-JVM/01-JVM基础#477. Java 堆中主要存放什么？|477]]、[[08-JVM/01-JVM基础#478. Java 栈中主要存放什么？|478]]、[[08-JVM/01-JVM基础#481. 方法区和元空间是什么？|481]]、[[08-JVM/01-JVM基础#482. `new` 一个 Java 对象大致经历什么过程？|482]]、[[08-JVM/01-JVM基础#483. 什么情况下可能发生 StackOverflowError？|483]]、[[08-JVM/01-JVM基础#484. 什么情况下可能发生 OutOfMemoryError？|484]]、[[08-JVM/02-GC与类加载#485. GC 是什么？|485]]、[[08-JVM/02-GC与类加载#487. 如何判断一个对象是否可以被回收？|487]]、[[08-JVM/02-GC与类加载#488. 什么是可达性分析？|488]]、[[08-JVM/02-GC与类加载#489. 什么是强引用、软引用、弱引用？|489]]。

### 30 秒参考回答

JVM 规范中的运行时区域包括 pc、JVM 栈、堆、方法区、运行时常量池和可选本地方法栈；栈和 pc 按线程建立，堆与方法区共享。典型 `new` 会解析类、分配存储、写默认值并执行构造初始化，但对象布局和 TLAB 属于实现细节，JIT 还可能消除分配。GC 主要通过从 GC Roots 做可达性分析判断对象生命周期，不是引用计数，也不保证不可达后立即回收。

### 60 秒扩展要点

- JVM 栈保存方法调用帧；局部变量中的引用不是对象本体。堆用于实例和数组，但 JIT 标量替换意味着源码每个 `new` 不一定对应可观察堆对象。
- 方法区是规范逻辑区域，HotSpot 元空间是类元数据的一种实现；“JDK 8 后方法区就等于元空间”不准确。
- 从 GC Roots 可达的对象保留，不可达对象通常具备回收资格；互相引用的孤岛可以被回收，可达但业务上无用的对象仍会形成逻辑泄漏。
- 强、软、弱、虚引用有不同处理语义；软引用不适合作为可靠缓存容量策略，弱引用也不能承诺某个确定时间点立即清理。
- StackOverflowError 常来自单线程调用深度超过栈限制；OOM 可能来自堆、元空间、直接/本地内存、GC overhead 或创建本地线程失败。
- GC 管理受管内存，不会自动关闭文件、Socket、数据库连接，也不能修复无上限缓存或 ThreadLocal 保留链。

### 120 秒场景闭环

假设服务运行一段时间后报 OOM，第一步保留完整错误消息、GC 日志和时间线，先分清 `Java heap space`、`Metaspace`、直接内存或 `unable to create native thread`，不能一律调大 `-Xmx`。堆问题再用对象直方图和 heap dump 查看增长类型与到 GC Roots 的保留路径，区分真实工作集过大和静态集合、ThreadLocal、监听器等泄漏；元空间看类加载/卸载与 ClassLoader，线程失败看线程数、`-Xss` 和系统限制。修复后用同类负载复测对象存活、GC 暂停、堆占用平台期和错误是否消失。若堆栈反复出现同一递归链，则应转向 StackOverflowError 的调用深度问题。该诊断是通用工程能力，不是简历已确认事故。

> [!check]- L0～L4 核对
> - L0：能按规范说明运行时区域、共享性与栈/堆职责。
> - L1：解释对象创建、GC Roots 可达性和引用语义，而不是只背分区名称。
> - L2：分清方法区/元空间、SOE/多类 OOM、不可达/立即回收与 GC/资源释放边界。
> - L3：根据错误消息选择 GC 日志、heap dump、ClassLoader、NMT 或线程证据。
> - L4：用保留路径、占用平台期和相同负载复测验证，并声明通用场景属性。

## ② Spring：S4 声明式事务与 MyBatis 写入链

知识依据：[[02-Spring/08-Spring事务#188. `@Transactional` 有什么作用？|188]]、[[02-Spring/08-Spring事务#190. Spring 声明式事务大致是怎么实现的？|190]]、[[02-Spring/08-Spring事务#192. Spring 默认哪些异常会触发回滚？|192]]、[[02-Spring/08-Spring事务#194. `rollbackFor` 有什么作用？|194]]、[[02-Spring/08-Spring事务#195. 把异常 catch 住为什么可能导致事务不回滚？|195]]、[[02-Spring/08-Spring事务#196. 同一个类内部调用另一个事务方法为什么可能失效？|196]]、[[02-Spring/08-Spring事务#198. 什么是事务传播行为？|198]]、[[02-Spring/08-Spring事务#200. 项目中为什么主表和子表写入需要放在同一个事务中？|200]]、[[03-MyBatis/01-MyBatis#204. Mapper 接口没有实现类为什么可以执行？|204]]、[[03-MyBatis/01-MyBatis#207. `#{}` 和 `${}` 有什么区别？|207]]、[[03-MyBatis/01-MyBatis#215. `<foreach>` 有什么作用？|215]]、[[03-MyBatis/01-MyBatis#216. 如何通过 `<foreach>` 实现批量插入？|216]]、[[03-MyBatis/01-MyBatis#217. 什么是主键回填？|217]]、[[03-MyBatis/01-MyBatis#219. 为什么项目中的子表插入需要主键回填？|219]]、[[07-项目/02-订单与关联数据#446. 为什么主表和子表必须放在同一事务中？|446]]。

### 30 秒参考回答

Spring 声明式事务通常由代理和事务拦截器在 Service 方法调用前加入或创建事务，并在正常完成时提交、满足回滚规则的异常传播时回滚。订单写入链是：插入主表、取得或回填主键、把同一 ID 写入明细、再批量插入子表；若这些记录共同构成一个本地业务结果，就应加入同一事务。写了 `@Transactional` 不代表必然生效，还要满足代理入口、事务管理器、线程和异常传播条件。

### 60 秒扩展要点

- 默认回滚通常针对 RuntimeException 和 Error；checked exception 是否回滚要按配置，`rollbackFor` 用来显式扩展规则，不是所有异常天然回滚。
- 业务方法 catch 异常后正常返回，外层事务拦截器可能看不到失败；应重新抛出、转换为仍匹配回滚规则的异常，或在明确必要时标记 rollback-only。
- 同类普通自调用不经过外层代理，private 方法也通常不是可靠事务入口；代理类型和 Spring 版本不能靠口诀代替运行验证。
- 命令式事务资源通常绑定当前线程，异步线程不会自动加入原事务；远程支付、消息等也不能由本地数据库事务回滚。
- MyBatis Mapper 由代理将方法映射到 SQL；`#{}` 通常通过参数绑定降低注入风险，`${}` 是文本替换，动态标识符必须走白名单。
- 主键回填只表示当前参数对象获得生成键，不等于事务已经提交；多值 INSERT 与 JDBC Batch 也不是同一种执行模型。

### 120 秒场景闭环

假设订单主表 INSERT 成功后，某条明细违反非空或唯一约束，最终数据库却留下孤立主表。先检查事务方法是否由外部 Spring Bean 调用、是否使用正确事务管理器和同一数据源、Mapper 调用是否在同一线程，以及异常是否在 Service 内被 catch 后转换成成功响应；再核对默认回滚规则或 `rollbackFor`。修复要让主表、主键回填和明细批量写入处于同一本地事务，让异常传播到事务拦截器后整体回滚；统一异常处理应在事务边界外映射安全响应。测试应注入第一条/中间明细失败、自调用、checked exception 和批量约束失败，确认主子数据都不存在且错误响应稳定。简历确认本项目使用声明式事务、主键回填和批量插入，具体 Mapper 配置及异常类型仍需代码核验。

> [!check]- L0～L4 核对
> - L0：说明事务代理的开始/提交/回滚，以及主键回填后的主子写入顺序。
> - L1：解释默认回滚、传播行为、Mapper 代理、参数绑定与批量写入。
> - L2：覆盖 catch、自调用、private、线程切换、多事务资源和外部系统边界。
> - L3：从代理入口、数据源、线程、异常传播和回滚规则定位孤立主表。
> - L4：通过不同失败位置验证主子全无或全有，并区分项目事实与待核验配置。

## ③ MySQL：M4 锁、超卖与死锁

知识依据：[[04-MySQL/05-锁#293. 为什么数据库需要锁？|293]]、[[04-MySQL/05-锁#295. 什么是排他锁？|295]]、[[04-MySQL/05-锁#296. 什么是行锁？|296]]、[[04-MySQL/05-锁#299. 什么是悲观锁？|299]]、[[04-MySQL/05-锁#300. 什么是乐观锁？|300]]、[[04-MySQL/05-锁#301. 乐观锁通常如何实现？|301]]、[[04-MySQL/05-锁#302. 什么是数据库死锁？|302]]、[[04-MySQL/05-锁#303. MySQL 为什么会产生死锁？|303]]、[[04-MySQL/05-锁#304. 如何降低死锁发生概率？|304]]。

### 30 秒参考回答

InnoDB 用锁协调写入与锁定读，普通一致性读可结合 MVCC 读取快照。所谓行锁实际跟随索引记录和扫描范围，不合适的索引可能锁住大量记录，但不是自动“升级表锁”。库存防超卖优先把判断与扣减合成 `UPDATE ... WHERE stock > 0`，按受影响行数判定；需要先读复杂状态时才评估 `FOR UPDATE`。多个事务取得锁顺序形成等待环会死锁，数据库回滚受害者后应用仍要安全处理。

### 60 秒扩展要点

- X 锁阻塞冲突写和锁定读，但普通 RC/RR 一致性读不一定被阻塞；必须区分快照读与当前/锁定读。
- Record、gap 和 next-key lock 的范围受索引、条件和隔离级别影响；无合适索引会扩大扫描与锁集合，不应简化成固定锁一行。
- 悲观方案在事务内先锁定再修改，适合冲突高且必须基于锁后状态决策，但会增加等待、死锁和持锁时间。
- 乐观方案把版本或旧状态放进同一条 UPDATE 条件，并检查受影响行数；影响 0 行还可能是记录不存在或状态非法，不能自动当重复成功。
- 死锁不只来自显式锁两行，二级索引、间隙、外键和重复键检查也会产生交叉锁集合；必须结合死锁日志和执行计划。
- 重试要重新执行整个已回滚事务、设置上限和短暂退避；事务含不可回滚外部副作用时，先保证幂等再谈重试。

### 120 秒场景闭环

假设两个批量扣减请求分别按 `[商品1, 商品2]` 和 `[商品2, 商品1]` 更新，偶发死锁。先从 `SHOW ENGINE INNODB STATUS` 或死锁日志确认受害事务、持有/等待的索引记录及 SQL，再核对执行计划和输入 ID 顺序，不能只凭“事务太长”下结论。修复可在进入事务前按稳定主键顺序排序，保证条件有合适索引，使用带库存条件的原子 UPDATE，并缩短事务、移出远程调用。死锁异常回滚后按有限次数重试完整事务，同时用业务请求号防止外部副作用重复。验证并发扣减总成功数不超过库存、最终库存不为负，并观察死锁、锁等待和重试率。该场景是通用并发设计，简历未确认真实库存压测或死锁治理成果。

> [!check]- L0～L4 核对
> - L0：说清锁/MVCC 分工、条件更新和死锁等待环。
> - L1：解释索引锁范围、悲观/乐观取舍、影响行数与受害事务回滚。
> - L2：否定“不走索引升级表锁、X 锁阻塞所有读、死锁无限重试”等绝对说法。
> - L3：用死锁日志、执行计划和加锁顺序定位，并处理完整事务重试。
> - L4：验证库存不负、成功数上限、锁等待和重试指标，声明通用场景边界。

## ④ Redis：R3 穿透、击穿与雪崩

知识依据：[[05-Redis/04-缓存穿透击穿雪崩#343. 什么是缓存穿透？|343]]、[[05-Redis/04-缓存穿透击穿雪崩#346. 如何解决缓存穿透？|346]]、[[05-Redis/04-缓存穿透击穿雪崩#347. 缓存空值如何解决缓存穿透？|347]]、[[05-Redis/04-缓存穿透击穿雪崩#348. 什么是布隆过滤器？|348]]、[[05-Redis/04-缓存穿透击穿雪崩#349. 布隆过滤器为什么存在误判？|349]]、[[05-Redis/04-缓存穿透击穿雪崩#350. 什么是缓存击穿？|350]]、[[05-Redis/04-缓存穿透击穿雪崩#353. 如何解决缓存击穿？|353]]、[[05-Redis/04-缓存穿透击穿雪崩#354. 互斥锁如何解决缓存击穿？|354]]、[[05-Redis/04-缓存穿透击穿雪崩#355. 什么是逻辑过期？|355]]、[[05-Redis/04-缓存穿透击穿雪崩#356. 什么是缓存雪崩？|356]]、[[05-Redis/04-缓存穿透击穿雪崩#358. 如何解决缓存雪崩？|358]]、[[05-Redis/04-缓存穿透击穿雪崩#359. TTL 加随机值有什么作用？|359]]、[[05-Redis/04-缓存穿透击穿雪崩#360. 缓存穿透、击穿、雪崩有什么区别？|360]]。

### 30 秒参考回答

穿透是大量请求查询事实源不存在的数据，缓存持续 miss 后回源；击穿是单个热点 Key 失效或未命中时，高并发同时重建；雪崩是大量 Key 同期失效、缓存服务故障或容量问题使大范围请求一起落到下游。三者的流量形态和故障范围不同，治理也不同：穿透靠校验/鉴权、空值、布隆和限流；击穿靠请求合并、互斥重建或可接受陈旧值；雪崩靠 TTL 分散、隔离降级、容量与高可用。

### 60 秒扩展要点

- 空值缓存只保护已确认不存在且重复出现的 Key，要使用较短 TTL并区分“无数据”和“查询失败”；不断变化的随机 Key 仍需校验、限流等入口治理。
- 标准布隆过滤器有假阳性：位冲突会把不存在元素判断为可能存在；在正确初始化、只添加且状态未丢失的前提下通常无假阴性，但数据漏加、删除或重建切换会破坏这个前提。
- 热点 Key 互斥重建要双重检查缓存、限制等待并保证锁所有权与释放；锁服务本身故障时还需失败策略，不能把分布式锁当绝对正确。
- 逻辑过期可先返回旧值并异步刷新，换取可用性，但只适合业务允许短暂陈旧且缓存长期保留、刷新任务可恢复的场景。
- TTL 抖动只降低批量同刻到期概率，不能解决 Redis 节点故障、热点单 Key、下游容量不足或所有 Key 使用相同业务截止时间。
- 多级缓存、高可用、限流降级和预热分别解决不同环节；任何方案都要同时评估回源数据库的承载上限。

### 120 秒场景闭环

假设大促时同时出现随机无效商品 ID、一个爆款 Key 到期、以及一批整点 TTL 缓存失效。先按 Key 基数、单 Key QPS、miss 分布、数据库查询类型和到期时间直方图区分：高基数不存在数据是穿透，单热点失效是击穿，同期大范围 miss 是雪崩。入口先做格式/租户/权限校验并限流，不存在结果短期缓存；热点重建用单飞/互斥并设置等待与降级；批量缓存打散允许范围内的 TTL，并准备预热、熔断和数据库保护。通过随机 Key、热点过期和批量到期三类故障注入分别观察回源峰值、P99、锁等待、旧值窗口和拒绝率。简历确认项目使用缓存，但空值、布隆、逻辑过期、互斥锁和雪崩治理均需代码与配置证据。

> [!check]- L0～L4 核对
> - L0：按不存在数据、单热点和大范围失效准确区分三类问题。
> - L1：解释空值/布隆、互斥/逻辑过期、TTL 抖动/降级各自机制。
> - L2：覆盖随机新 Key、布隆状态、锁失败、陈旧值和节点故障边界。
> - L3：先看 Key 基数、单 Key QPS 与 TTL/miss 分布，再做针对性治理。
> - L4：分别故障注入并验证回源、延迟、锁等待和拒绝，守住项目证据范围。

## ⑤ Web/项目：W3 统一异常、订单关联与历史快照

知识依据：[[07-项目/01-统一响应与异常处理#429. 为什么后端需要统一响应结构？|429]]、[[07-项目/01-统一响应与异常处理#431. 什么是全局异常处理？|431]]、[[07-项目/01-统一响应与异常处理#435. 为什么不在每个 Controller 里写 try-catch？|435]]、[[07-项目/01-统一响应与异常处理#436. 什么是业务异常？|436]]、[[07-项目/01-统一响应与异常处理#437. 数据库异常为什么不应该直接返回前端？|437]]、[[07-项目/01-统一响应与异常处理#438. 唯一约束异常如何转换成友好提示？|438]]、[[07-项目/02-订单与关联数据#439. 为什么订单表和订单明细表要拆开？|439]]、[[07-项目/02-订单与关联数据#443. 主键回填在关联数据写入中有什么作用？|443]]、[[07-项目/02-订单与关联数据#444. 为什么子表数据适合批量插入？|444]]、[[07-项目/02-订单与关联数据#446. 为什么主表和子表必须放在同一事务中？|446]]、[[07-项目/02-订单与关联数据#447. 为什么订单不能只保存 addressBookId？|447]]、[[07-项目/02-订单与关联数据#448. 什么是订单地址快照？|448]]、[[07-项目/02-订单与关联数据#451. 为什么历史订单不能跟随地址簿变化？|451]]。

### 30 秒参考回答

统一响应为调用方提供稳定的成功、错误码、消息和数据契约，全局异常处理负责把已知业务/约束异常映射成安全响应，并为未知异常保留内部诊断信息；它不能替代事务。订单与明细是一对多聚合，主表写入并取得 ID 后批量写明细，任一步失败应在同一本地事务整体回滚。订单还要复制下单时必要地址形成历史快照，不能只引用可变的 addressBookId。

### 60 秒扩展要点

- Controller 到处 try-catch 会重复映射逻辑并容易吞掉异常；全局处理器统一边界，但 Filter、异步线程或已提交响应仍可能需要其他处理。
- 业务异常表达可预期失败，未知异常对外给通用信息，对内保留 traceId、cause 和脱敏上下文；数据库错误原文不能直接作为 API 契约。
- 唯一约束是并发下最终防线，但不能把所有 `DataIntegrityViolationException` 都翻译成重复，需要稳定约束或结构化错误信息。
- 主键回填让应用把同一父 ID 写入明细，不表示已提交；批量插入减少往返，也不保证无限批次或自动处理每行失败。
- 地址簿是当前可变主数据，订单快照是下单时事实；快照有意不随普通地址簿修改同步，“旧值”并不是缓存不一致。
- 历史稳定不等于永远不能受控更正；改址、隐私删除或脱敏需要状态、权限和审计流程。

### 120 秒场景闭环

假设下单时先写订单主表并生成地址快照，批量写明细时触发约束异常。正确链路是异常继续传播，使本地事务回滚主表和明细，再由事务边界外的全局处理器映射安全错误码并记录 traceId；若 Service catch 后返回统一“失败对象”，事务可能正常提交而留下孤立订单。验证数据库中主子记录全无、响应不泄露 SQL，并覆盖无明细、非法商品、批量中间失败和重复请求。地址簿随后修改时，历史订单继续显示原快照；业务若允许改配送地址，应更新订单自身并留痕。简历已确认统一响应、数据库约束异常、主键回填、批量插入、声明式事务和地址快照，但具体字段、状态码、异常类型和改址能力仍待代码核验。

> [!check]- L0～L4 核对
> - L0：分清统一响应、异常映射、事务原子性和历史快照四种职责。
> - L1：解释主键回填/批量写入链、异常传播和快照的时间语义。
> - L2：覆盖处理器作用域、约束分类、外部系统、受控改址与隐私边界。
> - L3：针对明细失败检查事务是否回滚、异常是否被吞和响应是否安全。
> - L4：验证主子全无或全有、历史值稳定与日志可诊断，并逐项标注项目证据。

## ⑥ 算法：A4 堆、动态规划与正确性证明

知识依据：[[12-算法/02-算法基础#648. 堆通常解决什么问题？|648]]、[[12-算法/02-算法基础#649. 动态规划的基本思路是什么？|649]]、[[12-算法/01-Java算法常用API#632. PriorityQueue 如何使用？|632]]、[[12-算法/01-Java算法常用API#636. Java 如何自定义排序？|636]]。

### 30 秒参考回答

堆适合动态维护当前最高或最低优先级：求最大 K 个元素时维护大小 K 的最小堆，堆顶是当前候选中最容易淘汰的最小值。动态规划则先定义能决定答案的状态，再说明转移、base case、计算顺序和最终答案；它依赖重叠子问题与可组合结构，不能只因题目有递归就套 DP。两类题都必须说出维护的不变量，而不是只给模板。

### 60 秒扩展要点

- Java PriorityQueue 默认是最小堆，只保证 `peek/poll` 的堆顶顺序，直接迭代不保证整体有序；连续弹出 K 次通常是 `O(K log K)`。
- 最大 K 用最小堆，是因为容量满时可立刻比较并淘汰候选中的最小者；如果 K 接近 n，排序或快速选择可能更合适。
- Comparator 必须满足反对称、传递等契约，数值比较不能用直接相减；自然顺序同样要处理 null 与业务稳定性。
- DP 状态要包含决定未来所需的全部信息，转移要覆盖合法最后一步且不重不漏；初始化与遍历方向必须和依赖关系一致。
- 0/1 背包一维压缩常从大到小遍历容量，避免同一物品在本轮重复使用；完全背包的依赖与方向不同。
- 记忆化和递推都复用子问题结果。贪心、分治或图最短路更合适时，不能为使用 DP 而强行增加状态。

### 120 秒场景闭环

题目要求返回数组中最大的 K 个元素，结果按降序排列、保留重复值、不修改输入，且 `0 ≤ k ≤ n`。扫描时维护大小不超过 K 的最小堆：处理任意前缀后，堆中保存该前缀最大的至多 K 个元素；新值不大于堆顶时不可能进入全局最大 K，较大时替换堆顶。扫描结束后堆中就是答案集合，再从结果尾部反向填入每次 poll 的最小值即可得到降序。时间 `O(n log k + k log k)`，额外空间 `O(k)`；K 为 0 单独返回空数组。

### Java 参考实现

```java
import java.util.Arrays;
import java.util.PriorityQueue;

public class TopKLargest {
    public static int[] topKLargest(int[] nums, int k) {
        if (nums == null) {
            throw new IllegalArgumentException("nums must not be null");
        }
        if (k < 0 || k > nums.length) {
            throw new IllegalArgumentException("k must be between 0 and nums.length");
        }
        if (k == 0) {
            return new int[0];
        }

        PriorityQueue<Integer> minHeap = new PriorityQueue<>();
        for (int num : nums) {
            if (minHeap.size() < k) {
                minHeap.offer(num);
            } else if (num > minHeap.peek()) {
                minHeap.poll();
                minHeap.offer(num);
            }
        }

        int[] result = new int[k];
        for (int i = k - 1; i >= 0; i--) {
            result[i] = minHeap.poll();
        }
        return result;
    }

    private static void check(int[] expected, int[] actual) {
        if (!Arrays.equals(expected, actual)) {
            throw new AssertionError(
                    "expected=" + Arrays.toString(expected)
                            + ", actual=" + Arrays.toString(actual));
        }
    }

    private static void expectInvalid(Runnable action) {
        try {
            action.run();
            throw new AssertionError("expected IllegalArgumentException");
        } catch (IllegalArgumentException expected) {
            // 通过
        }
    }

    public static void main(String[] args) {
        check(new int[0], topKLargest(new int[0], 0));
        check(new int[] {6, 5}, topKLargest(new int[] {3, 2, 1, 5, 6, 4}, 2));
        check(new int[] {5, 5, 5}, topKLargest(new int[] {5, 4, 5, 5}, 3));
        check(
                new int[] {Integer.MAX_VALUE, 0},
                topKLargest(new int[] {Integer.MIN_VALUE, -1, 0, Integer.MAX_VALUE}, 2));
        check(new int[] {3, 2, 1}, topKLargest(new int[] {2, 1, 3}, 3));

        int[] input = {4, 1, 3, 2};
        int[] snapshot = input.clone();
        check(new int[] {4, 3}, topKLargest(input, 2));
        if (!Arrays.equals(snapshot, input)) {
            throw new AssertionError("input was modified");
        }

        expectInvalid(() -> topKLargest(null, 0));
        expectInvalid(() -> topKLargest(new int[] {1}, -1));
        expectInvalid(() -> topKLargest(new int[] {1}, 2));
    }
}
```

> [!check]- L0～L4 核对
> - L0：说明最大 K 为什么维护最小堆，并能完整说出 DP 的状态—转移—初始化—顺序。
> - L1：解释堆顶不变量、PriorityQueue 迭代边界与 DP 依赖关系。
> - L2：覆盖 K=0/K=n、重复值、整数极值、输入不变性及 K 接近 n 的方案取舍。
> - L3：能手写、编译并运行空数组、重复值、极值、全量 K 和非法输入测试。
> - L4：用前缀不变量证明堆中始终是最大 K 个候选，并给出准确时空复杂度。

## 核对结果回流

| 学习线 | 母题 | 与参考答案的首个差异 | 失败层 | 等级 | 最小修复节点 |
| --- | --- | --- | --- | --- | --- |
| ① Java 基础与运行时 | J3 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ② Spring 与数据访问 | S4 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ③ MySQL | M4 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ④ Redis | R3 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ⑤ Web、项目与工程 | W3 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ⑥ 算法与手写 | A4 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |

P0 答案参考层到此覆盖 24/24 条母题。下一阶段不再按空白补参考稿，而是从真实闭卷、模拟面试或项目代码核验结果中选择最小断点，进行换问法复测和证据补全。
