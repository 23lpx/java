---
category: 学习任务
phase: 第一轮
day: 15
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第十五天学习任务与闭卷题单

> Day 15 按各线真实剩余量推进：Java 并发、Linux 各学习 10 道新题；Spring/MyBatis、MySQL、Redis 分别训练动态 SQL 注入防线、库存防超卖和缓存雪崩；算法线手写三数之和。

导航：[[00-总目录]] · [[00-第一轮学习队列]] · [[00-第十四天学习任务与闭卷题单]] · [[00-每日学习与闭卷考核模板]]

## 开始前

- [ ] 复制 [[00-每日学习与闭卷考核模板]]，以当天日期命名。
- [ ] 从 Day 14 记录中为每条学习线选择一个 `不会` 或 `不稳` 断点。
- [ ] 如果某条线连续失败题超过 2 道，本日先修复最靠前的核心断点，再决定是否执行综合场景。
- [ ] Day 14 尚未执行时，先完成 [[00-第十四天学习任务与闭卷题单]]。

## Day 14 回流槽

| 学习线 | 回流题号 | Day 14 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 / 回查或实战 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | [[09-Java并发/02-锁与线程池]] 题 516～522；[[09-Java并发/03-并发进阶]] 题 591～593 | 10 / 0 | 画出线程池从核心线程、队列、最大线程到拒绝的接收路径，区分 HashMap/ConcurrentHashMap，再定位 CAS 与 AQS 的职责和边界 |
| ② Spring 与数据访问 | 回查 [[03-MyBatis/01-MyBatis#207. `#{}` 和 `${}` 有什么区别？|207]]、[[03-MyBatis/01-MyBatis#208. 为什么 `#{}` 可以降低 SQL 注入风险？|208]]、[[03-MyBatis/01-MyBatis#209. `${}` 为什么存在 SQL 注入风险？|209]]、[[03-MyBatis/01-MyBatis#210. `${}` 一般在什么场景使用？|210]]、[[03-MyBatis/01-MyBatis#211. 什么是 MyBatis 动态 SQL？|211]]、[[03-MyBatis/01-MyBatis#212. `<if>` 有什么作用？|212]]、[[03-MyBatis/01-MyBatis#213. `<where>` 有什么作用？|213]]、[[03-MyBatis/01-MyBatis#214. `<set>` 有什么作用？|214]]；完成动态排序安全设计 | 0 / 1 场景 | 区分参数绑定与 SQL 文本替换，把客户端排序字段映射为服务端固定片段，并补齐查询条件、授权和测试边界 |
| ③ MySQL | 回查 [[04-MySQL/02-表设计#240. 什么是唯一索引？|240]]、[[04-MySQL/05-锁#293. 为什么数据库需要锁？|293]]、[[04-MySQL/05-锁#295. 什么是排他锁？|295]]、[[04-MySQL/05-锁#299. 什么是悲观锁？|299]]、[[04-MySQL/05-锁#300. 什么是乐观锁？|300]]、[[04-MySQL/05-锁#301. 乐观锁通常如何实现？|301]]；完成库存防超卖演练 | 0 / 1 场景 | 用带库存条件的原子更新竞争扣减权，根据受影响行数分流，并比较悲观锁、乐观条件更新和幂等创建的边界 |
| ④ Redis | 回查 [[05-Redis/01-Redis基础#318. 为什么缓存通常要设置 TTL？|318]]、[[05-Redis/03-Redis缓存#342. 什么是缓存预热？|342]]、[[05-Redis/04-缓存穿透击穿雪崩#356. 什么是缓存雪崩？|356]]、[[05-Redis/04-缓存穿透击穿雪崩#357. 缓存雪崩通常是怎么产生的？|357]]、[[05-Redis/04-缓存穿透击穿雪崩#358. 如何解决缓存雪崩？|358]]、[[05-Redis/04-缓存穿透击穿雪崩#359. TTL 加随机值有什么作用？|359]]、[[05-Redis/04-缓存穿透击穿雪崩#360. 缓存穿透、击穿、雪崩有什么区别？|360]]；完成批量过期演练 | 0 / 1 场景 | 从同步过期时间线还原数据库回源洪峰，用 TTL 抖动、分批预热、限流降级和容量验证形成闭环 |
| ⑤ Web、项目与工程 | [[10-开发基础/04-Linux]] 题 572～581 | 10 / 0 | 串联目录定位、文件/日志查看、关键字检索、Java 进程与端口排查、信号终止和权限修改，所有命令先确认目标再执行 |
| ⑥ 算法与手写 | 回流 [[12-算法/02-算法基础#642. 双指针常见场景有哪些？|题 642]]；三数之和 | 0 + 实战 | 排序后固定一个数并用左右指针单调排除，说明三层去重、整数溢出、不修改输入和 `O(n²)` 正确性 |

> Spring/MyBatis、MySQL、Redis 已完成第一轮编号节点覆盖，本日只进行回查和综合场景，不把旧题计为新题。Day 15 的新题量为 20；三个场景按故障证据、并发时序和工程闭环单独验收。

### Spring/MyBatis 线综合场景

通用订单列表接口接收 `status`、`sort` 和 `direction`。Mapper 当前使用：

```xml
WHERE status = #{status}
ORDER BY ${sort} ${direction}
```

安全测试发现客户端可以让排序参数改变 SQL 结构。

必须完成：

1. 说明 `#{status}` 通常转换为 JDBC 参数标记并绑定值，而 `${sort}`、`${direction}` 会直接进入 SQL 文本；不能把区别简化为“一个自动转义、一个不转义”。
2. 排序列由 API 枚举映射到代码中的固定列名，方向只允许固定的 `ASC` / `DESC`；优先使用 `<choose>`、固定 statement 或类型安全 DSL，不把客户端字符串原样交给 `${}`。
3. 说明 `#{}` 不能充当列名、表名或排序方向；在值位置继续使用参数绑定，不能为了动态化而全部改成文本替换。
4. 可选查询条件使用 `<if>` 和 `<where>` 组合，验证空条件、单条件、多条件及无效枚举；动态 SQL 正确不等于用户自动拥有目标订单的访问权限。
5. 测试普通值、引号/注释字符、未知排序字段和超长输入，检查最终 SQL 结构仍来自服务端白名单；日志不得泄露敏感参数。
6. 该安全问题是通用演练，不得表述为简历项目真实漏洞；真实项目使用了哪些 Mapper 参数和排序字段仍需以代码为证据。

### MySQL 线综合场景

SKU 9001 当前库存为 1，两个请求几乎同时购买 1 件。应用若先查询库存再分别执行无条件扣减，两个请求都可能基于旧值判断“库存充足”。

必须完成：

1. 校验 `quantity > 0`，使用单条条件更新竞争扣减权：

   ```sql
   UPDATE sku_stock
   SET available = available - ?
   WHERE sku_id = ? AND available >= ?;
   ```

2. 受影响行数为 1 才表示本次取得扣减权；为 0 时进一步区分记录不存在、库存不足或状态不允许，不能直接当成“重复成功”或数据库故障。
3. 若扣库存和创建订单位于同一数据库，应放在明确的短事务中；订单插入失败时回滚扣减。跨库、消息或远程服务不因本地事务自动原子化。
4. 为请求建立业务幂等键，避免客户端重试重复创建订单；唯一约束解决重复创建，库存条件更新解决数量竞争，两者职责不同。
5. 比较 `SELECT ... FOR UPDATE` 后扣减的悲观方案与条件更新的乐观方案：前者显式持锁，后者冲突后按结果分流；热点行下都要评估等待、吞吐和重试上限。
6. 支付调用不得夹在线程长时间持有数据库锁的事务中；该库存模型是通用面试场景，不扩写成苍穹外卖已经实现的库存系统。

### Redis 线综合场景

一次发布后，系统在同一分钟预热了一万条菜品缓存，并统一设置 30 分钟 TTL。三十分钟后大量 Key 集中过期，缓存命中率骤降，数据库 QPS 和 P99 同时上升。

必须完成：

1. 根据“大量 Key 在相近时间失效并集中回源”识别缓存雪崩；单个热点 Key 到期更接近击穿，持续请求不存在数据更接近穿透。
2. 在业务允许的陈旧时间范围内为 TTL 增加抖动，并分批预热/刷新，避免把冷启动流量平移成下一次同步过期洪峰。
3. 对最热 Key 额外使用请求合并或单重建者，限制重建并发；TTL 抖动只能降低集中到期概率，不能保证热点或淘汰场景不回源。
4. 应用侧设置 Redis 超时和有限重试，对数据库回源设置并发上限、限流、熔断和降级；禁止缓存失效后让所有请求无保护地直达数据库。
5. Sentinel 或带副本的 Cluster 主要处理特定高可用/分片问题，不能消除业务配置导致的同步过期，也不能承诺故障切换期间零丢写、零延迟。
6. 用命中率、过期速率、回源 QPS/P99、降级量和 Redis 错误对齐时间线并验证修复；TTL、抖动范围和部署拓扑均按项目核验项表述。

### 算法线编码要求

实现 `List<List<Integer>> threeSum(int[] nums)`。`nums` 非 `null`，长度可以为 0；返回所有按数学整数相加等于 0 的不重复三元组。每个三元组内部升序，结果按字典序排列；不得修改调用者传入的数组。

至少验证：

```text
nums = []                                      -> []
nums = [0]                                     -> []
nums = [-1, 0, 1, 2, -1, -4]                  -> [[-1, -1, 2], [-1, 0, 1]]
nums = [0, 0, 0]                               -> [[0, 0, 0]]
nums = [0, 0, 0, 0]                            -> [[0, 0, 0]]
nums = [-2, 0, 1, 1, 2]                        -> [[-2, 0, 2], [-2, 1, 1]]
nums = [2147483647, 2147483647, 2]             -> []
```

完成代码后必须口述：为什么先复制再排序、固定 `i` 后左右指针各自排除了哪些不可能状态、`i` / `left` / `right` 三层如何去重，以及时间复杂度 `O(n²)`、复制数组与排序带来的额外空间边界。三数求和使用 `long`，避免 Java `int` 溢出把不存在的组合误判为 0。

### 每线完成条件

- [ ] Day 14 回流题已重新闭卷回答，并记录修复后的结果。
- [ ] Java 并发与 Linux 新题至少完成一次脱稿回答、饱和路径图或排障命令链。
- [ ] Spring/MyBatis 场景能区分值绑定、结构替换、白名单映射和数据访问授权。
- [ ] MySQL 场景包含原子条件更新、受影响行数、事务、幂等和悲观/乐观方案取舍。
- [ ] Redis 场景包含雪崩识别、过期打散、回源保护、高可用边界和指标验证。
- [ ] 三数之和代码通过全部用例，并能解释单调移动、完整性、去重和溢出边界。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-每日学习与闭卷考核模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[09-Java并发/02-锁与线程池#518. 拒绝策略是什么？|题 518：线程池拒绝策略是什么？]]

- 主问题：说明什么情况下执行器会拒绝任务，以及拒绝发生在任务被线程池接受之前意味着什么。
- 追问 1：线程、队列达到什么状态才进入拒绝，线程池 shutdown 后提交又如何？
- 追问 2：为什么 `CallerRunsPolicy` 可能形成反压却不保证绝不丢任务，关键任务应怎样设计失败路径？

### ② Spring 与数据访问

[[03-MyBatis/01-MyBatis#207. `#{}` 和 `${}` 有什么区别？|题 207：MyBatis 中 #{} 和 ${} 有什么区别？]]

- 主问题：从 SQL 结构、参数绑定和文本替换说明两者差异。
- 场景追问：动态 `ORDER BY` 为什么不能直接使用客户端字符串，`#{}` 为什么又不能替代列名？
- 边界追问：即使所有值都用了 `#{}`，授权缺失和其他动态片段为什么仍可能造成安全问题？

### ③ MySQL

[[04-MySQL/05-锁#301. 乐观锁通常如何实现？|题 301：乐观锁通常如何实现？]]

- 主问题：说明版本号或旧状态条件如何把比较与更新放进同一条 SQL，并检查受影响行数。
- 场景追问：库存扣减为什么要把 `available >= quantity` 放进 `WHERE`，影响 0 行有哪些可能？
- 边界追问：高冲突热点库存为什么可能更适合排队、分片或悲观方案，而不是无限乐观重试？

### ④ Redis

[[05-Redis/04-缓存穿透击穿雪崩#358. 如何解决缓存雪崩？|题 358：如何解决缓存雪崩？]]

- 主问题：从集中失效、高可用故障和数据库保护三个层次给出治理方案。
- 场景追问：同一分钟预热的一万条 Key 如何避免三十分钟后再次同步过期？
- 边界追问：TTL 抖动、Sentinel/Cluster 和限流降级各解决什么，为什么任何单项都不是完整答案？

### ⑤ Web、项目与工程

[[10-开发基础/04-Linux#579. 如何查看端口占用？|题 579：Linux 如何查看端口占用？]]

- 主问题：给出 `ss` 或 `lsof` 的监听端口排查命令，并解释 LISTEN、协议和本地地址。
- 追问 1：为什么只搜索数字 8080 可能误判，权限不足时会缺少什么信息？
- 追问 2：容器端口映射、IPv4/IPv6 和网络命名空间为什么要求同时检查多个视角？

### ⑥ 算法与手写

[[12-算法/02-算法基础#642. 双指针常见场景有哪些？|题 642：双指针常见场景有哪些？]]

- 主问题：说明左右对撞、同向读写和快慢指针分别依赖什么不变量。
- 手写：重新实现今天的三数之和，不能查看白天代码。
- 追问：移动左右指针为什么不会漏解？三层去重分别防止哪类重复？

## 评分规则

Java、Linux 题每题 10 分：核心结论 2 分、原理链路 2 分、成立条件与边界 2 分、两层追问 2 分、场景或工程影响 2 分。

Spring/MyBatis、MySQL、Redis 场景题每题 10 分：证据链 2 分、故障时序 2 分、成立条件 2 分、治理闭环 2 分、项目真实性 2 分。

算法题 10 分：函数契约 1 分、双指针不变量 3 分、去重与溢出边界 2 分、代码和返回值 2 分、复杂度 2 分。

总分 60 分：

- 48～60：整体通过；单题不足 8 分仍记为 `不稳`。
- 36～47：Day 16 保留对应回流槽，并减少失败学习线的新题量或场景复杂度。
- 0～35：暂停薄弱线扩题，优先修复重复失败的知识链。

## 考后动作

- [ ] 对照原笔记纠错，记录首个错误断点。
- [ ] 将六道题分别标记为 `不会` / `不稳` / `通过`。
- [ ] 仅根据闭卷证据更新相关知识笔记的 `status`。
- [ ] Java 并发和 Linux 只有在本日 20 道新题通过后，才推进到各自下一段。
- [ ] 三个综合场景未通过时，下一日继续修复失败链，不用新的场景掩盖问题。
- [ ] 把连续失败题和三数之和的首个错误加入 Day 16 回流。
- [ ] 原笔记疑点单独记录，核验可靠资料后再创建维护 PR。
