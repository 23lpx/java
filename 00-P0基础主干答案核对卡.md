---
category: 追问训练答案
phase: 第二轮
group: 基础主干
status: 可使用
tags:
  - Java后端
  - 求职
  - P0
  - 答案核对
---

# P0 基础主干答案核对卡

> 本卡覆盖 J1 / S2 / M2 / R2 / W1 / A1 六条基础主干，只在闭卷首答后使用。它是通用参考层，不是根据个人成绩生成的“第二组弱项题单”。

导航：[[00-总目录]] · [[00-P0母题与追问链地图]] · [[00-P0母题答案核对入口]] · [[00-追问链训练模板]] · [[00-第二轮学习队列]]

## 使用规则

1. 先保留自己的 30 / 60 / 120 秒首答、L0～L4 追问记录和算法运行结果，再看对应部分。
2. 示例允许换词和换顺序，但核心结论、成立条件、事实属性和验证方法不能改变。
3. 每条只记录第一个让答案失效的错误或缺失；后续碰巧答对不能覆盖首个断点。
4. 项目场景必须区分简历已确认事实、待代码核验实现和通用工程增强；不能把假设事故写成真实经历。
5. 核对结果只回流 [[00-第二轮学习队列#六线证据队列]]，本卡不预填 A/B/C 或“已掌握”。

## ① Java：J1 对象相等与 HashMap Key

知识依据：[[01-Java基础/02-Object与String#18. `==` 和 `equals()` 有什么区别？|18]]、[[01-Java基础/02-Object与String#20. 为什么重写 `equals()` 通常要重写 `hashCode()`？|20]]、[[01-Java基础/02-Object与String#21. `equals()` 相同，`hashCode()` 一定相同吗？|21]]、[[01-Java基础/02-Object与String#22. `hashCode()` 相同，`equals()` 一定相同吗？|22]]、[[01-Java基础/02-Object与String#23. `hashCode()` 有什么作用？|23]]、[[01-Java基础/02-Object与String#33. String 为什么适合作为 HashMap 的 Key？|33]]、[[01-Java基础/05-HashMap#63. HashMap 的 `put()` 流程是什么？|63]]、[[01-Java基础/05-HashMap#64. HashMap 的 `get()` 流程是什么？|64]]、[[01-Java基础/05-HashMap#67. HashMap 为什么同时需要 `hashCode()` 和 `equals()`？|67]]、[[01-Java基础/05-HashMap#75. HashMap 为什么线程不安全？|75]]。

### 30 秒参考回答

对基本类型，`==` 比较数值结果；对引用类型，`==` 比较是否指向同一对象。`equals()` 表示类型定义的逻辑相等，`Object` 默认实现仍是同一对象判断。对象作为 HashMap Key 时，`hashCode()` 用于缩小候选桶，`equals()` 用于确认同一个逻辑 Key；因此 `equals()` 为 true 必须有相同 hash，但相同 hash 允许冲突。参与相等判断的字段在入表后应保持稳定。

### 60 秒扩展要点

- `equals()` 要满足自反、对称、传递、一致和对 null 为 false 等契约；继承层次中尤其要防止对称性或传递性被破坏。
- `hashCode()` 不是唯一编号、内存地址或安全标识；不同对象可以有相同 hash，HashMap 仍需继续比较 Key。
- `put()` 和 `get()` 都依赖同一套 hash 与相等语义；只重写其中一个方法会让逻辑相等对象落入错误候选范围或无法命中。
- 可变 Key 入表后若其 hash/equals 字段改变，查找会按新 hash 去另一个桶，甚至遍历原桶也无法用新值确认原 Key。
- String 的不可变性和稳定的 equals/hashCode 使它常适合作为 Key，但大小写、空白和 Unicode 规范化仍属于业务 Key 设计问题。
- 普通 HashMap 不保证并发复合操作安全；正确的 Key 契约不能替代并发容器或外部同步。

### 120 秒场景闭环

假设把 `UserKey(id, tenantId)` 放入 HashMap 后又修改 `tenantId`，随后 `get(key)` 返回 null。先确认这两个字段是否参与 `equals()` 和 `hashCode()`，记录入表前后的 hash，再用一个未修改但逻辑值相同的新 Key 查询，区分是 Key 变异、相等契约错误还是并发修改。根因通常不是“HashMap 把数据丢了”，而是元素仍在原桶中，查找却按变化后的 hash 或相等结果走了另一条路径。修复优先使用不可变 Key，只选择生命周期内稳定的身份字段，并同时生成或审查 equals/hashCode；最后覆盖相等对象、hash 冲突、字段变异和并发访问测试。该案例是通用诊断场景，不代表个人项目发生过此故障。

> [!check]- L0～L4 核对
> - L0：分清引用的 `==`、默认 equals 和逻辑相等。
> - L1：说清 hash 缩小候选、equals 最终确认，以及“相等必同 hash、同 hash 未必相等”。
> - L2：指出可变 Key、继承契约、业务规范化和并发访问边界。
> - L3：能从查找失败定位到入表前后 hash、字段变化和相等实现，不先说数据丢失。
> - L4：用不可变 Key 与冲突/变异测试验证，并明确场景属性。

## ② Spring：S2 MVC 请求与异常响应链

知识依据：[[02-Spring/03-SpringMVC#127. 一个 HTTP 请求进入 Spring MVC 后经历哪些流程？|127]]、[[02-Spring/03-SpringMVC#128. DispatcherServlet 有什么作用？|128]]、[[02-Spring/03-SpringMVC#135. `@RequestBody` 有什么作用？|135]]、[[02-Spring/03-SpringMVC#136. Spring MVC 如何完成参数绑定？|136]]、[[02-Spring/03-SpringMVC#138. JSON 请求为什么可以转换成 Java 对象？|138]]、[[02-Spring/03-SpringMVC#139. Spring MVC 如何处理返回值？|139]]、[[02-Spring/04-参数校验#142. 为什么后端需要参数校验？|142]]、[[02-Spring/04-参数校验#145. 参数校验失败后如何统一处理？|145]]、[[07-项目/01-统一响应与异常处理#431. 什么是全局异常处理？|431]]、[[07-项目/01-统一响应与异常处理#432. `@RestControllerAdvice` 有什么作用？|432]]、[[07-项目/01-统一响应与异常处理#433. `@ExceptionHandler` 有什么作用？|433]]、[[07-项目/01-统一响应与异常处理#438. 唯一约束异常如何转换成友好提示？|438]]。

### 30 秒参考回答

请求到达 `DispatcherServlet` 后，Spring MVC 通过 `HandlerMapping` 找到包含拦截器的执行链，再由 `HandlerAdapter` 完成参数解析、类型转换、数据绑定与校验后调用 Controller。返回值由返回值处理器和 `HttpMessageConverter` 写入响应；处理阶段抛出的异常交给 `HandlerExceptionResolver` 链，例如由 `@RestControllerAdvice` 中的 `@ExceptionHandler` 映射成稳定响应。Filter 位于 Servlet 链外层，不能与 MVC 拦截器混为一层。

### 60 秒扩展要点

- `@RequestBody` 让参数解析器通过匹配的 `HttpMessageConverter` 读取请求体；JSON 常由 Jackson 处理，但它既不表示“只支持 POST”，也不代表所有绑定都走 JSON 或纯反射。
- 查询参数、路径变量、表单和请求体使用的解析/绑定路径不同，缺少参数、类型转换失败、消息不可读和 Bean Validation 失败可能对应不同异常。
- 后端是信任边界：Bean Validation 适合结构与通用约束，业务状态仍需服务层校验，唯一性和引用完整性最终还要数据库约束兜底。
- 全局异常处理应把已知异常映射为稳定错误码和安全信息；未知异常保留日志关联信息，对外不能泄露堆栈、SQL 或内部路径。
- `@RestControllerAdvice` 主要覆盖 MVC 处理链，Filter 中提前抛出的异常、异步线程异常或响应已经提交的情况需要其他边界处理。
- 数据库唯一约束可以兜住并发竞态，但不能把所有 `DataIntegrityViolationException` 都翻译成“数据重复”；应识别稳定约束或明确异常原因。

### 120 秒场景闭环

假设创建员工接口需要区分三类失败：JSON 语法错误、字段校验失败、并发创建相同用户名触发唯一约束。请求体不可读时在进入 Controller 前由消息转换链抛错；对象转换成功但违反注解约束时走校验异常；两个请求都通过“先查是否存在”后，数据库唯一约束仍可能拒绝其中一个。处理方案是在 Advice 中分别映射消息不可读、校验失败和已确认的唯一约束冲突，返回稳定错误码并记录请求标识；不能把未知数据库异常一律伪装成重复。验证用三类请求和一组并发请求检查 Controller 是否执行、事务是否回滚、HTTP 状态/业务码是否稳定以及日志是否保留根因。简历只确认项目使用统一异常处理并处理业务异常和数据库约束异常，具体类名、状态码与异常签名仍需代码核验。

> [!check]- L0～L4 核对
> - L0：说出 `DispatcherServlet → 映射/适配 → 参数 → Controller → 返回值/异常` 主链。
> - L1：解释参数解析、消息转换、校验、返回值处理和异常解析各自职责。
> - L2：能区分 Filter/MVC、不同绑定异常、业务校验和数据库约束边界。
> - L3：对 JSON、校验和并发唯一冲突分别定位并映射，不用一个 catch-all 掩盖根因。
> - L4：验证执行位置、回滚、响应与日志，并守住已确认项目事实范围。

## ③ MySQL：M2 索引、回表与执行计划

知识依据：[[04-MySQL/03-索引#249. 什么是数据库索引？|249]]、[[04-MySQL/03-索引#250. 索引为什么能提高查询速度？|250]]、[[04-MySQL/03-索引#251. 索引有什么缺点？|251]]、[[04-MySQL/03-索引#253. InnoDB 为什么使用 B+Tree？|253]]、[[04-MySQL/03-索引#258. 什么是聚簇索引？|258]]、[[04-MySQL/03-索引#260. 什么是二级索引？|260]]、[[04-MySQL/03-索引#262. 什么是回表？|262]]、[[04-MySQL/03-索引#263. 什么是覆盖索引？|263]]、[[04-MySQL/03-索引#265. 什么是联合索引？|265]]、[[04-MySQL/03-索引#266. 什么是最左匹配原则？|266]]、[[04-MySQL/03-索引#268. 常见的索引失效场景有哪些？|268]]、[[04-MySQL/03-索引#272. 什么是 EXPLAIN？|272]]、[[04-MySQL/03-索引#273. EXPLAIN 中 `type` 表示什么？|273]]、[[04-MySQL/03-索引#274. EXPLAIN 中 `key` 表示什么？|274]]、[[04-MySQL/03-索引#275. EXPLAIN 中 `rows` 表示什么？|275]]、[[04-MySQL/03-索引#276. EXPLAIN 中 `Extra` 有哪些常见信息？|276]]。

### 30 秒参考回答

InnoDB 索引是按页组织的有序 B+Tree 访问结构，用空间和写入维护成本换取更小的扫描范围及有序访问。聚簇索引叶子保存整行，二级索引叶子通常保存索引列和主键；查询二级索引未覆盖的列时，需要按候选主键回到聚簇索引取行。是否使用索引由优化器按成本选择，必须结合实际 SQL、联合索引顺序、数据分布、回表和排序查看 EXPLAIN，不能只看“建了索引”。

### 60 秒扩展要点

- B+Tree 高扇出、路径短、叶子有序，适合点查和范围访问；“减少扫描页/行”比“查询复杂度永远 O(log n)”更接近数据库执行成本。
- 联合索引按列元组排序。最左前缀决定高效定位的主要范围，但范围条件后的列仍可能参与索引条件下推、覆盖、过滤或排序，不能笼统说“全部失效”。
- 覆盖索引是相对具体查询而言，不是某个索引的永久属性；减少回表可能显著受益，但更宽索引会增加空间、写放大和缓存压力。
- 所谓“索引失效”要拆成：不能形成有效 range、仍做 full index scan、或优化器认为全表扫描成本更低；函数、隐式转换、`OR`、`!=`、`IS NULL` 都不能脱离版本、数据和查询形态直接判死刑。
- EXPLAIN 的 `type` 是访问方式，`key` 是实际选择的索引，`rows` 通常是估算扫描量，`Extra` 提供排序、临时表、覆盖等线索；单个字段不能独立证明快慢。
- `EXPLAIN ANALYZE` 会实际执行并给出运行时信息，不能对有副作用或昂贵 SQL 无评估地使用。

### 120 秒场景闭环

假设订单列表按 `user_id`、`status` 过滤并按 `create_time DESC` 分页，已有索引但响应仍慢。先保留真实 SQL、参数分布、返回列和排序/分页方式，再看 EXPLAIN 的访问方式、实际索引、估算行数及 `Using filesort` 等线索；条件允许时在安全数据集上用 `EXPLAIN ANALYZE` 对比估算与实际。若联合索引顺序不能同时缩小过滤范围和支持排序，或查询大量非覆盖列导致成批回表，应根据选择性、排序、页大小与写入成本设计候选索引，而不是机械拼入所有列。还要检查深分页、热点用户和统计信息。验证应比较扫描行/页、回表量、排序、P95/P99 和写入开销，并在相同参数分布下对照；这里是通用调优流程，不能无 SQL、数据量和执行计划就写成个人优化成果。

> [!check]- L0～L4 核对
> - L0：说明索引是访问结构与成本交换，不承诺建了必用或用了必快。
> - L1：说清聚簇、二级、回表、覆盖和联合索引的关系。
> - L2：能拆解“最左匹配”“索引失效”和 EXPLAIN 估算边界。
> - L3：从真实 SQL、参数分布和执行计划推进到候选索引，不先套口诀。
> - L4：以扫描、回表、排序、延迟和写成本做对照验证，并声明场景属性。

## ④ Redis：R2 Cache Aside 与一致性

知识依据：[[05-Redis/03-Redis缓存#330. 什么是 Cache Aside 模式？|330]]、[[05-Redis/03-Redis缓存#331. 查询 Redis + MySQL 的基本流程是什么？|331]]、[[05-Redis/03-Redis缓存#335. 数据库数据发生修改后缓存怎么办？|335]]、[[05-Redis/03-Redis缓存#336. 为什么通常删除缓存而不是更新缓存？|336]]、[[05-Redis/03-Redis缓存#337. 为什么 Redis 和 MySQL 可能出现数据不一致？|337]]、[[05-Redis/03-Redis缓存#338. 先更新数据库再删除缓存有什么问题？|338]]、[[05-Redis/03-Redis缓存#339. 先删除缓存再更新数据库有什么问题？|339]]、[[05-Redis/03-Redis缓存#340. 如何降低缓存和数据库不一致的概率？|340]]、[[05-Redis/03-Redis缓存#341. 什么是延迟双删？|341]]、[[05-Redis/05-RedisTemplate与SpringCache#367. `@Cacheable` 有什么作用？|367]]、[[05-Redis/05-RedisTemplate与SpringCache#368. `@CacheEvict` 有什么作用？|368]]、[[05-Redis/05-RedisTemplate与SpringCache#373. 项目中数据修改后如何清理对应缓存？|373]]。

### 30 秒参考回答

Cache Aside 由应用管理数据库事实源和可重建缓存：读时先查缓存，未命中再读库并按需回填；写时通常先提交数据库，再删除相关缓存，让后续读取重建。它不是强一致协议，因为数据库提交、缓存删除和并发回填不在一个原子事务中。降低风险要把失效放到提交后，并为删除失败提供幂等重试或可靠事件，同时用 TTL 控制错误值存活上限。

### 60 秒扩展要点

- “删缓存通常比更新缓存简单”是常用取舍，不是绝对规则；更新缓存仍要处理派生数据、并发覆盖和多 Key 原子性。
- 先更新数据库再删除缓存存在删除失败；还可能有旧读请求在数据库更新前读到旧值，却在删除完成后才把旧值回填。
- 先删缓存再更新数据库时，并发读可能在事务提交前读取旧库并重新填回旧值，窗口通常更直观。
- 延迟双删只能以概率覆盖某些时序，延迟值难确定；第二次删除若不可靠，不能宣称“彻底一致”。
- 可靠失效可结合事务提交后事件、幂等重试、Outbox 或 CDC；TTL、版本条件、请求合并和监控分别控制残留时间、旧值覆盖与回源压力。
- `@Cacheable` / `@CacheEvict` 依赖代理和 Key/condition 配置；自调用可能绕过代理，方法正常返回也不必然等于外层数据库事务已经提交。

### 120 秒场景闭环

假设修改菜品后偶发读到旧缓存，先还原两条竞态：删除调用失败；或一个慢读在更新前读到旧数据库值，等更新事务提交并删除缓存后才回填旧值。定位时关联数据库提交时刻、缓存 GET/SET/DEL、Key 与 TTL、事务边界、重试记录和请求标识，不能只看最终某次命中。方案先保证失效在事务成功提交后发生，再让删除事件可重试、幂等并可监控；允许短暂最终一致的场景设置合理 TTL，强约束读则考虑绕过缓存、版本校验或更强协调并承担代价。验证用可控并发制造慢读与更新交错，并注入删除失败，检查旧值最长存活时间、重试成功率和数据库回源峰值。简历确认项目使用 RedisTemplate 与 Spring Cache，但具体注解、Key、TTL、提交后失效和重试机制仍需代码核验。

> [!check]- L0～L4 核对
> - L0：说清数据库事实源、读旁路和写后失效，并承认不是强一致。
> - L1：能画出删除失败与慢读旧值回填两条时序。
> - L2：说明删除/更新、延迟双删、TTL、注解代理和事务提交边界。
> - L3：用日志与可控交错定位 Key、事务、SET/DEL 和重试，而不是只清缓存。
> - L4：验证旧值窗口、可靠重试与回源代价，并守住项目实现证据边界。

## ⑤ Web/项目：W1 JWT 认证与请求上下文

知识依据：[[06-Web/01-CookieSessionJWT#385. JWT 是什么？|385]]、[[06-Web/01-CookieSessionJWT#389. Signature 有什么作用？|389]]、[[06-Web/01-CookieSessionJWT#390. JWT 为什么可以实现无状态认证？|390]]、[[06-Web/01-CookieSessionJWT#391. JWT 中的数据是加密的吗？|391]]、[[06-Web/01-CookieSessionJWT#394. JWT 如何判断是否过期？|394]]、[[06-Web/01-CookieSessionJWT#397. JWT 有什么缺点？|397]]、[[06-Web/01-CookieSessionJWT#398. 如何让 JWT 主动失效？|398]]、[[06-Web/01-CookieSessionJWT#399. 项目中用户登录后的 JWT 完整流程是什么？|399]]、[[02-Spring/05-Filter与Interceptor#148. Interceptor 是什么？|148]]、[[02-Spring/05-Filter与Interceptor#154. 为什么 JWT 登录校验适合放在拦截器？|154]]、[[02-Spring/05-Filter与Interceptor#157. 为什么管理端和用户端可以使用两个拦截器？|157]]、[[06-Web/02-ThreadLocal#402. 为什么项目中用 ThreadLocal 保存 userId / empId？|402]]、[[06-Web/02-ThreadLocal#407. 为什么 ThreadLocal 使用完需要 `remove()`？|407]]、[[06-Web/02-ThreadLocal#408. Tomcat 线程池为什么会产生 ThreadLocal 数据残留风险？|408]]。

### 30 秒参考回答

JWT 是承载 Claims 的令牌格式；常见三段式签名令牌是 JWS，签名或 MAC 保护完整性并在密钥模型下验证来源，不会自动加密载荷。请求认证必须限定允许算法和密钥，验证签名以及 `iss`、`aud`、`exp`、`nbf` 等业务相关 Claims，再把已验证身份放入当前同步请求上下文。ThreadLocal 只解决同一线程内传递，建立它的边界必须在所有退出路径清理；认证成功也不等于拥有目标资源权限。

### 60 秒扩展要点

- Base64URL 解码只是在读载荷，不是验签；签名载荷通常可读，因此 Bearer Token 本身应按凭证保护，不能放敏感明文或写入普通日志。
- “无状态”通常只表示请求路径不必按 token 查询服务端 Session；签名密钥、用户状态、撤销信息和 Refresh Token 仍可能需要服务端状态。
- `exp` 是 NumericDate 秒值，但校验不能只看过期时间；还要防算法混淆、错误 issuer/audience、尚未生效和用途混用，并只允许很小的时钟偏差。
- 主动失效可用短期 Access Token、按 `jti`/token hash 的拒绝列表、用户 tokenVersion/not-before、在线 introspection 和 Refresh Token 轮换；每种方案都在状态、延迟和撤销粒度间取舍。
- HandlerInterceptor 属于 MVC 处理链，不是 AOP，也未必覆盖静态资源、Filter 提前结束和所有异步路径；通用生产安全框架常把认证放在 Spring Security Filter Chain。
- 双拦截器可以隔离管理端和用户端的路径、密钥或 Claims 规则，但必须验证路由覆盖、排除项和身份类型，不能只凭“分成两个类”证明安全。

### 120 秒场景闭环

假设线程池复用后用户 B 偶尔读到用户 A 的身份。先检查两个拦截器各自匹配哪些路径、Token 验证后何处写入 ThreadLocal、认证拒绝、Controller 异常、提前返回和异步切换时是否都清理或重新建立；用请求 ID、线程名和已脱敏用户 ID 还原交错，禁止记录完整 Token。修复应让创建上下文的最外层边界用可靠的 `try/finally` 或框架完成回调清理，异步任务显式传递最小身份并在目标线程独立清理；业务层仍要按角色和资源归属授权。最后用同一线程交替处理不同身份、无 Token、异常和异步路径的测试验证隔离。简历只确认 JWT、双拦截器和 ThreadLocal 链路，算法、Claims、有效期、撤销、清理位置与授权规则均待代码核验，串号事故本身也不是已确认经历。

> [!check]- L0～L4 核对
> - L0：分清 JWT、JWS、签名完整性与机密性，并说明验签不是解码。
> - L1：说清 Claims 校验、拦截器请求链和 ThreadLocal 建立/读取/清理。
> - L2：覆盖无状态、撤销、异步传播、路径遗漏和认证/授权边界。
> - L3：从路由、线程与所有退出路径定位身份残留，不把 ThreadLocal 当全局上下文。
> - L4：用交错、异常和异步测试验证，同时逐项标记项目已确认与待核验事实。

## ⑥ 算法：A1 API、Comparator 与输入契约

知识依据：[[12-算法/01-Java算法常用API#628. ArrayList 常用方法有哪些？|628]]、[[12-算法/01-Java算法常用API#629. HashMap 常用方法有哪些？|629]]、[[12-算法/01-Java算法常用API#630. HashSet 常用方法有哪些？|630]]、[[12-算法/01-Java算法常用API#631. Deque 常用方法有哪些？|631]]、[[12-算法/01-Java算法常用API#632. PriorityQueue 如何使用？|632]]、[[12-算法/01-Java算法常用API#633. Arrays 常用方法有哪些？|633]]、[[12-算法/01-Java算法常用API#634. Collections 常用方法有哪些？|634]]、[[12-算法/01-Java算法常用API#635. StringBuilder 常用方法有哪些？|635]]、[[12-算法/01-Java算法常用API#636. Java 如何自定义排序？|636]]。

### 30 秒参考回答

手写算法前先把输入、输出、是否允许修改输入、异常行为和顺序要求写成契约，再选择只提供所需语义的 API。自定义排序的 Comparator 必须保持反对称、传递和一致的比较结果，数值比较用 `Integer.compare` 等方法，不能直接相减导致溢出。还要主动检查集合视图、空值、迭代顺序和方法重载等 API 边界，最后用极值、重复值、空输入和输入不变性测试契约。

### 60 秒扩展要点

- `ArrayList.remove(int)` 与 `remove(Object)` 是重载；`subList` 通常是原列表视图，不是自动复制的独立列表。
- HashMap 的 null 返回可能表示无映射或映射值为 null，HashSet 同样依赖稳定 equals/hashCode；二者默认迭代顺序都不应写进输出契约。
- `ArrayDeque` 不接受 null，并应统一从哪一端入队/出队；PriorityQueue 只保证堆顶和 `poll` 顺序，直接迭代不是整体有序。
- `Arrays.asList` 是由数组支持的定长 List，基本类型数组会成为单个元素；`binarySearch` 必须与排序使用同一顺序。
- `Collections.sort/reverse` 等会修改传入列表，unmodifiable view 不等于深度不可变；StringBuilder 可变且非线程安全，`char` 也不等于完整 Unicode 字素。
- Comparable 定义类型的自然顺序，Comparator 提供外部顺序；树结构里 compare 为 0 可能决定元素是否被视为同一个排序键，即使 equals 不同。

### 120 秒场景闭环

题目要求“接收若干闭区间，返回按起点升序、终点升序排列的深拷贝，不修改输入；null 或任一行长度不是 2 时拒绝”。先把契约写清，再逐行复制，使用两个 `Integer.compare` 做字典序排序。直接写 `a[0] - b[0]` 在 `MIN_VALUE` 与 `MAX_VALUE` 间会溢出并破坏比较器契约；只复制外层数组又会让结果与输入共享行。验证必须覆盖空输入、常规乱序、相同起点、重复区间、整数极值、非法行和返回值修改不影响原数组。排序前复制为 `O(n)`，排序为 `O(n log n)`，结果深拷贝占 `O(n)` 空间。

### Java 参考实现

```java
import java.util.Arrays;

public class IntervalSorter {
    public static int[][] sortedIntervalsCopy(int[][] intervals) {
        if (intervals == null) {
            throw new IllegalArgumentException("intervals must not be null");
        }

        int[][] copy = new int[intervals.length][2];
        for (int i = 0; i < intervals.length; i++) {
            if (intervals[i] == null || intervals[i].length != 2) {
                throw new IllegalArgumentException("each interval must have length 2");
            }
            copy[i][0] = intervals[i][0];
            copy[i][1] = intervals[i][1];
        }

        Arrays.sort(copy, (left, right) -> {
            int byStart = Integer.compare(left[0], right[0]);
            return byStart != 0 ? byStart : Integer.compare(left[1], right[1]);
        });
        return copy;
    }

    private static void checkMatrix(int[][] expected, int[][] actual) {
        if (!Arrays.deepEquals(expected, actual)) {
            throw new AssertionError(
                    "expected=" + Arrays.deepToString(expected)
                            + ", actual=" + Arrays.deepToString(actual));
        }
    }

    private static void expectInvalid(Runnable action) {
        try {
            action.run();
            throw new AssertionError("expected IllegalArgumentException");
        } catch (IllegalArgumentException expected) {
            // 通过
        }
    }

    public static void main(String[] args) {
        checkMatrix(new int[0][], sortedIntervalsCopy(new int[0][]));
        checkMatrix(
                new int[][] {{1, 1}, {1, 2}, {3, 4}},
                sortedIntervalsCopy(new int[][] {{3, 4}, {1, 2}, {1, 1}}));
        checkMatrix(
                new int[][] {{1, 2}, {1, 2}, {1, 3}},
                sortedIntervalsCopy(new int[][] {{1, 3}, {1, 2}, {1, 2}}));
        checkMatrix(
                new int[][] {{Integer.MIN_VALUE, 1}, {0, 0}, {Integer.MAX_VALUE, -1}},
                sortedIntervalsCopy(new int[][] {
                    {Integer.MAX_VALUE, -1}, {0, 0}, {Integer.MIN_VALUE, 1}
                }));

        int[][] input = {{2, 3}, {1, 4}};
        int[][] result = sortedIntervalsCopy(input);
        result[0][0] = 99;
        checkMatrix(new int[][] {{2, 3}, {1, 4}}, input);

        expectInvalid(() -> sortedIntervalsCopy(null));
        expectInvalid(() -> sortedIntervalsCopy(new int[][] {{1}}));
        expectInvalid(() -> sortedIntervalsCopy(new int[][] {null}));
    }
}
```

> [!check]- L0～L4 核对
> - L0：先说输入、输出、修改性、异常和顺序契约，再选择 API。
> - L1：解释深拷贝和字典序 Comparator，并知道 `poll` 顺序、视图与迭代顺序边界。
> - L2：主动防比较器溢出、浅拷贝、重载误用、null 与不确定顺序。
> - L3：能手写、编译并运行覆盖空值、重复值、极值、非法输入和输入不变性的测试。
> - L4：证明 Comparator 按起点和终点构成全序，给出 `O(n log n)` 时间与 `O(n)` 额外空间。

## 核对结果回流

| 学习线 | 母题 | 与参考答案的首个差异 | 失败层 | 等级 | 最小修复节点 |
| --- | --- | --- | --- | --- | --- |
| ① Java 基础与运行时 | J1 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ② Spring 与数据访问 | S2 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ③ MySQL | M2 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ④ Redis | R2 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ⑤ Web、项目与工程 | W1 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ⑥ 算法与手写 | A1 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |

完成核对后，只把真实首个断点同步到 [[00-第二轮学习队列#六线证据队列]]。没有本组首答记录时，本卡只表示答案参考已覆盖，不表示这些母题已通过或属于个人弱项。
