---
category: 学习任务
phase: 第一轮
day: 11
status: 待开始
tags:
  - Java后端
  - 求职
  - 闭卷考核
---

# 第十一天学习任务与闭卷题单

> Day 11 继续按各线真实进度推进：Java、Spring、Web/工程各学习 10 道新题；MySQL 训练深分页治理，Redis 训练内存与故障恢复；算法线进入动态规划与零钱兑换。

导航：[[00-总目录]] · [[00-第一轮学习队列]] · [[00-第十天学习任务与闭卷题单]] · [[00-每日学习与闭卷考核模板]]

## 开始前

- [ ] 复制 [[00-每日学习与闭卷考核模板]]，以当天日期命名。
- [ ] 从 Day 10 记录中为每条学习线选择一个 `不会` 或 `不稳` 断点。
- [ ] 如果某条线连续失败题超过 2 道，本日先修复最靠前的核心断点，再决定是否执行综合场景。
- [ ] Day 10 尚未执行时，先完成 [[00-第十天学习任务与闭卷题单]]。

## Day 10 回流槽

| 学习线 | 回流题号 | Day 10 结果 | 15 分钟修复动作 | 修复后结果 |
| --- | --- | --- | --- | --- |
| ① Java 基础与运行时 |  | 不会 / 不稳 / 通过 |  |  |
| ② Spring 与数据访问 |  | 不会 / 不稳 / 通过 |  |  |
| ③ MySQL |  | 不会 / 不稳 / 通过 |  |  |
| ④ Redis |  | 不会 / 不稳 / 通过 |  |  |
| ⑤ Web、项目与工程 |  | 不会 / 不稳 / 通过 |  |  |
| ⑥ 算法与手写 |  | 不会 / 不稳 / 通过 |  |  |

## 白天六线任务

| 学习线 | 今日范围 | 新题量 / 实战 | 有效输出 |
| --- | --- | ---: | --- |
| ① Java 基础与运行时 | [[08-JVM/01-JVM基础]] 题 476～484；[[08-JVM/02-GC与类加载]] 题 485 | 10 / 0 | 串联共享内存区、堆栈分工、程序计数器、方法区/元空间、对象创建和两类内存错误，再说明 GC 解决的问题 |
| ② Spring 与数据访问 | [[02-Spring/08-Spring事务]] 题 197～200；[[03-MyBatis/01-MyBatis]] 题 201～206 | 10 / 0 | 收口事务方法可见性、传播和项目主子表原子写入，再建立 MyBatis、JDBC、Mapper 代理与 XML/注解映射的调用链 |
| ③ MySQL | 回查 [[04-MySQL/01-SQL基础#238. 深分页为什么可能比较慢？|238]]、[[04-MySQL/03-索引#263. 什么是覆盖索引？|263]]、[[04-MySQL/03-索引#267. 联合索引 `(a,b,c)` 哪些查询可以使用索引？|267]]、[[04-MySQL/03-索引#272. 什么是 EXPLAIN？|272]]、[[04-MySQL/03-索引#275. EXPLAIN 中 `rows` 表示什么？|275]]、[[04-MySQL/03-索引#276. EXPLAIN 中 `Extra` 有哪些常见信息？|276]]；完成深分页治理演练 | 0 / 1 场景 | 从扫描并丢弃前置结果解释深分页成本，提出稳定复合游标和候选索引，并用执行计划与真实数据验证 |
| ④ Redis | 回查 [[05-Redis/02-过期与内存管理#323. 什么是定期删除？|323]]、[[05-Redis/02-过期与内存管理#326. 什么是内存淘汰策略？|326]]、[[05-Redis/02-过期与内存管理#327. Redis 有哪些常见的内存淘汰策略？|327]]、[[05-Redis/06-持久化#377. RDB 和 AOF 有什么区别？|377]]、[[05-Redis/06-持久化#380. Redis 重启后如何恢复数据？|380]]、[[05-Redis/07-Redis进阶#583. Redis 主从复制是什么？|583]]、[[05-Redis/07-Redis进阶#585. Redis Sentinel 是什么？|585]]、[[05-Redis/07-Redis进阶#586. Redis Cluster 是什么？|586]]；完成内存与恢复演练 | 0 / 1 场景 | 区分过期与淘汰、RDB/AOF 的 RPO/RTO、复制与故障转移、Sentinel 与 Cluster，所有版本和部署结论保留核验边界 |
| ⑤ Web、项目与工程 | [[06-Web/05-Web开发常识]] 题 532～541 | 10 / 0 | 区分 Entity、DTO、VO 和接口边界，串联参数校验、分页、接口幂等以及重复提交订单的业务风险 |
| ⑥ 算法与手写 | 回流 [[12-算法/02-算法基础#649. 动态规划的基本思路是什么？|题 649]]；零钱兑换最少硬币数 | 0 + 实战 | 从状态定义、base case、转移来源和计算顺序构造一维 DP，说明不可达哨兵、完全背包语义和复杂度 |

> MySQL 与 Redis 本日仍为综合实战，不增加编号题，也不重复计算覆盖量。场景答案需要给出可验证证据、适用边界和失败后的处理，不能只列技术名词。

### MySQL 线综合场景

给定订单查询：

```sql
SELECT id, user_id, amount, created_at
FROM orders
WHERE status = ?
ORDER BY created_at DESC, id DESC
LIMIT 100000, 20;
```

必须完成：

1. 解释大 offset 为什么通常仍要访问并丢弃大量前置结果，成本如何受过滤率、排序、覆盖索引和回表影响。
2. 提出以 `(created_at, id)` 为稳定复合游标的 Keyset 分页写法，说明它适合连续翻页但不直接支持任意跳页。
3. 为 `status + created_at + id` 提出候选联合索引；指出 InnoDB 二级索引叶子包含主键，但查询 `user_id`、`amount` 仍可能回表，不能无代价地把所有列都加入索引。
4. 用传统 `EXPLAIN` 和安全环境中的 `EXPLAIN ANALYZE` 检查扫描行数、排序、回表与实际耗时；优化前后都要在代表性数据上测量。

### Redis 线综合场景

假设 Redis 实例保存约两百万个缓存 Key，大多数带 TTL。近期 `used_memory` 接近 `maxmemory`，`evicted_keys` 上升；业务还要求实例重启后尽量缩小数据恢复窗口，并希望主节点故障时自动切换。

必须完成：

1. 区分惰性/定期过期删除和 `maxmemory` 淘汰，解释未过期 Key 为什么也可能被淘汰。
2. 根据纯缓存、冷热分布和回源承载能力比较 `noeviction`、`allkeys-lru`、`allkeys-lfu` 与 volatile 类策略；不得脱离 Redis 版本和真实配置背固定答案。
3. 从可接受数据丢失窗口、恢复时间、fork/写时复制、持续刷盘和备份方式比较 RDB、AOF 或组合方案。
4. 区分主从复制、Sentinel 和 Cluster：副本提供复制基础，Sentinel 管理非分片拓扑故障转移，Cluster 同时涉及分片；三者都不能推出强一致或零丢写。
5. 列出至少六项证据：`used_memory`、RSS/碎片率、`evicted_keys`、`expired_keys`、持久化状态、复制延迟、命中率或数据库回源峰值，并说明指标异常后的处置顺序。

### 算法线编码要求

实现 `int coinChange(int[] coins, int amount)`。`coins` 非 `null`、长度至少为 1，面额互不相同且均为正整数；`0 <= amount <= 10000`，每种硬币可使用任意次。返回凑成金额所需的最少硬币数，无法凑成时返回 `-1`，不得修改输入数组。

至少验证：

```text
coins = [1, 2, 5], amount = 11  -> 3
coins = [2], amount = 3         -> -1
coins = [2], amount = 0         -> 0
coins = [2, 5, 10, 1], amount = 27 -> 4
coins = [3, 7], amount = 14     -> 2
```

完成代码后必须口述：`dp[x]` 的精确定义、`dp[0]` 为什么为 0、转移 `dp[x] = min(dp[x], dp[x - coin] + 1)` 的成立条件、不可达状态为什么不能直接加 1，以及时间复杂度 `O(amount × coins.length)`、额外空间 `O(amount)`。还要说明本题允许重复使用硬币，与 0/1 背包的遍历语义不同。

### 每线完成条件

- [ ] Day 10 回流题已重新闭卷回答，并记录修复后的结果。
- [ ] Java、Spring/MyBatis、Web/工程的新题至少完成一次脱稿回答或职责链输出。
- [ ] MySQL 场景包含原查询成本、复合游标、候选索引和执行计划证据。
- [ ] Redis 场景明确区分过期、淘汰、持久化、复制、故障转移和分片。
- [ ] 接口幂等没有被简化为响应完全相同、HTTP 方法名或单个分布式锁。
- [ ] 零钱兑换代码通过全部用例，并能解释状态、转移和不可达哨兵。

## 晚间闭卷考核题单

建议总时长 60～90 分钟。完成前关闭原笔记，结果写入当天的 [[00-每日学习与闭卷考核模板#晚间闭卷考核]]。

### ① Java 基础与运行时

[[08-JVM/01-JVM基础#482. `new` 一个 Java 对象大致经历什么过程？|题 482：new 一个 Java 对象大致经历什么过程？]]

- 主问题：从类解析/初始化、内存分配、默认零值、对象头到构造器调用说明典型过程。
- 追问 1：`new` 指令、`<clinit>` 和 `<init>` 分别是什么关系？
- 追问 2：为什么 TLAB、对象头布局、分代位置和逃逸分析不能被说成 JVM 规范固定步骤？

### ② Spring 与数据访问

[[03-MyBatis/01-MyBatis#204. Mapper 接口没有实现类为什么可以执行？|题 204：Mapper 接口没有实现类为什么可以执行？]]

- 主问题：从 JDK 动态代理、MapperProxy/MapperMethod、SqlSession 和 MappedStatement 说明调用链。
- 追问 1：statement 通常如何定位，缺少绑定或返回类型不匹配会怎样？
- 追问 2：Mapper 代理负责 SQL 分派，为什么不等于它负责事务决策或就是 SQL 缓存？

### ③ MySQL

[[04-MySQL/01-SQL基础#238. 深分页为什么可能比较慢？|题 238：深分页为什么可能比较慢？]]

- 主问题：说明 `LIMIT offset, size` 访问并丢弃前置结果的成本来源。
- 场景追问：对白天订单查询如何设计稳定的复合游标，为什么单独写 `id < lastId` 不一定正确？
- 边界追问：Keyset 分页解决了什么，又牺牲了什么；为什么仍需执行计划和真实数据压测？

### ④ Redis

[[05-Redis/02-过期与内存管理#326. 什么是内存淘汰策略？|题 326：什么是内存淘汰策略？]]

- 主问题：说明触发条件、候选 Key 范围、选择规则和 `noeviction` 的含义。
- 场景追问：纯缓存实例如何在 LRU、LFU 或其他策略间选择，应监控哪些结果？
- 边界追问：为什么淘汰不等于过期，volatile 策略也不能当作重要数据的可靠保护？

### ⑤ Web、项目与工程

[[06-Web/05-Web开发常识#539. 什么是接口幂等？|题 539：什么是接口幂等？]]

- 主问题：从重复执行后的预期业务效果定义幂等，不要求每次响应字节完全相同。
- 追问 1：POST 能否设计为幂等，PUT/DELETE 的方法语义为什么仍不能替代正确实现？
- 追问 2：幂等键、唯一约束、条件更新、状态机和分布式锁分别能覆盖哪些边界？

### ⑥ 算法与手写

[[12-算法/02-算法基础#649. 动态规划的基本思路是什么？|题 649：动态规划的基本思路是什么？]]

- 主问题：按状态、转移、base case、计算顺序和答案位置说明 DP 解题过程。
- 手写：重新实现今天的零钱兑换，不能查看白天代码。
- 追问：为什么本题不是贪心地总取最大面额？不可达状态若使用 `Integer.MAX_VALUE`，转移时要防止什么问题？

## 评分规则

Java、Spring/MyBatis、Web/工程题每题 10 分：核心结论 2 分、原理链路 2 分、成立条件与边界 2 分、两层追问 2 分、场景或工程影响 2 分。

MySQL、Redis 场景题每题 10 分：核心链路 2 分、证据或监控 2 分、故障边界 2 分、方案取舍 2 分、项目真实性 2 分。

算法题 10 分：函数契约 1 分、状态与 base case 2 分、转移与计算顺序 3 分、代码和返回值 2 分、复杂度与边界 2 分。

总分 60 分：

- 48～60：整体通过；单题不足 8 分仍记为 `不稳`。
- 36～47：Day 12 保留对应回流槽，并减少失败学习线的新题量或场景复杂度。
- 0～35：暂停薄弱线扩题，优先修复重复失败的知识链。

## 考后动作

- [ ] 对照原笔记纠错，记录首个错误断点。
- [ ] 将六道题分别标记为 `不会` / `不稳` / `通过`。
- [ ] 仅根据闭卷证据更新相关知识笔记的 `status`。
- [ ] MySQL 与 Redis 场景未通过时，下一日继续修复失败链，不用新的场景掩盖问题。
- [ ] 把连续失败题和 DP 状态/转移的首个错误加入 Day 12 回流。
- [ ] 原笔记疑点单独记录，核验可靠资料后再创建维护 PR。
