---
category: 追问训练答案
phase: 第二轮
group: 扩展主干
status: 可使用
tags:
  - Java后端
  - 求职
  - P0
  - 答案核对
---

# P0 扩展主干答案核对卡

> 本卡覆盖 J2 / S3 / M1 / R1 / W2 / A3 六条扩展主干，只在闭卷首答后使用。它补充通用参考答案，不代表这些内容是个人弱项。

导航：[[00-总目录]] · [[00-P0母题与追问链地图]] · [[00-P0母题答案核对入口]] · [[00-追问链训练模板]] · [[00-第二轮学习队列]]

## 使用规则

1. 先独立完成 30 / 60 / 120 秒首答、L0～L4 追问与算法运行，再打开对应部分。
2. 示例答案用于核对结论、机制、边界、场景和证据，不要求逐字复述。
3. 只记录第一个使答案失效的断点；没有真实记录时，不能预填等级或更新掌握状态。
4. 项目相关内容继续区分简历已确认事实、待代码核验实现和通用工程增强，不补造故障与优化成果。
5. 核对后只把最小修复节点回流 [[00-第二轮学习队列#六线证据队列]]。

## ① Java：J2 集合选择、迭代与并发修改

知识依据：[[01-Java基础/04-集合#42. Java 常用集合有哪些？|42]]、[[01-Java基础/04-集合#43. List、Set、Map 有什么区别？|43]]、[[01-Java基础/04-集合#45. ArrayList 和 LinkedList 有什么区别？|45]]、[[01-Java基础/04-集合#50. ArrayList 是线程安全的吗？|50]]、[[01-Java基础/04-集合#52. LinkedList 插入一定比 ArrayList 快吗？|52]]、[[01-Java基础/04-集合#53. HashSet 为什么可以去重？|53]]、[[01-Java基础/04-集合#56. Iterator 是什么？|56]]、[[01-Java基础/04-集合#58. 什么是 ConcurrentModificationException？|58]]、[[01-Java基础/04-集合#59. 为什么 foreach 遍历集合时修改集合可能报错？|59]]、[[01-Java基础/04-集合#60. Comparable 和 Comparator 有什么区别？|60]]、[[01-Java基础/05-HashMap#78. HashMap 和 ConcurrentHashMap 有什么区别？|78]]。

### 30 秒参考回答

集合先按业务语义选择：List 表示有位置和明确迭代顺序的序列，通常允许重复；Set 表示按相等或排序规则唯一的成员；Map 表示唯一 Key 到 Value 的映射；FIFO/LIFO 优先看 Queue/Deque。实现类再决定顺序、空值、复杂度和并发契约。遍历中需要删除时使用该迭代器支持的 `remove`、`removeIf` 或构建新集合，不能在增强 for 中随意调用集合的结构修改方法。

### 60 秒扩展要点

- “Set 无序”不准确：HashSet 不保证迭代顺序，LinkedHashSet 保持 encounter order，TreeSet 按排序规则组织。
- ArrayList 适合索引访问和多数顺序处理，尾部追加是均摊 O(1)；LinkedList 只有在已定位节点或两端操作时才省去元素搬移，按索引定位仍是 O(n)。
- HashSet 能否正确去重取决于 equals/hashCode 或排序契约；元素入集合后修改参与相等或排序的字段会破坏查找语义。
- Iterator 的 `remove` 删除最近一次 `next` 返回的元素，属于可选操作且有调用时序；不是所有迭代器都支持。
- fail-fast 通过修改计数尽早暴露意外结构变化，但只是 best effort 错误检测，不能作为线程安全、同步或必然抛异常的保证。
- `Collections.synchronizedXxx` 的复合操作与遍历仍需按同一锁契约保护；ConcurrentHashMap 的单个操作并发安全也不等于任意“先查再改”组合自动原子。

### 120 秒场景闭环

假设单线程代码用增强 for 遍历订单列表，并在命中条件时调用 `orders.remove(order)`，运行中抛出 `ConcurrentModificationException`。先确认是否发生结构修改、修改来自当前循环还是回调/其他线程，以及迭代器类型；不要通过 catch 后继续或复制一份列表来掩盖语义。若只是单线程过滤，可用 `removeIf`、显式 Iterator.remove 或一次构建新列表；若同一实例确实被多线程共享，则先定义一致性和原子操作要求，再选择外部锁、快照型集合或并发容器。最后覆盖连续命中、首尾删除、空集合和并发交错测试，并确认结果顺序与重复语义。这个例子是通用集合诊断，不是已确认项目事故。

> [!check]- L0～L4 核对
> - L0：能从顺序、重复、映射、队列语义选接口，而不是先背实现类。
> - L1：解释 ArrayList/LinkedList 的定位与修改成本，以及 Iterator 的受控删除。
> - L2：指出 Set 顺序、可变元素、fail-fast best effort 和复合并发操作边界。
> - L3：区分当前迭代器修改、外部结构修改和真正的多线程共享后再选修复。
> - L4：用顺序、重复、连续删除和并发交错验证，且不把通用异常写成个人经历。

## ② Spring：S3 AOP 代理与公共字段自动填充

知识依据：[[02-Spring/06-AOP#158. 什么是 AOP？|158]]、[[02-Spring/06-AOP#163. 什么是 Pointcut？|163]]、[[02-Spring/06-AOP#164. 什么是 Advice？|164]]、[[02-Spring/06-AOP#167. Spring AOP 底层大致如何实现？|167]]、[[02-Spring/06-AOP#168. JDK 动态代理和 CGLIB 有什么区别？|168]]、[[02-Spring/06-AOP#171. 为什么 Spring 事务可以通过 AOP 实现？|171]]、[[02-Spring/07-反射#175. Method 对象是什么？|175]]、[[02-Spring/07-反射#177. `Method.invoke()` 有什么作用？|177]]、[[02-Spring/07-反射#180. 项目的 AutoFill 为什么需要反射？|180]]、[[02-Spring/07-反射#181. 为什么不直接调用实体类 setter？|181]]。

### 30 秒参考回答

AOP 把日志、事务、审计字段等横切逻辑从业务方法中抽离。Spring AOP 通常为匹配 Pointcut 的 Bean 创建 JDK 接口代理或 CGLIB 类代理，调用经代理进入 Advice 链后再到目标方法。公共字段自动填充可以在切面中识别写操作和实体参数，再通过共同接口或经过校验的反射调用 setter；关键前提是调用经过代理、切点准确、参数和审计字段契约明确。

### 60 秒扩展要点

- JoinPoint 是运行时可增强位置，Pointcut 选择位置，Advice 定义何时做什么，Aspect 把切点与通知模块化；不能把这些概念都等同于“切面”。
- Spring 自动代理创建器通常在 Bean 后处理阶段包装匹配对象；同类中的普通 `this` 自调用不经过外层代理，手工 `new` 的对象也不受容器切面管理。
- JDK 代理实现接口，CGLIB 类代理生成子类；配置可以强制类代理，因此“有接口必然 JDK”不成立。final 类、final/private 方法等不能被子类代理覆盖。
- Around 通知必须正确调用 `proceed` 并保留异常语义；通知顺序还会影响事务、缓存和重试的嵌套边界。
- 反射按运行时类型查 Method 并调用只是实现选择。统一 `Auditable` 接口、ORM 审计或 MyBatis 插件也可能更类型安全。
- `Method.invoke` 会检查目标、参数与访问条件，业务异常常被 `InvocationTargetException` 包装；不能吞掉原始 cause，也不能假设 `setAccessible` 永远成功。

### 120 秒场景闭环

假设新增一个批量写入方法后，部分实体的 `updateTime` 没有填充。先检查方法是否由 Spring Bean 的代理入口调用、切点注解是否命中、方法可见性与代理配置；再核对通知拿到的是单实体、集合还是包装 DTO，操作类型、当前用户来源、setter 名称和参数类型是否符合约定。若约定长期稳定，可在启动期验证并缓存 Method；若模型可控，优先让实体实现共同审计接口，减少字符串和运行期错误。还要测试单条/批量、创建/更新、无身份任务、异常回滚和自调用路径。简历确认公共字段自动填充这一能力，但具体注解、参数位置、代理类型和反射实现仍需仓库代码核验。

> [!check]- L0～L4 核对
> - L0：分清 Pointcut、Advice、Aspect，并说明增强发生在代理调用链。
> - L1：能解释代理创建、拦截器链、JDK/CGLIB 与反射调用。
> - L2：覆盖自调用、final/private、通知顺序、批量参数和反射异常边界。
> - L3：从代理入口、切点、参数契约、身份来源和 setter 逐层定位漏填。
> - L4：用单条/批量/异常/自调用测试验证，并区分简历事实与待核验实现。

## ③ MySQL：M1 SQL 结果语义、连接与分页

知识依据：[[04-MySQL/01-SQL基础#225. SQL 的执行顺序是什么？|225]]、[[04-MySQL/01-SQL基础#226. `WHERE` 和 `HAVING` 有什么区别？|226]]、[[04-MySQL/01-SQL基础#227. `GROUP BY` 有什么作用？|227]]、[[04-MySQL/01-SQL基础#228. `ORDER BY` 有什么作用？|228]]、[[04-MySQL/01-SQL基础#229. `LIMIT` 如何实现分页？|229]]、[[04-MySQL/01-SQL基础#230. INNER JOIN 是什么？|230]]、[[04-MySQL/01-SQL基础#231. LEFT JOIN 是什么？|231]]、[[04-MySQL/01-SQL基础#232. INNER JOIN 和 LEFT JOIN 有什么区别？|232]]、[[04-MySQL/01-SQL基础#233. 什么是子查询？|233]]、[[04-MySQL/01-SQL基础#234. `COUNT(*)` 和 `COUNT(column)` 有什么区别？|234]]、[[04-MySQL/01-SQL基础#238. 深分页为什么可能比较慢？|238]]。

### 30 秒参考回答

SQL 先保证结果语义，再讨论执行计划。`FROM/JOIN → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT` 是理解名称和结果的逻辑顺序，不是优化器固定的物理步骤。JOIN 按匹配关系生成行组合，一对多会放大行数；LEFT JOIN 的右表条件放在 WHERE 还可能过滤补 NULL 行。分页必须有确定性 ORDER BY，且 LIMIT 截取的是连接后的结果行，不是自动按父对象分页。

### 60 秒扩展要点

- 优化器可以重排连接、下推条件或改写子查询，所以不能用逻辑处理口诀推断实际访问顺序；性能仍要看执行计划和运行指标。
- WHERE 过滤分组前的输入行，HAVING 过滤分组结果；能否下推取决于语义等价，聚合条件不能机械搬到 WHERE。
- GROUP BY 不保证顺序；启用 `ONLY_FULL_GROUP_BY` 时，非聚合表达式要满足分组或可判定的函数依赖规则。
- INNER JOIN 只保留匹配组合，LEFT JOIN 还保留未匹配左行；重复连接键可让结果多于任一输入表，不能描述为简单集合交集。
- LEFT JOIN 后 `COUNT(*)` 会统计补 NULL 的那一行，统计匹配子项时通常应 COUNT 右表非空主键。
- OFFSET 深分页通常要访问并丢弃大量前置结果；Keyset/Seek 分页依赖稳定且最好唯一的排序键，但不支持任意跳页。

### 120 秒场景闭环

假设页面要展示 10 个订单及各自明细，却直接对 `orders LEFT JOIN order_detail` 的结果执行 `ORDER BY create_time DESC LIMIT 10`，最终只得到 4 个订单。原因是 LIMIT 截取 10 条连接结果行，一个订单的多条明细会占据多个名额。修复可先按 `create_time, id` 的稳定顺序分页取得 10 个订单 ID，再批量查询明细并组装；或用适合语义的聚合方案，但要评估字段和成本。总数统计也应明确是订单数还是连接行数，必要时单独 COUNT 父表。验证覆盖无明细、单明细、多明细、相同时间和翻页交界，并检查 SQL、执行计划和查询次数。简历确认项目有分页查询，但未确认具体 SQL、PageHelper 或主子表分页实现，不能把该修复写成真实优化成果。

> [!check]- L0～L4 核对
> - L0：说明逻辑顺序不等于物理执行，并把分页对象定义清楚。
> - L1：解释 JOIN 基数、LEFT JOIN 的 ON/WHERE、聚合与 LIMIT 结果行语义。
> - L2：覆盖稳定排序、COUNT NULL 语义、深分页和子查询改写边界。
> - L3：能定位“一页父对象不足”是连接放大后再 LIMIT，而不是盲目调 pageSize。
> - L4：验证零/一/多明细与翻页交界，核对执行计划并守住项目证据范围。

## ④ Redis：R1 数据结构、Key、过期与内存

知识依据：[[05-Redis/01-Redis基础#305. Redis 是什么？|305]]、[[05-Redis/01-Redis基础#307. Redis 为什么快？|307]]、[[05-Redis/01-Redis基础#308. Redis 是完全单线程的吗？|308]]、[[05-Redis/01-Redis基础#310. 什么是 IO 多路复用？|310]]、[[05-Redis/01-Redis基础#311. Redis 常见的五种基本数据类型是什么？|311]]、[[05-Redis/01-Redis基础#317. 什么是 TTL？|317]]、[[05-Redis/01-Redis基础#319. Redis 中 Key 应该如何设计？|319]]、[[05-Redis/02-过期与内存管理#320. Redis 中的数据过期后会立即删除吗？|320]]、[[05-Redis/02-过期与内存管理#321. Redis 有哪些过期删除策略？|321]]、[[05-Redis/02-过期与内存管理#325. Redis 内存满了怎么办？|325]]、[[05-Redis/02-过期与内存管理#326. 什么是内存淘汰策略？|326]]、[[05-Redis/02-过期与内存管理#328. LRU 是什么？|328]]、[[05-Redis/02-过期与内存管理#329. LFU 是什么？|329]]。

### 30 秒参考回答

Redis 是以内存数据集为核心的数据结构服务器，类型选择要匹配访问操作，Key 要表达业务命名空间、身份和版本，缓存通常设置有依据的 TTL。Redis 低延迟来自内存访问、高效结构、事件驱动和 I/O 多路复用等组合，不是“完全只有一个线程”。TTL 到期后正常命令不会把值当作有效数据返回，但物理清理由被动与主动过期渐进完成；达到 maxmemory 后的淘汰是另一套容量机制。

### 60 秒扩展要点

- String、Hash、List、Set、ZSet 分别适合简单值/计数、字段对象、顺序序列、唯一成员和带 score 排序集合；应从需要的原子操作选择，而不是只看数据外形。
- Redis 经典命令执行路径主要串行化，但持久化、复制、释放内存和部分 I/O 可由其他线程/进程参与；Redis 6+ I/O 线程也不等于命令可以任意并行修改数据。
- I/O 多路复用让一个线程观察多个连接的就绪事件，它不是“一个连接一个线程”，也不能消除慢命令、大 Value 和网络带宽成本。
- Key 应短而可读、避免冲突并包含稳定业务标识；集群多 Key 操作还要考虑 hash tag，批量扫描不能在生产上直接依赖阻塞式 `KEYS`。
- 过期采用访问时的被动检查与受预算的主动采样清理，不是到点全库立即删除；过期、显式 DEL/UNLINK 和内存淘汰是不同机制。
- LRU/LFU 都是采样近似；LFU 还是概率计数加衰减。策略要与访问分布匹配，默认 noeviction 或 volatile 无候选时，增长型写入可能直接报错。

### 120 秒场景闭环

假设缓存命中率看似较高，但 Redis P99、内存和 `evicted_keys` 同时上升，数据库回源也开始增加。先关联 `used_memory`/RSS/碎片、maxmemory 与 policy、Key 数和 TTL 分布、Value 大小、热 Key、慢日志、命令耗时、网络吞吐及淘汰/过期速率，区分大 Value、热点单 Key、集中到期、容量不足和慢命令。方案可能是限制 Value 与批量响应、拆分热 Key、打散合理 TTL、调整数据结构或按真实访问分布选择 LRU/LFU，并为系统开销预留内存；不能只把 maxmemory 调大或切换 policy。验证观察 P95/P99、命中率、淘汰、回源峰值和 RSS。现有简历只确认使用 RedisTemplate 与 Spring Cache，Redis 版本、Key、TTL、maxmemory、淘汰策略和调优结果都待配置与代码核验。

> [!check]- L0～L4 核对
> - L0：分清数据结构服务器、TTL 逻辑过期、物理清理和容量淘汰。
> - L1：解释类型选择、事件循环/多路复用、Key/TTL 与近似 LRU/LFU。
> - L2：主动否定“完全单线程、到期立即删除、内存满必淘汰且不会 OOM”。
> - L3：从内存、Key/Value、命令、TTL、淘汰和回源证据定位，而不是只改一个参数。
> - L4：以延迟、命中、淘汰、回源和 RSS 验证，并标记所有待核验项目配置。

## ⑤ Web/项目：W2 HTTP 语义、幂等与网络链路

知识依据：[[06-Web/03-HTTP#411. HTTP 是什么？|411]]、[[06-Web/03-HTTP#416. GET 和 POST 有什么区别？|416]]、[[06-Web/03-HTTP#417. PUT 和 POST 有什么区别？|417]]、[[06-Web/03-HTTP#418. PUT 和 PATCH 有什么区别？|418]]、[[06-Web/03-HTTP#419. 什么是 HTTP 方法幂等？|419]]、[[06-Web/03-HTTP#420. 哪些 HTTP 方法通常具有幂等性？|420]]、[[06-Web/03-HTTP#424. 401 和 403 有什么区别？|424]]、[[06-Web/03-HTTP#427. HTTP 和 HTTPS 有什么区别？|427]]、[[06-Web/03-HTTP#428. HTTPS 为什么更安全？|428]]、[[06-Web/04-TCP与网络#523. TCP 和 UDP 有什么区别？|523]]、[[06-Web/04-TCP与网络#524. TCP 为什么是可靠协议？|524]]、[[06-Web/04-TCP与网络#525. TCP 三次握手过程是什么？|525]]、[[06-Web/04-TCP与网络#529. 什么是 TIME_WAIT？|529]]、[[06-Web/04-TCP与网络#530. DNS 是什么？|530]]、[[06-Web/04-TCP与网络#531. 浏览器输入 URL 后大致发生了什么？|531]]。

### 30 秒参考回答

HTTP 定义资源请求与响应的应用语义，版本可以运行在 TCP 或 QUIC 上。方法幂等指重复相同请求的预期服务器效果与一次相同，不要求响应和日志完全一致；GET/HEAD 等 safe 方法以及 PUT、DELETE 按标准是幂等的，POST/PATCH 默认不能这样假设。网络超时只说明客户端没拿到结果，不能证明服务端未执行，所以非幂等写请求必须用稳定业务键、唯一约束或状态查询控制重试。

### 60 秒扩展要点

- GET 是 safe、幂等的资源获取语义；POST 让目标资源处理内容，不只等于“新增”。参数位置、长度和安全性不能简化成 URL 对 Body。
- PUT 面向客户端确定 URI 的创建/替换并定义为幂等；PATCH 的幂等性取决于具体补丁格式和操作，“设置为值”与“累加值”不同。
- 幂等不等于无副作用或响应完全相同；DELETE 第一次 204、之后 404 仍可保持目标资源不存在的效果。
- HTTPS 用 TLS 提供传输机密性、完整性和对端认证基础，但不能自动防 SQL 注入、XSS、越权、终端泄露或错误日志。
- TCP 提供可靠有序字节流，不保留应用消息边界；可靠传输也无法让一次 HTTP 写操作与客户端响应接收成为原子事件。
- DNS、TCP/QUIC、TLS 和连接复用存在缓存与协议分支；浏览器访问不保证每次都重新 DNS、三次握手、TLS，再发一个请求。

### 120 秒场景闭环

假设客户端 POST 创建订单后超时并自动重试。可能的时序是首个请求已在数据库提交，但响应在网络中丢失；第二次请求若重新创建，就产生重复副作用。客户端应为一次业务意图生成稳定请求号，重试时复用；服务端用唯一约束或幂等记录，把请求号与订单结果置于可靠事务边界，重复请求返回已有结果。若状态仍不确定，可提供按请求号查询，而不是因为 TCP 可靠就推断“超时等于没执行”。验证需要并发相同请求号、提交前失败、提交后断开和响应丢失等故障注入，确认只产生一个订单且重试得到稳定业务结果。项目虽有 HTTP 下单与支付链路，但幂等键、重试协议和故障演练在代码证据前只能作为通用方案。

> [!check]- L0～L4 核对
> - L0：定义 HTTP 幂等的“预期服务器效果”，并知道超时不等于未执行。
> - L1：说清 GET/POST/PUT/PATCH/DELETE 语义和 TCP/TLS/DNS 的分层职责。
> - L2：覆盖响应可不同、业务幂等、连接复用、HTTP/3 与 HTTPS 非万能边界。
> - L3：针对提交前后、响应丢失和并发重试设计稳定业务键与状态查询。
> - L4：通过故障注入验证唯一副作用和结果复用，并守住项目实现证据。

## ⑥ 算法：A3 栈队列、树图与搜索

知识依据：[[12-算法/02-算法基础#639. 栈和队列常见题型有哪些？|639]]、[[12-算法/02-算法基础#644. BFS 的基本写法是什么？|644]]、[[12-算法/02-算法基础#645. DFS 的基本写法是什么？|645]]、[[12-算法/02-算法基础#646. 回溯的基本模板是什么？|646]]、[[12-算法/02-算法基础#647. 二叉树常见遍历方式有哪些？|647]]。

### 30 秒参考回答

栈适合最近未匹配状态、嵌套结构和深度搜索，队列适合先进先出、层序与广度搜索。无权或每条边代价相同的图中，BFS 按层扩展，第一次到达状态的层数就是最少边数；要在入队时标记，避免同一状态被多个前驱重复加入。DFS 适合连通、子树与枚举，但 visited 是否撤销取决于它是全局访问状态还是当前路径状态。

### 60 秒扩展要点

- 单调栈/队列只保留仍可能成为答案的候选，要说明保存值还是下标、单调方向以及元素何时过期。
- BFS 的“第一次到达最短”只适用于正确建模的无权/等权状态图；非负带权图通常考虑 Dijkstra，不能继续用普通队列。
- BFS 应在入队时标记；出队才标记会让同一节点被多个前驱反复入队，增加时间和空间。
- DFS 的全局 visited 通常不撤销；回溯的 path-local 选择需要在兄弟分支前恢复。深图递归可能栈溢出，可改显式栈。
- 前/中/后序表示根相对左右子树的访问时机；BST 中序有序还依赖树确实满足 BST 与重复键约定。
- visited 的键必须包含决定未来的完整状态；坐标相同但钥匙、方向或剩余资源不同，可能是不同搜索状态。

### 120 秒场景闭环

题目给出只含 0/1 的矩形网格，0 可走、1 障碍，求从左上到右下四方向移动的最少步数；非法网格抛异常，起点或终点阻塞及无路返回 -1，并且不能修改输入。每个可走格子是状态，四方向移动都是代价 1，因此用 BFS；`distance != -1` 同时表示已入队并记录首次距离。由于队列按非递减距离出队，第一次取到终点的距离最小；每格最多入队一次，所以时间和额外空间都是 `O(rows × cols)`。若方向带不同权重，这个证明失效，应换带权最短路算法。

### Java 参考实现

```java
import java.util.ArrayDeque;
import java.util.Arrays;
import java.util.Deque;

public class GridShortestPath {
    private static final int[][] DIRECTIONS = {
        {1, 0}, {-1, 0}, {0, 1}, {0, -1}
    };

    public static int shortestPath(int[][] grid) {
        validate(grid);
        int rows = grid.length;
        int cols = grid[0].length;
        if (grid[0][0] == 1 || grid[rows - 1][cols - 1] == 1) {
            return -1;
        }

        int[][] distance = new int[rows][cols];
        for (int[] row : distance) {
            Arrays.fill(row, -1);
        }

        Deque<int[]> queue = new ArrayDeque<>();
        queue.offer(new int[] {0, 0});
        distance[0][0] = 0;

        while (!queue.isEmpty()) {
            int[] current = queue.poll();
            int row = current[0];
            int col = current[1];
            if (row == rows - 1 && col == cols - 1) {
                return distance[row][col];
            }

            for (int[] direction : DIRECTIONS) {
                int nextRow = row + direction[0];
                int nextCol = col + direction[1];
                if (nextRow < 0 || nextRow >= rows
                        || nextCol < 0 || nextCol >= cols
                        || grid[nextRow][nextCol] == 1
                        || distance[nextRow][nextCol] != -1) {
                    continue;
                }
                distance[nextRow][nextCol] = distance[row][col] + 1;
                queue.offer(new int[] {nextRow, nextCol});
            }
        }
        return -1;
    }

    private static void validate(int[][] grid) {
        if (grid == null || grid.length == 0
                || grid[0] == null || grid[0].length == 0) {
            throw new IllegalArgumentException("grid must be non-empty");
        }
        int cols = grid[0].length;
        for (int[] row : grid) {
            if (row == null || row.length != cols) {
                throw new IllegalArgumentException("grid must be rectangular");
            }
            for (int cell : row) {
                if (cell != 0 && cell != 1) {
                    throw new IllegalArgumentException("cell must be 0 or 1");
                }
            }
        }
    }

    private static void check(int expected, int actual) {
        if (expected != actual) {
            throw new AssertionError("expected=" + expected + ", actual=" + actual);
        }
    }

    private static void expectInvalid(Runnable action) {
        try {
            action.run();
            throw new AssertionError("expected IllegalArgumentException");
        } catch (IllegalArgumentException expected) {
            // 通过
        }
    }

    public static void main(String[] args) {
        check(0, shortestPath(new int[][] {{0}}));
        check(-1, shortestPath(new int[][] {{1}}));
        check(4, shortestPath(new int[][] {
            {0, 0, 1}, {1, 0, 1}, {0, 0, 0}
        }));
        check(-1, shortestPath(new int[][] {{0, 1}, {1, 0}}));
        check(3, shortestPath(new int[][] {{0, 0, 0}, {1, 1, 0}}));

        int[][] input = {{0, 0}, {1, 0}};
        int[][] snapshot = {{0, 0}, {1, 0}};
        check(2, shortestPath(input));
        if (!Arrays.deepEquals(snapshot, input)) {
            throw new AssertionError("input was modified");
        }

        expectInvalid(() -> shortestPath(null));
        expectInvalid(() -> shortestPath(new int[][] {{0}, {0, 1}}));
        expectInvalid(() -> shortestPath(new int[][] {{0, 2}}));
    }
}
```

> [!check]- L0～L4 核对
> - L0：按 LIFO/FIFO 与搜索目标选择栈、队列、DFS 或 BFS。
> - L1：解释 BFS 层序、入队标记和等权最短性，分清 DFS visited 与回溯撤销。
> - L2：覆盖带权边、完整状态键、递归深度、非法输入和输入不变性。
> - L3：能手写、编译并运行单点、阻塞、无路、矩形、输入不变和非法网格测试。
> - L4：用队列距离单调证明最短性，并给出 `O(rows × cols)` 时间和空间。

## 核对结果回流

| 学习线 | 母题 | 与参考答案的首个差异 | 失败层 | 等级 | 最小修复节点 |
| --- | --- | --- | --- | --- | --- |
| ① Java 基础与运行时 | J2 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ② Spring 与数据访问 | S3 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ③ MySQL | M1 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ④ Redis | R1 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ⑤ Web、项目与工程 | W2 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |
| ⑥ 算法与手写 | A3 |  | L0 / L1 / L2 / L3 / L4 | A / B / C / 通过 |  |

完成核对后，只把真实首个断点同步到 [[00-第二轮学习队列#六线证据队列]]。答案参考覆盖不等于闭卷通过，也不自动生成个人薄弱点题单。
